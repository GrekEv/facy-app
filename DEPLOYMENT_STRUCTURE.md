# 📁 Структура файлов для деплоя

## 🔧 Конфигурационные файлы

### Для Railway
- **`Procfile`** - Определяет процессы (web и worker)
- **`railway.json`** - Конфигурация Railway (опционально)
- **`railway.toml`** - Альтернативная конфигурация Railway

### Для Vercel
- **`vercel.json`** - Конфигурация маршрутов и проксирования

### Для Git
- **`.gitignore`** - Игнорируемые файлы (не коммитить .env, данные и т.д.)

## 📝 Документация

- **`DEPLOY.md`** - Подробная инструкция по деплою
- **`QUICK_DEPLOY.md`** - Быстрая инструкция за 5 минут
- **`ENV_EXAMPLE.txt`** - Пример переменных окружения

## 🚀 Как это работает

### Railway (Бэкенд)
1. Railway читает `Procfile`
2. Запускает `web` процесс (FastAPI) на порту из переменной `PORT`
3. Запускает `worker` процесс (Telegram бот)
4. Предоставляет URL вида: `https://your-app.up.railway.app`

### Vercel (Фронтенд)
1. Vercel читает `vercel.json`
2. Хостит статические файлы из `static/` и `templates/`
3. Проксирует `/api/*` запросы на Railway
4. Предоставляет URL вида: `https://your-app.vercel.app`

### Интеграция
- Vercel URL указывается в `WEBAPP_URL` в Railway
- Telegram бот использует `WEBAPP_URL` для открытия Web App
- Все API запросы идут через Vercel прокси на Railway

## ⚙️ Переменные окружения

Все переменные настраиваются в Railway → Settings → Variables

**Обязательные:**
- `BOT_TOKEN` - Токен от BotFather
- `WEBAPP_URL` - URL от Vercel

**Автоматические (Railway):**
- `PORT` - Порт для веб-сервера (устанавливается автоматически)

**Опциональные:**
- `DEEPFACE_API_KEY` - Для реальной работы Face Swap
- `FFANS_API_KEY` - Для реальной генерации
- `DATABASE_URL` - PostgreSQL URL (если создали базу)
- `ENVIRONMENT` - production/development
- `ADMIN_IDS` - ID администраторов

## 📦 Что деплоится

### Railway получает:
- Весь код проекта
- Python зависимости из `requirements.txt`
- Запускает процессы из `Procfile`

### Vercel получает:
- Статические файлы (`static/`, `templates/`)
- Конфигурацию маршрутов (`vercel.json`)

## 🔄 Процесс деплоя

1. **GitHub** - Код загружается в репозиторий
2. **Railway** - Автоматически деплоит при пуше в main ветку
3. **Vercel** - Автоматически деплоит при пуше в main ветку
4. **Обновление** - После деплоя обновите `WEBAPP_URL` в Railway

## 🐛 Отладка

### Логи Railway
Railway Dashboard → Deployments → View Logs

### Логи Vercel
Vercel Dashboard → Deployments → View Logs

### Проверка API
```bash
curl https://your-app.up.railway.app/health
```

### Проверка Web App
Откройте в браузере: `https://your-app.vercel.app`

