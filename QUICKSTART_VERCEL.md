# ⚡ Быстрый старт на Vercel - 10 минут

## 🎯 Что нужно сделать

### Шаг 1: Добавьте mangum в requirements.txt ✅

Уже добавлено! Проверьте, что в `requirements.txt` есть:
```
mangum>=0.17.0
```

### Шаг 2: Настройте базу данных

**Важно:** SQLite НЕ работает на Vercel! Нужен PostgreSQL.

#### Вариант A: Vercel Postgres (самый простой)

1. В Vercel Dashboard → ваш проект → **Storage** → **Create Database**
2. Выберите **Postgres**
3. Скопируйте `POSTGRES_URL` из переменных окружения

#### Вариант B: Внешний PostgreSQL

Используйте любой из:
- Railway PostgreSQL
- Supabase
- Neon
- PlanetScale

### Шаг 3: Настройте переменные окружения

В Vercel Dashboard → **Settings** → **Environment Variables**:

```bash
# ОБЯЗАТЕЛЬНЫЕ
BOT_TOKEN=ваш_токен_бота
WEBAPP_URL=https://your-app.vercel.app
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname

# Или для Vercel Postgres:
# DATABASE_URL=$POSTGRES_URL  (Vercel автоматически подставит)

# API ключи (если есть)
REPLICATE_API_KEY=your_key
OPENAI_API_KEY=your_key
```

### Шаг 4: Деплой

#### Через GitHub (рекомендуется):

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Add Vercel configuration"
   git push
   ```

2. В Vercel Dashboard:
   - **Add New Project**
   - Подключите ваш GitHub репозиторий
   - Vercel автоматически определит конфигурацию
   - Нажмите **Deploy**

#### Через Vercel CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Деплой
vercel

# Продакшен
vercel --prod
```

### Шаг 5: Настройте домен (если есть)

1. Vercel Dashboard → **Settings** → **Domains**
2. Добавьте ваш домен (например, `onlyface.art`)
3. Настройте DNS записи
4. Обновите `WEBAPP_URL` в переменных окружения

---

## ✅ Проверка

После деплоя:

```bash
# Проверка API
curl https://your-app.vercel.app/health
# Должно вернуть: {"status":"healthy"}
```

---

## ⚠️ Важные моменты

### 1. База данных
- ❌ **SQLite не работает** на Vercel
- ✅ **Используйте PostgreSQL**

### 2. Файлы
- ❌ Локальные файлы (`/uploads`, `/generated`) не сохраняются
- ✅ Используйте Vercel Blob Storage или S3 (если нужны загрузки)

### 3. Бот
- Бот должен работать отдельно (не на Vercel)
- Запустите бота локально или на другом сервере
- Укажите `WEBAPP_URL` на ваш Vercel URL

---

## 🚀 Готово!

Теперь ваш бэкенд работает на Vercel! 🎉

**Следующие шаги:**
1. Настройте бота (локально или на другом сервере)
2. Укажите `WEBAPP_URL` в BotFather
3. Протестируйте работу приложения

---

## 📚 Подробная документация

См. [DEPLOY_VERCEL.md](DEPLOY_VERCEL.md) для детальной информации.

