# Полная инструкция: Настройка PostgreSQL на Timeweb

## Шаг 1: Создание базы данных в панели Timeweb

### 1.1. Вход в панель
1. Откройте https://timeweb.cloud/
2. Войдите в аккаунт

### 1.2. Создание базы данных
1. В левом меню найдите **"Базы данных"**
2. Нажмите **"Создать базу данных"** или **"Заказать"**
3. Выберите:
   - **Тип:** PostgreSQL 18
   - **Регион:** Выберите ближайший (например, Нидерланды)
   - **Конфигурация:** Минимум 1 CPU, 1 GB RAM, 8 GB NVMe

### 1.3. Настройка сети

**Вариант A: Публичный IP (рекомендуется)**
- Нажмите **"+ Публичный IP"**
- Стоимость: ~150 ₽/мес
- **Преимущества:** Простое подключение с любого сервера
- **Недостатки:** Дополнительная стоимость

**Вариант B: Приватная сеть**
- Нажмите **"+ Добавить сеть"**
- Выберите существующую приватную сеть или создайте новую
- Укажите приватный IP (например: 192.168.0.4)
- **Преимущества:** Бесплатно, безопаснее
- **Недостатки:** Работает только внутри приватной сети Timeweb

### 1.4. Дополнительные услуги
- **Бэкапы:** Рекомендуется включить (автоматические резервные копии)

### 1.5. Завершение
- Нажмите **"Заказать"**
- Дождитесь создания базы данных (обычно 1-2 минуты)

## Шаг 2: Получение данных подключения

### 2.1. Откройте базу данных
1. В списке баз данных найдите созданную базу
2. Откройте её (кликните на название)
3. Перейдите на вкладку **"Подключение"**

### 2.2. Настройте параметры подключения
В разделе **"Способ подключения"**:
- Выберите **"Командная строка"**
- **Тип подключения:** "Домен"
- **Пользователь:** выберите пользователя (обычно `gen_user`)
- **База данных:** выберите базу данных (обычно `default_db`)

### 2.3. Скопируйте данные
Из команды `psql` скопируйте:
- **Хост:** `ad9d6b1abc9d6aa538e0dea5.twc1.net` (формат `xxxxx.twc1.net`)
- **Порт:** `5432`
- **Пользователь:** `gen_user` (или другой)
- **База данных:** `default_db` (или другая)
- **Пароль:** нажмите на иконку глаза 👁️, чтобы увидеть пароль

**Пример команды подключения:**
```bash
psql 'postgresql://gen_user:пароль@ad9d6b1abc9d6aa538e0dea5.twc1.net:5432/default_db?sslmode=verify-full'
```

## Шаг 3: Установка SSL сертификата

Timeweb требует SSL сертификат для безопасного подключения.

### Вариант A: Автоматическая установка на сервере

```bash
ssh root@72.56.85.215

# Скачайте скрипт установки
wget https://raw.githubusercontent.com/GrekEv/facy-app/main/install_db_certificate.sh
chmod +x install_db_certificate.sh
./install_db_certificate.sh
```

Скрипт автоматически:
- Скачает сертификат с Timeweb
- Установит в `~/.cloud-certs/root.crt`
- Настроит права доступа
- Добавит переменную окружения

### Вариант B: Загрузка с локального компьютера

Если вы скачали сертификат `ca.crt` на компьютер:

```bash
# На вашем компьютере (в директории где лежит ca.crt)
# Скачайте скрипт загрузки
wget https://raw.githubusercontent.com/GrekEv/facy-app/main/upload_and_install_cert.sh
chmod +x upload_and_install_cert.sh

# Запустите (он загрузит ca.crt на сервер)
./upload_and_install_cert.sh
```

### Вариант C: Ручная установка

```bash
ssh root@72.56.85.215

# Создайте директорию
mkdir -p ~/.cloud-certs

# Скачайте сертификат
curl -o ~/.cloud-certs/root.crt "https://st.timeweb.com/cloud-static/ca.crt"

# Установите права
chmod 0600 ~/.cloud-certs/root.crt

# Добавьте в .bashrc
echo "" >> ~/.bashrc
echo "# Timeweb PostgreSQL SSL Certificate" >> ~/.bashrc
echo "export PGSSLROOTCERT=\$HOME/.cloud-certs/root.crt" >> ~/.bashrc

# Примените для текущей сессии
export PGSSLROOTCERT=$HOME/.cloud-certs/root.crt
```

### Проверка установки сертификата

```bash
# Проверьте наличие файла
ls -la ~/.cloud-certs/root.crt

# Должно показать:
# -rw------- 1 root root 1675 Nov 27 15:18 /root/.cloud-certs/root.crt
```

## Шаг 4: Настройка DATABASE_URL на сервере

### 4.1. Подключитесь к серверу

```bash
ssh root@72.56.85.215
cd ~/facy-app
```

### 4.2. Откройте .env для редактирования

```bash
nano .env
```

### 4.3. Удалите старую строку с Yandex Cloud

Найдите и удалите (или закомментируйте):
```
DATABASE_URL=postgresql+asyncpg://facy_user:etxX4gk272PdJYH@rc1a-6t9pb3se81b4idf5.mdb.yandexcloud.net:6432/facy_db?ssl=require
```

### 4.4. Добавьте новую строку для Timeweb

Добавьте в конец файла (замените на ваши данные):
```
DATABASE_URL=postgresql+asyncpg://gen_user:ваш_пароль@ad9d6b1abc9d6aa538e0dea5.twc1.net:5432/default_db?ssl=require
```

**Важно:**
- Формат хоста: `xxxxx.twc1.net` (не `postgres-xxx.timeweb.cloud`)
- Формат: `postgresql+asyncpg://` (не просто `postgresql://`)
- Параметр `?ssl=require` обязателен
- Если пароль содержит специальные символы (`@`, `+`, `~`, `#`), можно использовать как есть - Python обработает автоматически

**Пример с реальными данными:**
```
DATABASE_URL=postgresql+asyncpg://gen_user:e%7D%2B%400Go~xgGcwt@ad9d6b1abc9d6aa538e0dea5.twc1.net:5432/default_db?ssl=require
```

### 4.5. Сохраните файл

- `Ctrl+O` (сохранить)
- `Enter` (подтвердить имя файла)
- `Ctrl+X` (выйти)

### 4.6. Проверьте содержимое .env

```bash
# Покажите DATABASE_URL (скрывая пароль)
grep DATABASE_URL .env | sed 's/:[^:@]*@/:***@/'
```

## Шаг 5: Перезапуск контейнеров

### 5.1. Определите команду docker compose

```bash
if command -v docker-compose &> /dev/null; then
    COMPOSE="docker-compose"
else
    COMPOSE="docker compose"
fi
```

### 5.2. Остановите контейнеры

```bash
$COMPOSE -f docker-compose.prod.yml down
```

### 5.3. Запустите заново

```bash
$COMPOSE -f docker-compose.prod.yml up -d
```

### 5.4. Подождите запуска

```bash
echo "Ожидание запуска API (30 секунд)..."
sleep 30
```

### 5.5. Проверьте статус

```bash
# Статус контейнеров
$COMPOSE -f docker-compose.prod.yml ps

# Должны быть в статусе "Up" или "Running"
```

## Шаг 6: Проверка подключения

### 6.1. Проверка health endpoint

```bash
# На сервере
curl http://localhost:8000/api/health

# Должен вернуть JSON:
# {
#   "status": "ok",
#   "database": "connected" или "ok",
#   ...
# }
```

### 6.2. Проверка через домен

```bash
# С вашего компьютера
curl https://onlyface.art/api/health
```

### 6.3. Проверка логов

```bash
# Логи API контейнера
$COMPOSE -f docker-compose.prod.yml logs api | grep -i database

# Должны быть сообщения об успешном подключении
```

## Решение проблем

### Проблема: "Connection refused"

**Причины:**
- База данных не запущена в панели Timeweb
- Неправильный хост или порт
- Firewall блокирует подключение

**Решение:**
1. Проверьте статус базы данных в панели Timeweb
2. Убедитесь, что хост имеет формат `xxxxx.twc1.net`
3. Проверьте, что порт `5432` открыт

### Проблема: "Authentication failed"

**Причины:**
- Неправильный пароль
- Неправильный пользователь

**Решение:**
1. Проверьте пароль в панели Timeweb (нажмите на иконку глаза)
2. Убедитесь, что используете правильного пользователя (`gen_user`)

### Проблема: "SSL connection required"

**Причины:**
- Отсутствует параметр `?ssl=require` в DATABASE_URL
- Неправильный формат URL

**Решение:**
1. Убедитесь, что используете формат `postgresql+asyncpg://`
2. Добавьте `?ssl=require` в конец URL

### Проблема: "Database does not exist"

**Причины:**
- Неправильное имя базы данных

**Решение:**
1. Проверьте имя базы данных в панели Timeweb
2. Обычно это `default_db` или имя, которое вы указали при создании

## Альтернатива: SQLite

Если не хотите настраивать PostgreSQL, можно использовать SQLite:

```bash
ssh root@72.56.85.215
cd ~/facy-app

# Удалите DATABASE_URL из .env
sed -i '/^DATABASE_URL=/d' .env

# Создайте директорию для БД
mkdir -p data

# Перезапустите
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml up -d
```

SQLite создастся автоматически в `data/app.db`.

## Важные моменты

1. **Формат хоста:** Timeweb использует формат `xxxxx.twc1.net`, а не `postgres-xxx.timeweb.cloud`
2. **SSL обязателен:** Всегда используйте `?ssl=require` в DATABASE_URL
3. **Формат для asyncpg:** Используйте `postgresql+asyncpg://`, а не просто `postgresql://`
4. **Сертификат:** Для Python приложений с asyncpg сертификат не обязателен, но рекомендуется установить
5. **Пароль:** Если пароль содержит специальные символы, можно использовать как есть

## Быстрая проверка всех настроек

```bash
ssh root@72.56.85.215
cd ~/facy-app

# 1. Проверка сертификата
ls -la ~/.cloud-certs/root.crt

# 2. Проверка DATABASE_URL
grep DATABASE_URL .env | sed 's/:[^:@]*@/:***@/'

# 3. Проверка контейнеров
docker compose -f docker-compose.prod.yml ps

# 4. Проверка подключения
curl http://localhost:8000/api/health
```

## Готово!

После выполнения всех шагов ваше приложение должно успешно подключаться к PostgreSQL на Timeweb.

