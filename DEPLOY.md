# 🚀 Инструкция по деплою Facy на GitHub + Vercel + Railway

Это руководство поможет вам развернуть Telegram Mini App Facy в продакшене.

## 📋 Архитектура деплоя

- **GitHub** - репозиторий с кодом
- **Railway** - хостинг FastAPI бэкенда и Telegram бота
- **Vercel** - опционально для статических файлов (или используем Railway)

## 🔧 Шаг 1: Подготовка GitHub репозитория

### 1.1 Создайте репозиторий на GitHub

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Facy Telegram Mini App"

# Добавьте remote репозиторий (замените на ваш URL)
git remote add origin https://github.com/ваш-username/facy-app.git

# Запушьте код
git branch -M main
git push -u origin main
```

### 1.2 Проверьте .gitignore

Убедитесь, что `.gitignore` содержит:
- `.env` файлы
- `data/`, `uploads/`, `generated/` директории
- Python кэш файлы

## 🚂 Шаг 2: Деплой на Railway

### 2.1 Создайте аккаунт на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий

### 2.2 Настройте переменные окружения

В Railway перейдите в Settings → Variables и добавьте:

```env
# Обязательные
BOT_TOKEN=ваш_токен_от_BotFather
WEBAPP_URL=https://ваш-vercel-url.vercel.app

# Опциональные (для реальной работы)
DEEPFACE_API_KEY=ваш_ключ_deepface
DEEPFACE_API_URL=https://deepfacevideo.com/api
FFANS_API_KEY=ваш_ключ_ffans
FFANS_API_URL=https://ffans.ai/api

# База данных (Railway автоматически создаст PostgreSQL)
# Оставьте пустым для SQLite или укажите PostgreSQL URL от Railway

# Администраторы (через запятую)
ADMIN_IDS=ваш_telegram_id

# Режим (production для продакшена)
ENVIRONMENT=production
```

**Важно:** Railway автоматически предоставляет переменную `PORT`, она будет использована автоматически.

### 2.3 Настройте базу данных (опционально)

Railway может создать PostgreSQL базу данных:

1. В проекте Railway нажмите "New" → "Database" → "Add PostgreSQL"
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Обновите `config.py` если нужно использовать PostgreSQL вместо SQLite

### 2.4 Настройте сервисы

Railway автоматически определит два процесса из `Procfile`:
- **web** - FastAPI сервер (порт из переменной PORT)
- **worker** - Telegram бот

Если нужно запустить только один процесс, измените `Procfile`:

```procfile
web: python run_api.py && python main.py
```

Или создайте два отдельных сервиса в Railway.

### 2.5 Получите URL вашего API

После деплоя Railway предоставит URL вида:
```
https://ваш-проект.up.railway.app
```

Этот URL будет использоваться для API запросов.

## ▲ Шаг 3: Деплой на Vercel (опционально)

### 3.1 Создайте аккаунт на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "Add New Project"
4. Импортируйте ваш репозиторий

### 3.2 Настройте проект

Vercel автоматически определит конфигурацию из `vercel.json`.

**Важно:** Если вы используете Vercel только для статики, вам нужно настроить проксирование API запросов на Railway.

Обновите `vercel.json`:

```json
{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://ваш-проект.up.railway.app/api/:path*"
    },
    {
      "source": "/static/:path*",
      "destination": "/static/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/templates/index.html"
    }
  ]
}
```

### 3.3 Получите URL Vercel

После деплоя Vercel предоставит URL вида:
```
https://ваш-проект.vercel.app
```

Этот URL нужно указать в `WEBAPP_URL` в Railway.

## 🤖 Шаг 4: Настройка Telegram бота

### 4.1 Создайте бота через BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Введите название: `Facy`
4. Введите username: `facy_bot` (или другой доступный)
5. Скопируйте токен и добавьте в Railway переменные как `BOT_TOKEN`

### 4.2 Настройте Web App

1. В BotFather отправьте `/mybots`
2. Выберите вашего бота
3. Выберите "Bot Settings" → "Menu Button"
4. Включите "Menu Button"
5. Укажите URL: `https://ваш-проект.vercel.app` (или Railway URL если не используете Vercel)

### 4.3 Настройте команды бота

Команды уже настроены в коде:
- `/start` - Главное меню
- `/balance` - Мой баланс
- `/help` - Помощь

## 🔄 Шаг 5: Обновление переменных окружения

После получения URL от Vercel, обновите в Railway:

```env
WEBAPP_URL=https://ваш-проект.vercel.app
```

Railway автоматически перезапустит сервисы.

## ✅ Шаг 6: Проверка работы

### 6.1 Проверьте API

Откройте в браузере:
```
https://ваш-проект.up.railway.app/health
```

Должен вернуться: `{"status": "healthy"}`

### 6.2 Проверьте Web App

Откройте:
```
https://ваш-проект.vercel.app
```

Должна открыться главная страница Facy.

### 6.3 Проверьте бота

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите "🚀 Открыть приложение"
4. Должно открыться Web App

## 🔧 Альтернативный вариант: Только Railway

Если не хотите использовать Vercel, можно хостить всё на Railway:

1. Railway будет хостить FastAPI (который отдает статику)
2. Укажите Railway URL в `WEBAPP_URL`
3. Настройте Menu Button в BotFather на Railway URL

Railway URL будет выглядеть как:
```
https://ваш-проект.up.railway.app
```

## 📝 Важные замечания

### Переменные окружения

Railway автоматически предоставляет:
- `PORT` - порт для веб-сервера (используется автоматически)
- `RAILWAY_ENVIRONMENT` - окружение (production/development)

### База данных

- По умолчанию используется SQLite (файл `data/app.db`)
- Для продакшена рекомендуется PostgreSQL
- Railway может создать PostgreSQL автоматически

### Файлы

Директории `uploads/` и `generated/` должны быть доступны через веб.
Railway монтирует их как статические файлы через FastAPI.

### Логи

Логи доступны в Railway Dashboard → Deployments → View Logs

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте `BOT_TOKEN` в Railway переменных
2. Проверьте логи в Railway Dashboard
3. Убедитесь, что worker процесс запущен

### Web App не открывается

1. Проверьте `WEBAPP_URL` в Railway переменных
2. Убедитесь, что URL доступен (откройте в браузере)
3. Проверьте настройки Menu Button в BotFather

### API не работает

1. Проверьте логи в Railway Dashboard
2. Проверьте переменную `PORT` (Railway устанавливает автоматически)
3. Убедитесь, что web процесс запущен

### CORS ошибки

CORS уже настроен в `api/main.py` для всех источников (`allow_origins=["*"]`).

## 🔐 Безопасность

1. **Никогда не коммитьте `.env` файлы**
2. Используйте переменные окружения в Railway
3. Для продакшена используйте PostgreSQL вместо SQLite
4. Настройте `ADMIN_IDS` для доступа к админ-функциям

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app)
- [Vercel Documentation](https://vercel.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Web Apps](https://core.telegram.org/bots/webapps)

---

**Готово!** Ваше приложение Facy развернуто и готово к использованию! 🎉

