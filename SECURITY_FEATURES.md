# 🛡️ Система безопасности и модерации контента

## ✅ Что было реализовано

### 1. 🔍 Автоматическая фильтрация контента

**Файл:** `services/content_moderation_service.py`

#### Функции:
- ✅ Проверка текстовых промптов на запрещенные слова
- ✅ Обнаружение попыток обхода фильтров (пробелы, замены букв)
- ✅ Двухуровневая проверка (запрещенные + подозрительные слова)
- ✅ Логирование всех нарушений

#### Запрещенные категории:
```python
- NSFW контент (porn, nsfw, nude, sex, эротика, etc.)
- Насилие (violence, blood, weapon, кровь, оружие)
- Наркотики (drugs, наркотики)
- Разжигание ненависти (hate, ненависть)
```

### 2. 🌐 Интеграция в API

**Файл:** `api/main.py`

```python
# Проверка ПЕРЕД обработкой запроса
is_allowed, reason = content_moderation.check_text_content(request.prompt)
if not is_allowed:
    # Логирование нарушения
    content_moderation.log_violation(...)
    # Отклонение запроса
    raise HTTPException(status_code=400, detail="Запрос отклонен")
```

#### Что происходит:
1. **Проверка промпта** перед отправкой в AI
2. **Логирование нарушения** (user_id, промпт, причина, время)
3. **Отклонение запроса** с объяснением причины
4. **БЕЗ списания поинтов** за заблокированный запрос

### 3. 🖥️ Клиентская фильтрация

**Файл:** `static/js/app.js`

```javascript
// Проверка на клиенте ДО отправки на сервер
function checkContentSafety(text) {
    const bannedKeywords = ['porn', 'порно', 'xxx', ...];
    // Быстрая проверка на клиенте
}
```

#### Преимущества:
- ⚡ Мгновенная обратная связь пользователю
- 💰 Экономия трафика (не отправляем запрещенные запросы)
- 🔒 Двойная защита (клиент + сервер)

### 4. 🤖 Telegram бот - Политика контента

**Файл:** `handlers/content_policy.py`

#### Новые функции в боте:
- Кнопка "🔒 Политика контента" в меню помощи
- Подробное описание правил
- Подтверждение принятия пользователем
- Легкий доступ к информации о правилах

### 5. 📋 Документация

#### Созданные документы:

**CONTENT_POLICY.md** - Политика контента
- Что запрещено (подробно)
- Что разрешено
- Система наказаний
- Юридическая информация

**LEGAL_NOTICE.md** - Юридическое уведомление
- Отказ от ответственности
- Соответствие законодательству
- Защита данных
- Контакты для правоохранительных органов

**SECURITY_FEATURES.md** - Этот файл
- Описание системы безопасности
- Технические детали
- Инструкции для администраторов

## 🔧 Техническая архитектура защиты

```
Пользователь
    ↓
[Клиентская проверка JS]  ← Первый уровень защиты
    ↓ (если прошел)
Отправка на сервер
    ↓
[FastAPI валидация]        ← Второй уровень
    ↓
[Content Moderation Service] ← Третий уровень (основной)
    ↓
    ├─→ Разрешено → Обработка AI API
    │
    └─→ Запрещено → Логирование + Отклонение
            ↓
        [Журнал нарушений]
            ↓
        [Система наказаний]
```

## 📊 Логирование нарушений

### Что логируется:

```python
{
    "timestamp": "2025-11-09 10:30:45",
    "user_id": 123456789,
    "content_type": "image_generation",
    "prompt": "запрещенный промпт...",
    "reason": "Обнаружено недопустимое содержание: 'porn'",
    "ip_address": "xxx.xxx.xxx.xxx",
    "action": "blocked"
}
```

### Где хранится:
- Файл логов: `logs/violations.log`
- База данных: таблица `content_violations` (можно добавить)
- Rotation: 90 дней

## 🚨 Система наказаний

### Реализовано в коде:

```python
# В базе данных User:
violation_count: int = 0
is_banned: bool = False
ban_reason: str = None
banned_until: datetime = None
```

### Автоматические действия:

**1-е нарушение:**
```python
user.violation_count = 1
# Отправка предупреждения в Telegram
await bot.send_message(user_id, "⚠️ Предупреждение...")
```

**2-е нарушение:**
```python
user.violation_count = 2
user.banned_until = datetime.now() + timedelta(days=7)
# Временная блокировка
```

**3-е нарушение:**
```python
user.violation_count = 3
user.is_banned = True
user.ban_reason = "Множественные нарушения политики контента"
# Постоянный бан
```

## 🔐 Дополнительные меры безопасности

### 1. Rate Limiting
```python
# TODO: Добавить в будущих версиях
@limiter.limit("10/minute")
async def generate_image(...):
    ...
```

### 2. IP Ban-листы
```python
# TODO: Блокировка подозрительных IP
BANNED_IPS = set()
```

### 3. Captcha для подозрительных
```python
# TODO: После N нарушений требовать captcha
if user.violation_count >= 1:
    await send_captcha(user_id)
```

### 4. AI-модерация изображений
```python
# TODO: Интеграция с Google Vision API
result = await vision_api.detect_safe_search(image_url)
if not result.safe:
    block_user()
```

## 📱 Пользовательский опыт

### Когда запрос заблокирован:

**Web App:**
```
⛔ Обнаружено недопустимое содержание.
Пожалуйста, ознакомьтесь с политикой контента.
```

**Telegram Bot:**
```
❌ Запрос отклонен: Обнаружено недопустимое содержание: 'porn'

Пожалуйста, ознакомьтесь с политикой контента (/help в боте).
```

### Как пользователь узнает правила:

1. При первом запуске бота
2. Кнопка "🔒 Политика контента" в меню
3. При попытке нарушения
4. В README и документации

## 🔍 Мониторинг

### Метрики для отслеживания:

```python
# Ежедневная статистика
daily_stats = {
    "total_requests": 1000,
    "blocked_requests": 15,
    "block_rate": 1.5%,
    "unique_violators": 8,
    "banned_users": 2
}
```

### Алерты:

- 📧 Email при блокировке пользователя
- 📱 Telegram уведомления администраторам
- 📊 Еженедельный отчет о нарушениях

## 🛠️ Для администраторов

### Просмотр логов нарушений:

```bash
# Просмотр всех нарушений
tail -f logs/violations.log

# Поиск по user_id
grep "123456789" logs/violations.log

# Статистика за сегодня
grep "$(date +%Y-%m-%d)" logs/violations.log | wc -l
```

### Разблокировка пользователя:

```python
# Через API или БД
user = await get_user(telegram_id)
user.is_banned = False
user.banned_until = None
user.violation_count = 0
await session.commit()
```

### Добавление новых запрещенных слов:

```python
# Файл: services/content_moderation_service.py
BANNED_KEYWORDS = [
    'существующие слова',
    'новое_слово',  # добавить сюда
]
```

## ✅ Тестирование системы

### Как протестировать фильтрацию:

```python
# 1. Клиент (Web App)
# Откройте консоль браузера:
checkContentSafety("test porn image")  // должно вернуть false

# 2. Сервер (API)
# Отправьте запрос с запрещенным словом:
curl -X POST http://localhost:8000/api/generate/image \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123, "prompt": "porn image"}'
# Должен вернуть 400 Bad Request
```

### Проверка логирования:

```bash
# Запустите приложение
python run_api.py

# В другом терминале проверьте логи
tail -f logs/app.log | grep "violation"
```

## 📈 Метрики эффективности

**Цели:**
- ✅ 99%+ блокировка явных нарушений
- ✅ < 1% ложных срабатываний
- ✅ Ответ в течение 100ms
- ✅ 0 NSFW контента в продакшене

## 🔮 Будущие улучшения

### Планируется добавить:

1. **AI-модерация изображений**
   - Google Cloud Vision API
   - AWS Rekognition
   - Детекция NSFW на результатах

2. **Машинное обучение**
   - ML модель для определения паттернов обхода
   - Обучение на логах нарушений
   - Предсказание потенциальных нарушителей

3. **Blockchain логирование**
   - Неизменяемая запись нарушений
   - Для юридических доказательств

4. **Интеграция с правоохранительными органами**
   - API для передачи данных
   - Автоматические отчеты о серьезных нарушениях

## 📞 Контакты безопасности

**Сообщить об уязвимости:**
- Email: security@yourservice.com

**Сообщить о нарушении:**
- Email: abuse@yourservice.com
- Telegram: @your_support

---

## ✅ Итоги

### Что реализовано:

✅ Автоматическая фильтрация текста (клиент + сервер)  
✅ Логирование всех нарушений  
✅ Отклонение запрещенных запросов  
✅ Политика контента в боте  
✅ Полная документация (3 файла)  
✅ Предупреждения в README  
✅ Система наказаний (готова к расширению)  

### Защита от:

🛡️ NSFW контента  
🛡️ Насилия  
🛡️ Незаконной деятельности  
🛡️ Попыток обхода фильтров  
🛡️ Массовых нарушений  

---

**⚖️ Система безопасности работает на всех уровнях приложения!**

