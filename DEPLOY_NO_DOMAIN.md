# 🚀 Развертывание без домена - Telegram Mini App

Для Telegram Mini Apps **обязательно нужен HTTPS**. Вот несколько вариантов развертывания без покупки домена:

---

## 🟢 Вариант 1: Railway (РЕКОМЕНДУЕТСЯ) ⭐

**Railway** предоставляет бесплатный хостинг с автоматическим HTTPS и доменом.

### Преимущества:
- ✅ Бесплатный план: $5 кредитов в месяц
- ✅ Автоматический HTTPS
- ✅ Автоматический домен (например: `your-app.up.railway.app`)
- ✅ Простое развертывание через GitHub
- ✅ Поддержка Docker
- ✅ База данных PostgreSQL (опционально)

### Шаги развертывания:

1. **Зарегистрируйтесь на Railway:**
   - Откройте https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   ```
   BOT_TOKEN=ваш_токен_бота
   WEBAPP_URL=https://your-app.up.railway.app
   DATABASE_URL=sqlite+aiosqlite:///./data/app.db
   ```

4. **Настройте деплой:**
   - Railway автоматически определит Dockerfile
   - Или создайте `railway.json`:
   ```json
   {
     "build": {
       "builder": "DOCKERFILE",
       "dockerfilePath": "Dockerfile"
     },
     "deploy": {
       "startCommand": "python run_api.py",
       "restartPolicyType": "ON_FAILURE",
       "restartPolicyMaxRetries": 10
     }
   }
   ```

5. **Получите URL:**
   - После деплоя Railway даст вам URL типа: `https://your-app.up.railway.app`
   - Обновите `WEBAPP_URL` в переменных окружения

6. **Настройте бота:**
   - Откройте @BotFather в Telegram
   - `/mybots` → выберите бота
   - Bot Settings → Menu Button
   - URL: `https://your-app.up.railway.app`

### Запуск бота отдельно:

Для бота создайте отдельный сервис в Railway или запустите локально:

```bash
# Локально (если API на Railway)
BOT_TOKEN=ваш_токен WEBAPP_URL=https://your-app.up.railway.app python main.py
```

---

## 🟡 Вариант 2: Render

**Render** - еще один бесплатный хостинг с автоматическим HTTPS.

### Преимущества:
- ✅ Бесплатный план (с ограничениями)
- ✅ Автоматический HTTPS
- ✅ Автоматический домен (например: `your-app.onrender.com`)
- ✅ Простое развертывание

### Шаги развертывания:

1. **Зарегистрируйтесь на Render:**
   - Откройте https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - New → Web Service
   - Подключите репозиторий
   - Настройки:
     - Build Command: `pip install -r requirements.txt`
     - Start Command: `python run_api.py`
     - Environment: Python 3

3. **Настройте переменные окружения:**
   ```
   BOT_TOKEN=ваш_токен_бота
   WEBAPP_URL=https://your-app.onrender.com
   DATABASE_URL=sqlite+aiosqlite:///./data/app.db
   PORT=8000
   ```

4. **Получите URL и настройте бота** (как в Railway)

---

## 🔵 Вариант 3: ngrok (для локального тестирования)

**ngrok** создает HTTPS туннель к вашему локальному серверу.

### Преимущества:
- ✅ Мгновенный запуск
- ✅ Идеально для разработки и тестирования
- ✅ Бесплатный план доступен

### Недостатки:
- ❌ URL меняется при каждом перезапуске (на бесплатном плане)
- ❌ Не подходит для продакшена

### Шаги развертывания:

1. **Установите ngrok:**
   ```bash
   # macOS
   brew install ngrok
   
   # Linux
   wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
   tar -xzf ngrok-v3-stable-linux-amd64.tgz
   sudo mv ngrok /usr/local/bin/
   
   # Windows
   # Скачайте с https://ngrok.com/download
   ```

2. **Зарегистрируйтесь и получите токен:**
   - Откройте https://ngrok.com
   - Зарегистрируйтесь (бесплатно)
   - Скопируйте токен авторизации

3. **Авторизуйтесь:**
   ```bash
   ngrok config add-authtoken ваш_токен
   ```

4. **Запустите API локально:**
   ```bash
   python run_api.py
   ```

5. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 8000
   ```

6. **Скопируйте HTTPS URL:**
   - ngrok покажет URL типа: `https://abc123.ngrok-free.app`
   - Скопируйте этот URL

7. **Настройте переменные окружения:**
   ```bash
   export WEBAPP_URL=https://abc123.ngrok-free.app
   export BOT_TOKEN=ваш_токен_бота
   ```

8. **Настройте бота в BotFather:**
   - URL: `https://abc123.ngrok-free.app`

9. **Запустите бота:**
   ```bash
   python main.py
   ```

### Автоматический запуск с ngrok:

Создайте скрипт `start_with_ngrok.sh`:

```bash
#!/bin/bash
# Запуск API в фоне
python run_api.py &
API_PID=$!

# Ждем запуска API
sleep 3

# Запускаем ngrok
ngrok http 8000 --log=stdout > ngrok.log &
NGROK_PID=$!

# Ждем получения URL
sleep 5

# Извлекаем URL из логов
NGROK_URL=$(curl -s http://localhost:4040/api/tunnels | grep -o 'https://[^"]*\.ngrok-free\.app' | head -1)

echo "✅ API запущен на http://localhost:8000"
echo "✅ ngrok туннель: $NGROK_URL"
echo "📝 Обновите WEBAPP_URL в BotFather: $NGROK_URL"

# Запускаем бота
export WEBAPP_URL=$NGROK_URL
python main.py

# Очистка при выходе
kill $API_PID $NGROK_PID
```

---

## 🟣 Вариант 4: Cloudflare Tunnel (для VPS/сервера)

**Cloudflare Tunnel** создает безопасный туннель без необходимости открывать порты.

### Преимущества:
- ✅ Полностью бесплатно
- ✅ Стабильный URL (можно настроить свой поддомен)
- ✅ Не нужно открывать порты на сервере
- ✅ Автоматический HTTPS

### Шаги развертывания:

1. **Установите cloudflared:**
   ```bash
   # Linux
   curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
   chmod +x cloudflared
   sudo mv cloudflared /usr/local/bin/
   ```

2. **Авторизуйтесь:**
   ```bash
   cloudflared tunnel login
   ```

3. **Создайте туннель:**
   ```bash
   cloudflared tunnel create telegram-app
   ```

4. **Настройте туннель:**
   ```bash
   cloudflared tunnel route dns telegram-app your-app.yourdomain.com
   ```
   Или используйте случайный поддомен:
   ```bash
   cloudflared tunnel --url http://localhost:8000
   ```

5. **Запустите туннель:**
   ```bash
   cloudflared tunnel run telegram-app
   ```

6. **Используйте полученный URL в BotFather**

---

## 📋 Сравнение вариантов

| Вариант | Сложность | Стабильность | Стоимость | Для продакшена |
|---------|-----------|--------------|-----------|----------------|
| **Railway** | ⭐⭐ | ⭐⭐⭐⭐⭐ | Бесплатно* | ✅ Да |
| **Render** | ⭐⭐ | ⭐⭐⭐⭐ | Бесплатно* | ✅ Да |
| **ngrok** | ⭐ | ⭐⭐ | Бесплатно | ❌ Нет |
| **Cloudflare** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Бесплатно | ✅ Да |

*Бесплатные планы с ограничениями

---

## 🎯 Рекомендация

**Для быстрого старта:** используйте **Railway** или **Render** - они дают автоматический HTTPS и домен без настройки.

**Для разработки:** используйте **ngrok** для локального тестирования.

**Для продакшена:** используйте **Railway** или **Cloudflare Tunnel** на VPS.

---

## ✅ Проверка работы

После развертывания проверьте:

1. **API работает:**
   ```bash
   curl https://your-app.up.railway.app/health
   # Должен вернуть: {"status":"healthy"}
   ```

2. **Web App открывается:**
   - Откройте URL в браузере
   - Должна открыться главная страница

3. **Бот работает:**
   - Откройте бота в Telegram
   - `/start`
   - Нажмите "🚀 Открыть приложение"
   - Должно открыться Web App

---

## 🔧 Устранение проблем

### Проблема: Web App не открывается в Telegram

**Решение:**
1. Убедитесь, что URL начинается с `https://`
2. Проверьте, что Menu Button настроен в BotFather
3. Проверьте, что API отвечает: `curl https://your-url/health`

### Проблема: API не отвечает

**Решение:**
1. Проверьте логи в Railway/Render
2. Убедитесь, что порт правильный (обычно 8000)
3. Проверьте переменные окружения

### Проблема: ngrok URL меняется

**Решение:**
- На бесплатном плане URL меняется при перезапуске
- Для стабильного URL используйте Railway/Render
- Или купите платный план ngrok ($8/месяц)

---

## 📚 Дополнительные ресурсы

- [Railway Docs](https://docs.railway.app)
- [Render Docs](https://render.com/docs)
- [ngrok Docs](https://ngrok.com/docs)
- [Cloudflare Tunnel Docs](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)

