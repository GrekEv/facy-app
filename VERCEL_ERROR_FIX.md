# 🔧 Исправление ошибки 500 на Vercel

## Проблема
Serverless Function падает с ошибкой 500: FUNCTION_INVOCATION_FAILED

## Исправления

### 1. Добавлена обработка ошибок при монтировании статических файлов
- Статические файлы теперь монтируются с проверкой существования директорий
- Добавлена обработка исключений при монтировании

### 2. Улучшена обработка ошибок в api/index.py
- Добавлено логирование для отладки
- Добавлен fallback handler при ошибках инициализации
- Проверка DATABASE_URL

### 3. Изменен DATABASE_URL по умолчанию
- Теперь пустая строка вместо SQLite (SQLite не работает на serverless)
- Должен быть установлен через переменные окружения в Vercel

## Важно: Настройте переменные окружения в Vercel

### Обязательные:
```
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname
BOT_TOKEN=ваш_токен_бота
WEBAPP_URL=https://your-app.vercel.app
```

### Рекомендуемые:
```
ENVIRONMENT=production
```

## Проверка логов

В Vercel Dashboard:
1. Зайдите в ваш проект
2. Deployments → выберите последний деплой
3. Functions → api/index → View Function Logs

Там вы увидите детальные логи ошибок, если они есть.

## Если ошибка сохраняется

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что DATABASE_URL настроен правильно (PostgreSQL)
3. Проверьте, что все переменные окружения установлены
4. Убедитесь, что requirements.txt содержит все необходимые зависимости

