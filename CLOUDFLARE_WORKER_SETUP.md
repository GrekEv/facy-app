# Настройка бесплатного прокси через Cloudflare Workers

## Преимущества Cloudflare Workers

✅ **Бесплатно** - 100,000 запросов в день  
✅ **Быстро** - работает на edge сети Cloudflare  
✅ **Надежно** - инфраструктура Cloudflare  
✅ **Безопасно** - ваш собственный прокси  

## Пошаговая инструкция

### Шаг 1: Регистрация

1. Зайдите на https://workers.cloudflare.com
2. Зарегистрируйтесь или войдите в аккаунт
3. Подтвердите email (если требуется)

### Шаг 2: Создание Worker

1. Нажмите **"Create Worker"**
2. Дайте имя вашему Worker (например: `openai-proxy`)
3. Скопируйте код из файла `cloudflare-worker-proxy.js`
4. Вставьте код в редактор
5. Нажмите **"Deploy"**

### Шаг 3: Получение URL прокси

После деплоя вы получите URL вида:
```
https://openai-proxy.your-username.workers.dev
```

Этот URL и будет вашим прокси!

### Шаг 4: Настройка в приложении

Добавьте в `.env` файл:

```bash
OPENAI_PROXY=https://openai-proxy.your-username.workers.dev
```

**✅ Готово!** Код уже поддерживает Cloudflare Workers автоматически. 
Просто укажите URL Worker'а в `OPENAI_PROXY`, и всё будет работать!

## Альтернатива: Готовый прокси через Cloudflare Workers

Если не хотите настраивать сами, можно использовать готовые решения:

1. **OpenAI Proxy** - https://github.com/justjavac/openai-proxy
2. **Cloudflare OpenAI Proxy** - готовые шаблоны в Cloudflare Workers

## Важно

⚠️ Cloudflare Workers имеет ограничения:
- 100,000 запросов в день (бесплатный план)
- 10ms CPU time на запрос
- 50MB памяти

Для продакшена с большим трафиком лучше использовать платный план или свой прокси.

## Проверка работы

### Вариант 1: Через curl

```bash
curl -X POST https://openai-proxy.your-username.workers.dev/v1/images/generations \
  -H "Authorization: Bearer YOUR_OPENAI_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"dall-e-3","prompt":"a beautiful sunset","size":"1024x1024"}'
```

Если получаете ответ с URL изображения - прокси работает!

### Вариант 2: Через тестовый скрипт

1. Убедитесь, что в `.env` указан:
   ```bash
   OPENAI_API_KEY=ваш_ключ
   OPENAI_PROXY=https://openai-proxy.your-username.workers.dev
   IMAGE_GENERATION_PROVIDER=openai
   ```

2. Запустите тест:
   ```bash
   python3 test_image_generation.py
   ```

3. Если видите успешную генерацию - всё работает!

## Примеры URL Worker'ов

После создания Worker вы получите URL вида:
- `https://openai-proxy.your-username.workers.dev`
- `https://openai-proxy.account-name.workers.dev`

Просто скопируйте этот URL и добавьте в `.env` как `OPENAI_PROXY`.

## Устранение проблем

### Проблема: "Invalid endpoint"
**Решение:** Убедитесь, что используете правильный путь. Worker автоматически перенаправляет запросы к OpenAI API.

### Проблема: "Proxy error"
**Решение:** Проверьте, что Worker развернут и доступен. Откройте URL Worker'а в браузере - должна быть ошибка 405 (это нормально для GET запросов).

### Проблема: "OpenAI API error"
**Решение:** Проверьте ваш OpenAI API ключ. Worker только перенаправляет запросы, но не проверяет ключ.

## Готово!

Теперь ваш прокси настроен и работает! 🎉

