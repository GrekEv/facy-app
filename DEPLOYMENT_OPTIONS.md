# 🌐 Варианты развертывания - С доменом И без домена

Приложение поддерживает **оба варианта** развертывания:

## ✅ С доменом (ваш случай)

Если у вас есть домен (например, `onlyface.art`):

```bash
# В .env файле
WEBAPP_URL=https://onlyface.art
```

**Преимущества:**
- ✅ Стабильный URL
- ✅ Профессиональный вид
- ✅ Можно настроить SSL сертификат
- ✅ Собственный брендинг

**Настройка:**
1. Настройте DNS записи для домена
2. Установите SSL сертификат (Let's Encrypt)
3. Укажите `WEBAPP_URL=https://your-domain.com` в `.env`
4. Настройте Menu Button в BotFather с вашим доменом

---

## ✅ Без домена (Railway, Render, ngrok)

Если у вас нет домена, можно использовать бесплатные сервисы:

### 🟢 Railway (РЕКОМЕНДУЕТСЯ)

```bash
# В Railway переменных окружения
WEBAPP_URL=https://your-app.up.railway.app
# ИЛИ оставьте пустым - определится автоматически из RAILWAY_PUBLIC_DOMAIN
```

**Преимущества:**
- ✅ Автоматический HTTPS
- ✅ Автоматический домен
- ✅ Бесплатный план ($5 кредитов/месяц)
- ✅ Простое развертывание

### 🟡 Render

```bash
# В Render переменных окружения
WEBAPP_URL=https://your-app.onrender.com
# ИЛИ оставьте пустым - определится автоматически из RENDER_EXTERNAL_URL
```

### 🔵 ngrok (для тестирования)

```bash
# В .env или переменных окружения
WEBAPP_URL=https://xxx.ngrok-free.app
```

---

## 🔧 Автоматическое определение URL

Приложение **автоматически определяет URL**, если `WEBAPP_URL` не указан:

1. **Railway**: Использует `RAILWAY_PUBLIC_DOMAIN`
2. **Render**: Использует `RENDER_EXTERNAL_URL`
3. **Локально**: Использует `http://localhost:8000`

**Это означает, что вы можете:**
- ✅ Указать домен явно: `WEBAPP_URL=https://onlyface.art`
- ✅ Оставить пустым на Railway/Render - определится автоматически
- ✅ Использовать ngrok URL для тестирования

---

## 📋 Примеры конфигурации

### С доменом (продакшен)
```bash
BOT_TOKEN=ваш_токен
WEBAPP_URL=https://onlyface.art
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
```

### Railway (без домена)
```bash
BOT_TOKEN=ваш_токен
# WEBAPP_URL можно не указывать - определится автоматически
# ИЛИ указать явно:
WEBAPP_URL=https://your-app.up.railway.app
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
```

### Render (без домена)
```bash
BOT_TOKEN=ваш_токен
# WEBAPP_URL можно не указывать - определится автоматически
# ИЛИ указать явно:
WEBAPP_URL=https://your-app.onrender.com
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
```

### ngrok (локальное тестирование)
```bash
BOT_TOKEN=ваш_токен
WEBAPP_URL=https://xxx.ngrok-free.app
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
```

---

## 🎯 Рекомендации

### Для продакшена:
- ✅ **Используйте домен** - более профессионально и стабильно
- ✅ Настройте SSL сертификат (Let's Encrypt)
- ✅ Укажите `WEBAPP_URL=https://your-domain.com` явно

### Для разработки/тестирования:
- ✅ **Railway/Render** - если нужен стабильный URL без домена
- ✅ **ngrok** - для быстрого локального тестирования

### Гибридный вариант:
- ✅ API на домене (`https://onlyface.art`)
- ✅ Бот может работать откуда угодно (локально, Railway, VPS)
- ✅ Главное - указать правильный `WEBAPP_URL` в BotFather

---

## ✅ Проверка работы

После настройки проверьте:

```bash
# Проверка API
curl https://your-url/health
# Должно вернуть: {"status":"healthy"}

# Проверка главной страницы
curl https://your-url/
# Должна вернуться HTML страница
```

В Telegram:
1. Откройте бота
2. `/start`
3. Нажмите "🚀 Открыть приложение"
4. Должно открыться Web App!

---

## 📚 Дополнительная документация

- **[DEPLOY_NO_DOMAIN.md](DEPLOY_NO_DOMAIN.md)** - Подробная инструкция по развертыванию без домена
- **[QUICKSTART_NO_DOMAIN.md](QUICKSTART_NO_DOMAIN.md)** - Быстрый старт без домена
- **[DEPLOY_COMPLETE.md](DEPLOY_COMPLETE.md)** - Полная инструкция с доменом
- **[README.md](README.md)** - Общая информация

---

**Важно:** Приложение работает **и с доменом, и без домена**! Просто укажите правильный `WEBAPP_URL` или оставьте пустым для автоматического определения.

