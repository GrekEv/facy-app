# 🚀 Подключение к Neon Postgres

Это руководство поможет вам подключить проект к базе данных Neon PostgreSQL.

## 📋 Шаг 1: Создание проекта в Neon

1. Перейдите на [Neon Console](https://console.neon.tech/)
2. Войдите или зарегистрируйтесь
3. Нажмите **Create Project**
4. Выберите регион (рекомендуется ближайший к вашему серверу)
5. Нажмите **Create Project**

## 📋 Шаг 2: Получение строки подключения

После создания проекта:

1. В Neon Console откройте ваш проект
2. Перейдите в раздел **Connection Details**
3. Скопируйте строку подключения (Connection String)
   
   Формат будет примерно таким:
   ```
   postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
   ```

## 📋 Шаг 3: Преобразование для asyncpg

Проект использует `asyncpg` драйвер, поэтому нужно добавить префикс `postgresql+asyncpg://`:

**Исходная строка Neon:**
```
postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
```

**Для проекта (с префиксом asyncpg):**
```
postgresql+asyncpg://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
```

Просто замените `postgresql://` на `postgresql+asyncpg://` в начале строки.

## 📋 Шаг 4: Настройка переменных окружения

### Для локальной разработки

Создайте файл `.env` в корне проекта:

```bash
# База данных Neon
DATABASE_URL=postgresql+asyncpg://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require

# Остальные переменные...
BOT_TOKEN=ваш_токен_бота
WEBAPP_URL=https://your-app.vercel.app
```

### Для Vercel

1. Откройте Vercel Dashboard → ваш проект
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте переменную:
   - **Name:** `DATABASE_URL`
   - **Value:** `postgresql+asyncpg://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require`
4. Выберите окружения (Production, Preview, Development)
5. Нажмите **Save**

### Для Railway

1. Откройте Railway Dashboard → ваш проект
2. Перейдите в **Variables**
3. Добавьте переменную:
   - **Key:** `DATABASE_URL`
   - **Value:** `postgresql+asyncpg://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require`
4. Нажмите **Add**

### Для Render

1. Откройте Render Dashboard → ваш сервис
2. Перейдите в **Environment**
3. Добавьте переменную:
   - **Key:** `DATABASE_URL`
   - **Value:** `postgresql+asyncpg://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require`
4. Нажмите **Save Changes**

## 📋 Шаг 5: Создание таблиц

После настройки подключения, таблицы создадутся автоматически при первом запуске приложения.

Если нужно создать таблицы вручную, используйте Neon SQL Editor:

1. В Neon Console откройте ваш проект
2. Перейдите в **SQL Editor**
3. Выполните SQL команды из `database/models.py` или используйте миграции

## 📋 Шаг 6: Проверка подключения

### Локальная проверка

Создайте файл `test_neon_connection.py`:

```python
import asyncio
from database.database import get_engine
from sqlalchemy import text

async def test_connection():
    try:
        engine = get_engine()
        async with engine.connect() as conn:
            result = await conn.execute(text("SELECT 1"))
            print("✅ Подключение к Neon успешно!")
            print(f"Результат: {result.scalar()}")
    except Exception as e:
        print(f"❌ Ошибка подключения: {e}")

if __name__ == "__main__":
    asyncio.run(test_connection())
```

Запустите:
```bash
python test_neon_connection.py
```

### Проверка через Neon SQL Editor

1. Откройте Neon Console → ваш проект
2. Перейдите в **SQL Editor**
3. Выполните запрос:
   ```sql
   SELECT version();
   ```
4. Если запрос выполнился успешно - подключение работает!

## 🔒 Безопасность

### Важные моменты:

1. **Никогда не коммитьте** строку подключения в Git
2. Используйте переменные окружения для хранения `DATABASE_URL`
3. Neon автоматически использует SSL (`sslmode=require`)
4. Регулярно обновляйте пароли доступа

### Рекомендации:

- Используйте разные базы данных для development и production
- Настройте автоматическое резервное копирование в Neon
- Ограничьте доступ к базе данных по IP (если возможно)

## 🆘 Решение проблем

### Ошибка: "connection refused"

**Причина:** Неправильный хост или порт в строке подключения

**Решение:** Проверьте строку подключения в Neon Console

### Ошибка: "authentication failed"

**Причина:** Неправильный username или password

**Решение:** 
1. Проверьте учетные данные в Neon Console
2. Если нужно, создайте нового пользователя в Neon

### Ошибка: "database does not exist"

**Причина:** База данных не создана

**Решение:**
1. В Neon Console создайте базу данных
2. Обновите строку подключения с правильным именем БД

### Ошибка: "SSL connection required"

**Причина:** Отсутствует `sslmode=require` в строке подключения

**Решение:** Добавьте `?sslmode=require` в конец строки подключения

## 📚 Дополнительные ресурсы

- [Neon Documentation](https://neon.tech/docs)
- [asyncpg Documentation](https://magicstack.github.io/asyncpg/)
- [SQLAlchemy asyncpg](https://docs.sqlalchemy.org/en/20/dialects/postgresql.html#asyncpg)

## ✅ Чеклист

- [ ] Создан проект в Neon
- [ ] Скопирована строка подключения
- [ ] Добавлен префикс `postgresql+asyncpg://`
- [ ] Настроена переменная `DATABASE_URL` в окружении
- [ ] Проверено подключение
- [ ] Созданы таблицы (автоматически или вручную)

## 🎉 Готово!

После выполнения всех шагов ваш проект будет подключен к Neon PostgreSQL и готов к работе!

