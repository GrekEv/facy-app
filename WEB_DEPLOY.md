# 🌐 Деплой через веб-интерфейсы (без CLI)

Самый простой способ развернуть Facy - через веб-интерфейсы Railway и Vercel.

## ⚡ Быстрый деплой за 10 минут

### Шаг 1: Railway (5 минут)

1. **Откройте Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub (кнопка "Login with GitHub")

2. **Создайте проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Найдите и выберите `GrekEv/facy-app`
   - Railway автоматически начнет деплой

3. **Настройте переменные окружения:**
   - В проекте Railway нажмите на сервис (обычно называется как ваш репозиторий)
   - Перейдите в "Variables" (вкладка слева)
   - Нажмите "New Variable" и добавьте:

```
BOT_TOKEN = ваш_токен_от_BotFather
WEBAPP_URL = https://ваш-vercel-url.vercel.app (обновим после Vercel)
ENVIRONMENT = production
```

4. **Получите Railway URL:**
   - В настройках сервиса найдите "Settings"
   - Скопируйте "Public Domain" (например: `facy-app.up.railway.app`)
   - Или создайте новый домен: "Settings" → "Generate Domain"

5. **Настройте процессы:**
   - Railway автоматически определит `Procfile` и создаст два процесса:
     - `web` - FastAPI сервер
     - `worker` - Telegram бот
   - Если процессы не создались автоматически:
     - "Settings" → "Service Source" → "Add Service"
     - Выберите "Empty Service"
     - В "Start Command" укажите: `python main.py`
     - Назовите его "worker"

### Шаг 2: Vercel (3 минуты)

1. **Откройте Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub

2. **Создайте проект:**
   - Нажмите "Add New Project"
   - Найдите и выберите `GrekEv/facy-app`
   - Нажмите "Import"

3. **Обновите vercel.json:**
   - Перед деплоем нужно обновить `vercel.json`
   - В GitHub репозитории откройте `vercel.json`
   - Замените `YOUR-RAILWAY-URL` на ваш Railway URL (без `https://`)
   - Например, если Railway URL: `https://facy-app.up.railway.app`
   - То замените на: `facy-app.up.railway.app`
   - Сделайте коммит и пуш

4. **Деплой:**
   - Вернитесь в Vercel
   - Нажмите "Deploy" (или Vercel автоматически задеплоит после пуша)
   - Дождитесь завершения деплоя

5. **Получите Vercel URL:**
   - После деплоя скопируйте URL (например: `https://facy-app.vercel.app`)

### Шаг 3: Обновите Railway (1 минута)

1. **Вернитесь в Railway:**
   - Откройте ваш проект
   - Перейдите в "Variables"
   - Найдите `WEBAPP_URL`
   - Измените значение на ваш Vercel URL
   - Railway автоматически перезапустит сервисы

### Шаг 4: Настройте Telegram бота (1 минута)

1. **Откройте BotFather:**
   - Перейдите в https://t.me/BotFather
   - Отправьте `/mybots`
   - Выберите вашего бота

2. **Настройте Menu Button:**
   - Выберите "Bot Settings" → "Menu Button"
   - Включите "Menu Button" (если выключен)
   - В поле "URL" введите ваш Vercel URL
   - Нажмите "Save"

## ✅ Готово!

Теперь:
1. Откройте вашего бота в Telegram
2. Нажмите "🚀 Открыть приложение" (или кнопку меню)
3. Должно открыться Web App

## 🔍 Проверка работы

### Проверьте API:
Откройте в браузере:
```
https://ваш-railway-url.up.railway.app/health
```
Должен вернуться: `{"status": "healthy"}`

### Проверьте Web App:
Откройте в браузере:
```
https://ваш-vercel-url.vercel.app
```
Должна открыться главная страница Facy

### Проверьте бота:
1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку открытия приложения

## 🐛 Решение проблем

### Railway не деплоит
- Проверьте логи: Railway → ваш проект → Deployments → View Logs
- Убедитесь, что все переменные окружения добавлены
- Проверьте что `BOT_TOKEN` правильный

### Vercel показывает ошибку
- Проверьте что `vercel.json` обновлен с правильным Railway URL
- Убедитесь, что Railway URL доступен (откройте в браузере)
- Проверьте логи: Vercel → ваш проект → Deployments → View Logs

### Бот не открывает Web App
- Проверьте `WEBAPP_URL` в Railway переменных
- Убедитесь, что Menu Button настроен в BotFather
- Проверьте что Vercel URL доступен (откройте в браузере)

### API запросы не работают
- Проверьте что Railway URL правильный в `vercel.json`
- Убедитесь, что Railway сервис запущен
- Проверьте CORS настройки в `api/main.py`

## 📝 Важные заметки

1. **Railway автоматически предоставляет PORT** - не добавляйте его вручную
2. **После каждого изменения в коде** - сделайте commit и push, Railway и Vercel автоматически задеплоят
3. **Переменные окружения** - обновляются без передеплоя, но может потребоваться перезапуск сервиса
4. **Логи** - всегда проверяйте логи при проблемах

## 🔄 Обновление кода

После изменения кода:
```bash
git add .
git commit -m "Описание изменений"
git push
```

Railway и Vercel автоматически задеплоят новую версию.

## 💰 Стоимость

- **Railway**: Бесплатный план включает $5 кредитов в месяц
- **Vercel**: Бесплатный план включает 100GB bandwidth в месяц

Для начала этого более чем достаточно!

---

**Готово! Ваше приложение развернуто и работает! 🎉**

