# 🔑 Настройка переменных окружения в Vercel

## Как добавить OPENAI_API_KEY в Vercel

### Способ 1: Через Vercel Dashboard (рекомендуется)

1. **Зайдите в Vercel Dashboard**
   - Откройте https://vercel.com/dashboard
   - Войдите в свой аккаунт

2. **Выберите ваш проект**
   - Найдите проект `telegram-deepface-app` (или ваш проект)
   - Кликните на него

3. **Откройте Settings**
   - В верхнем меню проекта нажмите **Settings**

4. **Перейдите в Environment Variables**
   - В левом меню выберите **Environment Variables**

5. **Добавьте переменную OPENAI_API_KEY**
   - Нажмите кнопку **Add New**
   - В поле **Key** введите: `OPENAI_API_KEY`
   - В поле **Value** вставьте ваш ключ OpenAI (начинается с `sk-...`)
   - Выберите окружения: **Production**, **Preview**, **Development** (или только нужные)
   - Нажмите **Save**

6. **Добавьте другие необходимые переменные** (если еще не добавлены):
   ```
   BOT_TOKEN=ваш_токен_бота
   DATABASE_URL=postgresql+asyncpg://...
   IMAGE_GENERATION_PROVIDER=openai
   ```

7. **Перезапустите деплой**
   - Перейдите в **Deployments**
   - Найдите последний деплой
   - Нажмите три точки (⋮) → **Redeploy**
   - Или просто сделайте новый commit и push в GitHub

### Способ 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Добавьте переменную окружения
vercel env add OPENAI_API_KEY

# Введите значение при запросе
# Выберите окружения (production, preview, development)

# Для продакшена:
vercel env add OPENAI_API_KEY production
```

### Способ 3: Через vercel.json (не рекомендуется для секретов)

⚠️ **ВНИМАНИЕ:** Не добавляйте секретные ключи напрямую в `vercel.json`! Они будут видны в репозитории.

## Проверка переменных окружения

После добавления переменных:

1. **Проверьте в Dashboard**
   - Settings → Environment Variables
   - Убедитесь, что `OPENAI_API_KEY` есть в списке

2. **Проверьте в логах**
   - Deployments → выберите деплой → Functions → api/index → View Function Logs
   - При запуске должно быть видно: `has_openai_key: True`

3. **Протестируйте генерацию**
   - Откройте ваше приложение
   - Попробуйте сгенерировать изображение
   - Проверьте логи на наличие ошибок

## Необходимые переменные для работы генерации

### Обязательные:
```
BOT_TOKEN=ваш_токен_бота
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname
```

### Для генерации изображений:
```
OPENAI_API_KEY=sk-ваш_ключ_openai
IMAGE_GENERATION_PROVIDER=openai
```

### Альтернативные провайдеры:
```
# Если используете FFans вместо OpenAI:
FFANS_API_KEY=ваш_ключ_ffans
IMAGE_GENERATION_PROVIDER=ffans

# Если используете Replicate:
REPLICATE_API_KEY=ваш_ключ_replicate
IMAGE_GENERATION_PROVIDER=replicate
```

## Где взять OpenAI API ключ?

1. Зайдите на https://platform.openai.com/api-keys
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите **Create new secret key**
4. Скопируйте ключ (он начинается с `sk-`)
5. ⚠️ **Важно:** Сохраните ключ сразу, его больше не будет видно!

## После добавления переменных

После добавления переменных окружения в Vercel:

1. **Сделайте redeploy** (перезапуск деплоя)
   - Vercel Dashboard → Deployments → последний деплой → Redeploy
   - Или сделайте новый commit и push

2. **Проверьте логи**
   - Убедитесь, что нет ошибок при запуске
   - Проверьте, что переменные загружены

3. **Протестируйте генерацию**
   - Откройте приложение
   - Попробуйте сгенерировать изображение
   - Проверьте консоль браузера (F12) на наличие ошибок

## Troubleshooting

### Переменная не работает?

1. **Проверьте имя переменной** - должно быть точно `OPENAI_API_KEY` (регистр важен!)
2. **Проверьте окружения** - убедитесь, что выбрали нужные окружения (Production, Preview)
3. **Сделайте redeploy** - переменные применяются только при новом деплое
4. **Проверьте логи** - в логах функции должно быть видно значение переменной

### Как проверить, что переменная загружена?

В логах функции (Vercel Dashboard → Deployments → Functions → api/index → Logs) должно быть:
```
Generating image with provider: openai, has_openai_key: True, has_ffans_key: False
```

Если `has_openai_key: False` - переменная не загружена, проверьте настройки.

