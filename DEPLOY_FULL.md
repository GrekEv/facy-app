# 🚀 Полная инструкция по деплою Facy

**Единая инструкция по развертыванию приложения на Яндекс.Облаке с полным функционалом генерации видео, изображений и замены лиц.**

---

## 📋 Содержание

1. [Требования для генерации видео и изображений](#требования-для-генерации)
2. [Выбор и настройка API сервисов](#выбор-и-настройка-api-сервисов) ⭐
3. [Деплой на Яндекс.Облако](#деплой-на-яндексоблако)
4. [Настройка API ключей](#настройка-api-ключей)
5. [Настройка Telegram бота](#настройка-telegram-бота)
6. [Проверка работы](#проверка-работы)
7. [Решение проблем](#решение-проблем)

---

## 📖 Полное руководство по API сервисам

**📚 Подробное руководство:** [API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md)

В руководстве вы найдете:
- ✅ Все доступные сервисы для замены лиц, генерации изображений и видео
- ✅ Сравнительные таблицы по качеству, стоимости и функциональности
- ✅ Инструкции по получению API ключей для каждого сервиса
- ✅ Рекомендации по выбору провайдеров для разных сценариев
- ✅ Информация о Sora (OpenAI), RunwayML, Pika Labs и других

---

## 🎯 Требования для генерации видео и изображений

### 📦 Аппаратные требования

#### Минимальная конфигурация (для тестирования):
- **CPU:** 2 vCPU (Intel Ice Lake или лучше)
- **RAM:** 4 ГБ
- **Диск:** 20 ГБ SSD
- **Сеть:** Публичный IP адрес

#### Рекомендуемая конфигурация (для продакшена):
- **CPU:** 4-8 vCPU
- **RAM:** 8-16 ГБ
- **Диск:** 50-100 ГБ SSD
- **GPU:** Опционально (для локальной обработки, но обычно используется внешний API)

### 🔑 API ключи и сервисы

**📚 Полное руководство:** [API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md)

Для полноценной работы приложения необходимы API ключи от сервисов. Вы можете выбрать из множества вариантов:

#### 🎭 Сервисы для замены лиц в видео:

1. **DeepFace Video** ⭐ (Рекомендуется)
   - Высокое качество, быстрая обработка
   - https://deepfacevideo.com
   - Стоимость: от $0.10 за видео

2. **FaceSwap.ru**
   - Российский сервис, низкая стоимость (10₽)
   - https://faceswap.ru
   - Только для фотографий

3. **DeepSwap.ai**
   - Поддержка до 6 лиц, высокое качество
   - https://www.deepswap.ai
   - API по запросу

#### 🎨 Сервисы для генерации изображений:

1. **FFans AI** ⭐ (Рекомендуется)
   - Генерация изображений и видео
   - https://ffans.ai
   - Множество моделей и стилей

2. **Leonardo.AI**
   - Отличное качество, специализация на игровых ресурсах
   - https://leonardo.ai
   - От $10/месяц

3. **Replicate (Stable Diffusion)**
   - Pay-as-you-go, множество моделей
   - https://replicate.com
   - От $0.002 за изображение

#### 🎬 Сервисы для генерации видео:

1. **OpenAI Sora** 🚀 (Скоро)
   - Лучшее качество, до 60 секунд
   - https://openai.com/sora
   - В разработке, waitlist доступен

2. **RunwayML Gen-2** ⭐ (Рекомендуется)
   - Отличное качество, до 5 секунд
   - https://runwayml.com
   - От $12/месяц

3. **Pika Labs**
   - Художественные стили
   - https://pika.art
   - API по запросу

4. **Stable Video Diffusion (Replicate)**
   - Бюджетный вариант, до 4 секунд
   - https://replicate.com
   - От $0.05 за видео

**💡 Рекомендации:**
- **Для старта:** DeepFace Video + FFans AI + Stable Video Diffusion
- **Для продакшена:** DeepFace Video + Leonardo.AI + RunwayML
- **Для максимального качества:** DeepSwap.ai + Leonardo.AI + Sora (когда будет доступен)

**📖 Подробнее:** См. [API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md) для полного сравнения и инструкций по получению ключей.

### 💾 Хранилище данных

#### Локальное хранилище (по умолчанию):
- Загруженные файлы: `./uploads/`
- Сгенерированные результаты: `./generated/`
- База данных: `./data/app.db`

#### Облачное хранилище (рекомендуется для продакшена):
- **Yandex Object Storage (S3-совместимое)**
  - Создайте бакет в Yandex Cloud
  - Настройте CORS для доступа из приложения
  - Используйте для хранения загруженных и сгенерированных файлов
  - **Преимущества:**
    - Масштабируемое хранилище
    - Низкая стоимость (от 1.5₽ за ГБ/месяц)
    - CDN интеграция для быстрой загрузки
    - Автоматическое резервное копирование

**📖 Инструкция по настройке Object Storage:** См. раздел "Настройка Yandex Object Storage" ниже

### 🌐 Сетевая инфраструктура

- **Домен:** Рекомендуется настроить домен для Web App
- **SSL сертификат:** Обязательно для HTTPS (Let's Encrypt бесплатный)
- **CDN:** Опционально, для ускорения загрузки статики

---

## 🔧 Выбор и настройка API сервисов

### Быстрый выбор (5 минут)

**Минимальная конфигурация для старта:**

1. **Замена лиц:** DeepFace Video
   - Зарегистрируйтесь на https://deepfacevideo.com
   - Получите API ключ

2. **Изображения:** FFans AI
   - Зарегистрируйтесь на https://ffans.ai
   - Получите API ключ

3. **Видео:** FFans AI (тот же ключ) или Stable Video Diffusion через Replicate
   - Для Replicate: зарегистрируйтесь на https://replicate.com

**Рекомендуемая конфигурация:**

1. **Замена лиц:** DeepFace Video
2. **Изображения:** Leonardo.AI или FFans AI
3. **Видео:** RunwayML Gen-2

**Премиум конфигурация:**

1. **Замена лиц:** DeepSwap.ai
2. **Изображения:** Leonardo.AI
3. **Видео:** Sora (OpenAI) - когда будет доступен

### Настройка провайдеров

В файле `.env` укажите выбранных провайдеров:

```env
# Выбор провайдеров
VIDEO_GENERATION_PROVIDER=runway  # runway, sora, pika, ffans, replicate
IMAGE_GENERATION_PROVIDER=ffans   # ffans, leonardo, replicate
```

**📚 Полная инструкция:** [API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md)

---

## 🟢 Деплой на Яндекс.Облако

### Шаг 1: Регистрация в Яндекс.Облаке (2 минуты)

1. Откройте https://console.cloud.yandex.ru
2. Нажмите **"Войти"** (можно через Яндекс ID)
3. Подтвердите телефон
4. **Получите бесплатный период: 4000₽ на 60 дней** 🎁
5. При первом входе создайте платежный аккаунт (требуется для использования сервисов)

### Шаг 2: Создание виртуальной машины (5 минут)

#### Вариант A: Через кнопку "Создать ресурс" (рекомендуется)

1. В верхнем правом углу консоли нажмите синюю кнопку **"+ Создать ресурс"**
2. В выпадающем меню выберите **"Виртуальная машина"**
3. Заполните настройки:

   **Основные параметры:**
   - **Имя:** `facy-app`
   - **Описание:** `Telegram Mini App для генерации видео и изображений` (опционально)
   - **Зона доступности:** `ru-central1-a` (Москва)
   - **Платформа:** Intel Ice Lake или AMD EPYC (рекомендуется Ice Lake)
   
   **Вычислительные ресурсы:**
   - **Платформа:** `Intel Ice Lake`
   - **vCPU:** 4 (рекомендуется для продакшена)
   - **RAM:** 8 ГБ (рекомендуется для продакшена)
   - **Гарантированная доля vCPU:** 100% (для стабильной работы)
   
   **Диски:**
   - **Загрузочный диск:** 
     - **Тип:** SSD
     - **Размер:** 50 ГБ (рекомендуется для продакшена)
     - **Образ:** Ubuntu 22.04 LTS
   
   **Сеть:**
   - **Сеть:** Выберите существующую или создайте новую
   - **Подсеть:** Выберите существующую или создайте новую в зоне `ru-central1-a`
   - **Публичный адрес:** ✅ **Автоматически** (важно для доступа из интернета)
   - **Группы безопасности:** Выберите существующую или создайте новую
   
   **Доступ:**
   - **Сервисный аккаунт:** Не требуется (можно оставить пустым)
   - **Логин:** `ubuntu` (автоматически для Ubuntu)
   - **SSH ключ:** Добавьте ваш публичный SSH ключ
     - Если нет ключа, создайте на локальной машине:
       ```bash
       ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
       cat ~/.ssh/id_rsa.pub
       ```
     - Скопируйте содержимое (начинается с `ssh-rsa`) и вставьте в поле "SSH ключ"
     - Или нажмите "Создать новый" для генерации ключа в облаке

4. Нажмите **"Создать ВМ"** внизу страницы
5. Дождитесь создания ВМ (1-2 минуты)
6. После создания найдите вашу ВМ в списке и скопируйте **публичный IPv4 адрес**

#### Вариант B: Через раздел Compute Cloud

1. В левом меню нажмите на иконку **"Все сервисы"** (сетка)
2. Найдите и выберите **"Compute Cloud"**
3. Перейдите в раздел **"Виртуальные машины"**
4. Нажмите **"Создать ВМ"**
5. Следуйте инструкциям из Варианта A выше

### Шаг 2.1: Настройка групп безопасности (важно!)

После создания ВМ необходимо настроить группы безопасности для доступа:

1. В консоли перейдите в **"Virtual Private Cloud"** → **"Группы безопасности"**
2. Найдите группу безопасности, связанную с вашей ВМ
3. Нажмите на неё и выберите **"Правила"**
4. Добавьте следующие правила для входящего трафика:

   **Правило 1: SSH доступ**
   - **Направление:** Входящий
   - **Протокол:** TCP
   - **Порт:** 22
   - **Источник:** CIDR (0.0.0.0/0) или ваш IP
   - **Описание:** SSH доступ

   **Правило 2: HTTP доступ**
   - **Направление:** Входящий
   - **Протокол:** TCP
   - **Порт:** 80
   - **Источник:** CIDR (0.0.0.0/0)
   - **Описание:** HTTP доступ

   **Правило 3: HTTPS доступ**
   - **Направление:** Входящий
   - **Протокол:** TCP
   - **Порт:** 443
   - **Источник:** CIDR (0.0.0.0/0)
   - **Описание:** HTTPS доступ

   **Правило 4: Порт приложения**
   - **Направление:** Входящий
   - **Протокол:** TCP
   - **Порт:** 8000
   - **Источник:** CIDR (0.0.0.0/0)
   - **Описание:** Facy App порт

5. Сохраните изменения

**Примечание:** Если вы используете существующую группу безопасности, убедитесь, что эти правила уже добавлены.

### Шаг 3: Подключение к серверу (1 минута)

#### Проверка доступности

Перед подключением убедитесь, что ВМ запущена:
1. В консоли Yandex Cloud перейдите в **"Виртуальные машины"**
2. Найдите вашу ВМ `facy-app`
3. Убедитесь, что статус **"Работает"** (зеленый индикатор)
4. Скопируйте **публичный IPv4 адрес** (например: `51.250.12.34`)

#### Подключение по SSH

```bash
ssh ubuntu@<ваш-ip-адрес>
```

Например:
```bash
ssh ubuntu@51.250.12.34
```

**Если подключение не удается:**

1. **Проверьте группы безопасности** (см. Шаг 2.1)
2. **Проверьте SSH ключ:**
   ```bash
   # На локальной машине проверьте наличие ключа
   ls -la ~/.ssh/id_rsa.pub
   
   # Если ключа нет, создайте новый
   ssh-keygen -t rsa -b 4096
   ```
3. **Попробуйте подключиться с указанием ключа:**
   ```bash
   ssh -i ~/.ssh/id_rsa ubuntu@<ваш-ip-адрес>
   ```
4. **Проверьте, что ВМ имеет публичный IP:**
   - В консоли Yandex Cloud → ВМ → Сеть → Публичный адрес

**При первом подключении** система попросит подтвердить подлинность хоста:
```
The authenticity of host '51.250.12.34' can't be established.
Are you sure you want to continue connecting (yes/no)?
```
Введите `yes` и нажмите Enter.

### Шаг 4: Автоматический деплой (5 минут)

Выполните одну команду:

```bash
curl -fsSL https://raw.githubusercontent.com/GrekEv/facy-app/main/deploy-cis.sh | bash
```

Скрипт автоматически:
- ✅ Установит Docker и Docker Compose
- ✅ Клонирует репозиторий
- ✅ Создаст необходимые директории
- ✅ Подготовит конфигурацию

**Или выполните вручную:**

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker ubuntu

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Установка Git
sudo apt install git -y

# Клонирование репозитория
git clone https://github.com/GrekEv/facy-app.git
cd facy-app

# Выход и повторный вход для применения изменений Docker
exit
```

Подключитесь снова:
```bash
ssh ubuntu@<ваш-ip-адрес>
cd facy-app
```

### Шаг 5: Настройка переменных окружения (3 минуты)

Создайте файл `.env`:

```bash
nano .env
```

Добавьте следующие переменные:

```env
# ============================================
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ
# ============================================

# Токен бота от BotFather
BOT_TOKEN=ваш_токен_бота

# URL вашего Web App (замените на ваш IP или домен)
WEBAPP_URL=http://ваш-ip:8000
# Или с доменом:
# WEBAPP_URL=https://ваш-домен.ru

# Режим работы
ENVIRONMENT=production

# База данных (SQLite по умолчанию)
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Сервер
HOST=0.0.0.0
PORT=8000

# ============================================
# API КЛЮЧИ ДЛЯ ГЕНЕРАЦИИ
# ============================================

# DeepFace API (для замены лиц в видео)
DEEPFACE_API_KEY=ваш_deepface_api_ключ
DEEPFACE_API_URL=https://deepfacevideo.com/api

# FFans API (для генерации изображений и видео)
FFANS_API_KEY=ваш_ffans_api_ключ
FFANS_API_URL=https://ffans.ai/api

# ============================================
# ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ
# ============================================

# ID администраторов через запятую
ADMIN_IDS=123456789,987654321

# Платежные системы (если используете)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# Криптовалютные платежи
CRYPTO_API_KEY=
CRYPTO_WALLET_ADDRESS_BTC=
CRYPTO_WALLET_ADDRESS_ETH=
CRYPTO_WALLET_ADDRESS_USDT=
```

**Важно:**
- Замените `ваш_токен_бота` на реальный токен от @BotFather
- Замените `ваш-ip` на IP адрес вашей ВМ
- Добавьте API ключи от DeepFace и FFans (см. раздел "Настройка API ключей")

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 6: Настройка firewall (2 минуты)

```bash
# Установка UFW
sudo apt install ufw -y

# Разрешить SSH
sudo ufw allow 22/tcp

# Разрешить HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Разрешить порт приложения
sudo ufw allow 8000/tcp

# Включить firewall
sudo ufw enable
```

### Шаг 7: Запуск приложения (1 минута)

```bash
cd facy-app
docker-compose -f docker-compose.prod.yml up -d
```

Проверьте статус:
```bash
docker-compose -f docker-compose.prod.yml ps
```

Просмотр логов:
```bash
docker-compose -f docker-compose.prod.yml logs -f
```

### Шаг 7.1: Настройка Yandex Object Storage (опционально, но рекомендуется)

Для продакшена рекомендуется использовать Yandex Object Storage вместо локального хранилища:

1. **Создание бакета:**
   - В консоли Yandex Cloud нажмите **"+ Создать ресурс"** → **"Object Storage"**
   - Или перейдите в **"Object Storage"** через меню сервисов
   - Нажмите **"Создать бакет"**
   - Заполните параметры:
     - **Имя:** `facy-storage` (уникальное имя)
     - **Тип доступа:** Публичный (для доступа к файлам через URL)
     - **Класс хранилища:** Standard
     - **Регион:** ru-central1
   - Нажмите **"Создать бакет"**

2. **Настройка CORS:**
   - Откройте созданный бакет
   - Перейдите в **"CORS"**
   - Добавьте правило:
     - **Разрешенные источники:** `*` (или ваш домен)
     - **Разрешенные методы:** GET, POST, PUT, DELETE
     - **Разрешенные заголовки:** `*`
     - **Максимальный возраст:** 3600

3. **Получение ключей доступа:**
   - Перейдите в **"Сервисные аккаунты"** → **"Создать аккаунт"**
   - Имя: `facy-storage-account`
   - Роль: `storage.editor`
   - Создайте статический ключ доступа
   - Сохраните **Access Key ID** и **Secret Access Key**

4. **Настройка в приложении:**
   Добавьте в `.env`:
   ```env
   # Yandex Object Storage
   YANDEX_STORAGE_BUCKET=facy-storage
   YANDEX_STORAGE_ACCESS_KEY=ваш_access_key_id
   YANDEX_STORAGE_SECRET_KEY=ваш_secret_access_key
   YANDEX_STORAGE_ENDPOINT=https://storage.yandexcloud.net
   ```

**Примечание:** Для использования Object Storage потребуется обновить код приложения для работы с S3 API.

### Шаг 8: Настройка домена и SSL (опционально, но рекомендуется)

#### 8.1. Настройка DNS

**Вариант A: Использование Yandex Cloud DNS (рекомендуется)**

1. В консоли Yandex Cloud перейдите в **"Cloud DNS"**
2. Создайте новую зону:
   - **Имя:** `facy-zone`
   - **Домен:** `ваш-домен.ru`
   - **Тип:** Публичная
3. После создания зоны получите NS-серверы
4. В панели управления вашего домена укажите эти NS-серверы
5. В зоне Cloud DNS создайте A-запись:
   - **Имя:** `@` (или `www`)
   - **Тип:** A
   - **Значение:** IP адрес вашей ВМ
   - **TTL:** 300

**Вариант B: Использование DNS провайдера домена**

1. В панели управления вашего домена добавьте A-запись:
   - **Имя:** `@` или `www`
   - **Значение:** IP адрес вашей ВМ
   - **TTL:** 3600

2. Дождитесь распространения DNS (5-30 минут)
3. Проверьте DNS:
   ```bash
   dig ваш-домен.ru
   # или
   nslookup ваш-домен.ru
   ```

#### 8.2. Установка Nginx

```bash
# Установка Nginx
sudo apt install nginx -y

# Создание конфигурации
sudo nano /etc/nginx/sites-available/facy
```

Добавьте конфигурацию:

```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Для WebSocket (если используется)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Статические файлы
    location /static/ {
        alias /home/ubuntu/facy-app/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # Загруженные файлы
    location /uploads/ {
        alias /home/ubuntu/facy-app/uploads/;
        expires 7d;
    }
    
    # Сгенерированные файлы
    location /generated/ {
        alias /home/ubuntu/facy-app/generated/;
        expires 7d;
    }
}
```

Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/facy /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 8.3. Установка SSL сертификата (Let's Encrypt)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получение сертификата
sudo certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Автоматическое обновление
sudo certbot renew --dry-run
```

После установки SSL обновите `.env`:

```bash
nano .env
```

Измените:
```env
WEBAPP_URL=https://ваш-домен.ru
```

Перезапустите приложение:
```bash
docker-compose -f docker-compose.prod.yml restart
```

---

## 🔑 Настройка API ключей

**📚 Подробное руководство:** [API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md)

### Быстрая настройка (выбранные провайдеры)

#### 1. DeepFace Video (замена лиц)

1. Зарегистрируйтесь на https://deepfacevideo.com
2. Перейдите в раздел "API" или "Developers"
3. Создайте новый API ключ
4. Добавьте в `.env`:
   ```env
   DEEPFACE_API_KEY=ваш_ключ_здесь
   DEEPFACE_API_URL=https://deepfacevideo.com/api
   ```

#### 2. FFans AI (изображения и видео)

1. Зарегистрируйтесь на https://ffans.ai
2. Перейдите в раздел "API" или "Developers"
3. Создайте новый API ключ
4. Добавьте в `.env`:
   ```env
   FFANS_API_KEY=ваш_ключ_здесь
   FFANS_API_URL=https://ffans.ai/api
   IMAGE_GENERATION_PROVIDER=ffans
   VIDEO_GENERATION_PROVIDER=ffans
   ```

#### 3. RunwayML (видео - рекомендуется)

1. Зарегистрируйтесь на https://runwayml.com
2. Перейдите в "Settings" → "API"
3. Создайте новый API ключ
4. Добавьте в `.env`:
   ```env
   RUNWAY_API_KEY=ваш_ключ_здесь
   RUNWAY_API_URL=https://api.runwayml.com/v1
   VIDEO_GENERATION_PROVIDER=runway
   ```

#### 4. Leonardo.AI (изображения - рекомендуется)

1. Зарегистрируйтесь на https://leonardo.ai
2. Перейдите в раздел "API" в настройках
3. Создайте новый API ключ
4. Добавьте в `.env`:
   ```env
   LEONARDO_API_KEY=ваш_ключ_здесь
   LEONARDO_API_URL=https://cloud.leonardo.ai/api/rest/v1
   IMAGE_GENERATION_PROVIDER=leonardo
   ```

#### 5. OpenAI Sora (видео - когда будет доступен)

1. Зарегистрируйтесь в waitlist на https://openai.com/sora
2. Дождитесь приглашения
3. Получите API ключ
4. Добавьте в `.env`:
   ```env
   OPENAI_API_KEY=ваш_ключ_здесь
   SORA_MODEL=sora
   VIDEO_GENERATION_PROVIDER=sora
   ```

#### 6. Replicate (Stable Diffusion/Video)

1. Зарегистрируйтесь на https://replicate.com
2. Перейдите в "Account" → "API Tokens"
3. Создайте новый токен
4. Добавьте в `.env`:
   ```env
   REPLICATE_API_KEY=ваш_ключ_здесь
   REPLICATE_API_URL=https://api.replicate.com/v1
   IMAGE_GENERATION_PROVIDER=replicate
   VIDEO_GENERATION_PROVIDER=replicate
   ```

### Пример полной конфигурации .env

```env
# Замена лиц
DEEPFACE_API_KEY=ваш_deepface_ключ
DEEPFACE_API_URL=https://deepfacevideo.com/api

# Генерация изображений (выберите один)
FFANS_API_KEY=ваш_ffans_ключ
FFANS_API_URL=https://ffans.ai/api
LEONARDO_API_KEY=ваш_leonardo_ключ
LEONARDO_API_URL=https://cloud.leonardo.ai/api/rest/v1
IMAGE_GENERATION_PROVIDER=ffans  # или leonardo, replicate

# Генерация видео (выберите один)
RUNWAY_API_KEY=ваш_runway_ключ
RUNWAY_API_URL=https://api.runwayml.com/v1
REPLICATE_API_KEY=ваш_replicate_ключ
REPLICATE_API_URL=https://api.replicate.com/v1
VIDEO_GENERATION_PROVIDER=runway  # или sora, pika, ffans, replicate
```

### Обновление переменных окружения

После добавления API ключей:

```bash
cd facy-app
nano .env
# Добавьте ключи и сохраните

# Перезапустите приложение
docker-compose -f docker-compose.prod.yml restart
```

**Примечание:** Если API ключи не указаны, приложение будет работать в mock-режиме (для тестирования).

**📚 Полная инструкция по всем сервисам:** [API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md)

---

## 🤖 Настройка Telegram бота

### Шаг 1: Создание бота (если еще не создан)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `Facy AI`)
   - Введите username бота (например: `facy_ai_bot`)
4. Скопируйте токен бота (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Добавьте токен в `.env` файл:
   ```env
   BOT_TOKEN=ваш_токен_здесь
   ```

### Шаг 2: Настройка Web App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Включите **"Menu Button"** (если выключен)
6. В поле **"URL"** введите:
   - С доменом: `https://ваш-домен.ru`
   - Или с IP: `http://ваш-ip:8000`
7. Нажмите **"Save"**

### Шаг 3: Настройка команд бота

Отправьте BotFather команду `/setcommands` и выберите вашего бота, затем отправьте:

```
start - Запустить бота
balance - Проверить баланс
help - Помощь
```

---

## ✅ Проверка работы

### 1. Проверка API

Откройте в браузере:
```
http://ваш-ip:8000/health
```

Должно вернуться:
```json
{"status": "healthy"}
```

### 2. Проверка Web App

Откройте в браузере:
```
http://ваш-ip:8000
```

Должна открыться главная страница Facy с интерфейсом.

### 3. Проверка бота

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Должно появиться приветственное сообщение с кнопкой "🚀 Открыть приложение"
4. Нажмите кнопку или используйте меню бота
5. Должна открыться Web App

### 4. Проверка генерации изображений

1. В Web App выберите вкладку "Генерация"
2. Введите описание (например: "Красивый закат на море")
3. Нажмите "Сгенерировать"
4. Должен появиться результат (или сообщение об ошибке, если нет API ключа)

### 5. Проверка замены лиц

1. В Web App выберите вкладку "Face Swap"
2. Загрузите фото с лицом
3. Загрузите видео
4. Нажмите "Создать"
5. Должен появиться результат (или сообщение об ошибке, если нет API ключа)

### 6. Проверка генерации видео

1. В Web App выберите вкладку "Генерация видео"
2. Введите описание сцены
3. Нажмите "Сгенерировать"
4. Должен появиться результат (или сообщение об ошибке, если нет API ключа)

---

## 🐛 Решение проблем

### Проблема: Порт не открыт

**Решение:**
```bash
# Проверьте firewall
sudo ufw status

# Проверьте настройки безопасности в панели Yandex Cloud
# Compute Cloud → ВМ → Сеть → Группы безопасности
# Убедитесь, что порт 8000 открыт для входящих соединений
```

### Проблема: Docker не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose -f docker-compose.prod.yml logs

# Проверьте, что порт свободен
sudo netstat -tulpn | grep 8000

# Пересоздайте контейнеры
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d
```

### Проблема: Бот не отвечает

**Решение:**
```bash
# Проверьте токен в .env
cat .env | grep BOT_TOKEN

# Проверьте логи бота
docker-compose -f docker-compose.prod.yml logs bot

# Убедитесь, что токен правильный и бот активен в BotFather
```

### Проблема: API ключи не работают

**Решение:**
1. Проверьте правильность ключей в `.env`
2. Убедитесь, что ключи активны в панели провайдера
3. Проверьте баланс на счету API провайдера
4. Проверьте логи:
   ```bash
   docker-compose -f docker-compose.prod.yml logs api
   ```

### Проблема: Web App не открывается в Telegram

**Решение:**
1. Проверьте `WEBAPP_URL` в `.env` - должен быть доступен из интернета
2. Убедитесь, что URL настроен в BotFather (Menu Button)
3. Проверьте, что приложение доступно по HTTP/HTTPS
4. Убедитесь, что используется HTTPS для продакшена (Telegram требует HTTPS)

### Проблема: Недостаточно места на диске

**Решение:**
```bash
# Проверьте использование диска
df -h

# Очистите старые Docker образы
docker system prune -a

# Очистите старые файлы генераций (осторожно!)
# Удалите файлы старше 7 дней из ./generated/
find ./generated -type f -mtime +7 -delete
```

---

## 📊 Мониторинг и логи

### Просмотр логов через Docker

```bash
# Все логи
docker-compose -f docker-compose.prod.yml logs -f

# Логи API
docker-compose -f docker-compose.prod.yml logs -f api

# Логи бота
docker-compose -f docker-compose.prod.yml logs -f bot
```

### Мониторинг в Yandex Cloud

1. **Мониторинг ВМ:**
   - В консоли Yandex Cloud перейдите в **"Виртуальные машины"**
   - Выберите вашу ВМ `facy-app`
   - Перейдите во вкладку **"Мониторинг"**
   - Просматривайте метрики:
     - Использование CPU
     - Использование памяти (RAM)
     - Использование диска
     - Сетевой трафик

2. **Настройка алертов:**
   - В разделе **"Мониторинг"** → **"Алерты"**
   - Создайте алерт на высокое использование CPU (>80%)
   - Создайте алерт на нехватку памяти
   - Создайте алерт на нехватку места на диске

3. **Логирование:**
   - Используйте **"Cloud Logging"** для централизованного хранения логов
   - Настройте экспорт логов Docker в Cloud Logging

### Статус контейнеров

```bash
docker-compose -f docker-compose.prod.yml ps
```

### Использование ресурсов

```bash
# Использование CPU и памяти
docker stats

# Использование диска
df -h

# Детальная информация о дисках
sudo du -sh /var/lib/docker/*
```

---

## 💰 Стоимость

### Яндекс.Облако

- **Бесплатный период:** 4000₽ на 60 дней 🎁
- **После бесплатного периода:**
  - **ВМ (4 vCPU, 8 GB RAM, 50 GB SSD):** ~1500-2000₽/месяц
  - **Object Storage (опционально):** 
    - Хранение: от 1.5₽ за ГБ/месяц
    - Запросы: первые 10,000 запросов бесплатно, далее от 0.5₽ за 1000 запросов
  - **Исходящий трафик:** 
    - Первые 10 ГБ бесплатно
    - Далее: ~1₽/ГБ (в пределах РФ), ~2₽/ГБ (международный)
  - **Cloud DNS (опционально):** 
    - Первые 1,000,000 запросов бесплатно
    - Далее: от 0.5₽ за 1000 запросов
  - **Load Balancer (опционально):** от 200₽/месяц

**💡 Экономия:**
- Используйте Object Storage вместо увеличения диска ВМ
- Настройте CDN для снижения трафика
- Используйте резервированные инстансы для долгосрочных проектов (скидка до 30%)
- Мониторьте использование ресурсов через консоль Yandex Cloud

### API сервисы

- **DeepFace API:** Зависит от тарифа провайдера (обычно от $0.10 за видео)
- **FFans API:** Зависит от тарифа провайдера (обычно от $0.05 за изображение, $0.20 за видео)

---

## 📚 Дополнительные ресурсы

### Документация по деплою:
- [Документация Yandex Cloud](https://cloud.yandex.ru/docs)
- [Документация Docker](https://docs.docker.com/)
- [Документация Telegram Bot API](https://core.telegram.org/bots/api)
- [Документация FastAPI](https://fastapi.tiangolo.com/)

### Документация по API сервисам:
- **[API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md)** - Полное руководство по всем API сервисам
- [OpenAI Sora](https://openai.com/sora)
- [RunwayML Documentation](https://docs.runwayml.com)
- [Replicate Documentation](https://replicate.com/docs)
- [Leonardo.AI API Docs](https://docs.leonardo.ai)
- [DeepFace Video API](https://deepfacevideo.com/api-docs)

---

## ✅ Чеклист деплоя

### Подготовка
- [ ] Зарегистрирован аккаунт в Яндекс.Облаке
- [ ] Создан платежный аккаунт
- [ ] Получены API ключи от выбранных сервисов (DeepFace, FFans, RunwayML и др.)

### Создание инфраструктуры
- [ ] Создана виртуальная машина через "+ Создать ресурс"
- [ ] Настроены группы безопасности (SSH, HTTP, HTTPS, порт 8000)
- [ ] Проверен публичный IP адрес ВМ
- [ ] Создан SSH ключ и добавлен в ВМ

### Установка и настройка
- [ ] Подключение к серверу по SSH успешно
- [ ] Установлен Docker и Docker Compose
- [ ] Клонирован репозиторий
- [ ] Настроен `.env` файл с токеном бота
- [ ] Добавлены API ключи в `.env`
- [ ] Выбраны провайдеры (VIDEO_GENERATION_PROVIDER, IMAGE_GENERATION_PROVIDER)

### Запуск приложения
- [ ] Запущено приложение через Docker Compose
- [ ] Проверены логи на наличие ошибок
- [ ] Проверена доступность API: `http://ваш-ip:8000/health`
- [ ] Проверена доступность Web App: `http://ваш-ip:8000`

### Настройка Telegram бота
- [ ] Создан бот через @BotFather
- [ ] Настроен Menu Button с URL приложения
- [ ] Проверена работа бота в Telegram

### Опционально (для продакшена)
- [ ] Настроен домен (Cloud DNS или внешний провайдер)
- [ ] Установлен SSL сертификат (Let's Encrypt)
- [ ] Настроен Nginx как reverse proxy
- [ ] Создан бакет в Object Storage
- [ ] Настроен CORS для Object Storage
- [ ] Настроен мониторинг и логирование

---

**Готово! Ваше приложение развернуто и готово к работе!** 🎉

Если возникли вопросы, проверьте раздел "Решение проблем" или логи приложения.

