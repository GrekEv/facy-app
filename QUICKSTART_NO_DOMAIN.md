# ⚡ Быстрый старт без домена - 5 минут

## 🎯 Вариант 1: Railway (Самый простой) ⭐

### Шаг 1: Регистрация (1 минута)
1. Откройте https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий

### Шаг 2: Настройка переменных (2 минуты)
В Railway → Settings → Variables добавьте:

```
BOT_TOKEN=ваш_токен_от_BotFather
WEBAPP_URL=https://your-app.up.railway.app
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
```

**Важно:** `WEBAPP_URL` сначала оставьте пустым, Railway даст вам URL после деплоя.

### Шаг 3: Деплой (2 минуты)
1. Railway автоматически определит Dockerfile и запустит деплой
2. Дождитесь завершения деплоя (2-3 минуты)
3. Скопируйте URL из Railway (типа: `https://your-app.up.railway.app`)
4. Обновите `WEBAPP_URL` в переменных окружения этим URL
5. Railway автоматически перезапустит сервис

### Шаг 4: Настройка бота (1 минута)
1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Bot Settings → Menu Button
5. Включите Menu Button
6. В поле URL вставьте ваш Railway URL: `https://your-app.up.railway.app`
7. Сохраните

### Шаг 5: Запуск бота локально
Бот можно запустить локально (API будет на Railway):

```bash
export BOT_TOKEN=ваш_токен
export WEBAPP_URL=https://your-app.up.railway.app
python main.py
```

Или создайте два сервиса в Railway:
- Первый сервис: API (команда: `python run_api.py`)
- Второй сервис: Bot (команда: `python main.py`)

### ✅ Готово!
Откройте бота в Telegram → `/start` → "🚀 Открыть приложение"

---

## 🎯 Вариант 2: ngrok (Для тестирования)

### Шаг 1: Установка ngrok (1 минута)
```bash
# macOS
brew install ngrok

# Linux
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar -xzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin/
```

### Шаг 2: Регистрация (1 минута)
1. Откройте https://ngrok.com
2. Зарегистрируйтесь (бесплатно)
3. Скопируйте токен авторизации

### Шаг 3: Авторизация (30 секунд)
```bash
ngrok config add-authtoken ваш_токен
```

### Шаг 4: Запуск (2 минуты)
```bash
# Установите переменные окружения
export BOT_TOKEN=ваш_токен_бота

# Запустите скрипт
./start_with_ngrok.sh
```

Скрипт автоматически:
1. Запустит API на порту 8000
2. Создаст ngrok туннель
3. Покажет HTTPS URL
4. Запустит бота

### Шаг 5: Настройка BotFather
1. Скопируйте ngrok URL из вывода скрипта
2. Откройте @BotFather → `/mybots` → ваш бот
3. Bot Settings → Menu Button → URL: `https://xxx.ngrok-free.app`

### ✅ Готово!

---

## 🎯 Вариант 3: Render

### Шаг 1: Регистрация (1 минута)
1. Откройте https://render.com
2. Войдите через GitHub

### Шаг 2: Создание сервиса (2 минуты)
1. New → Web Service
2. Подключите репозиторий
3. Настройки:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python run_api.py`
   - Environment: Python 3

### Шаг 3: Переменные окружения (1 минута)
```
BOT_TOKEN=ваш_токен
WEBAPP_URL=https://your-app.onrender.com
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
PORT=8000
```

### Шаг 4: Деплой и настройка бота (как в Railway)

---

## 🔍 Проверка работы

После развертывания проверьте:

```bash
# Проверка API
curl https://your-app-url/health
# Должно вернуть: {"status":"healthy"}

# Проверка главной страницы
curl https://your-app-url/
# Должна вернуться HTML страница
```

---

## ❓ Частые проблемы

### Web App не открывается в Telegram
- ✅ Убедитесь, что URL начинается с `https://`
- ✅ Проверьте Menu Button в BotFather
- ✅ Проверьте, что API отвечает: `curl https://your-url/health`

### API не отвечает
- ✅ Проверьте логи в Railway/Render
- ✅ Убедитесь, что порт правильный (8000)
- ✅ Проверьте переменные окружения

### ngrok URL меняется
- ✅ На бесплатном плане URL меняется при перезапуске
- ✅ Для стабильного URL используйте Railway/Render

---

## 📚 Подробная документация

См. [DEPLOY_NO_DOMAIN.md](DEPLOY_NO_DOMAIN.md) для детальной информации по всем вариантам.

