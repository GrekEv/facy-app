# Полная инструкция: Настройка PostgreSQL на Timeweb

## Оглавление
1. [Создание базы данных](#шаг-1-создание-базы-данных)
2. [Получение данных подключения](#шаг-2-получение-данных-подключения)
3. [Установка SSL сертификата](#шаг-3-установка-ssl-сертификата)
4. [Настройка на сервере](#шаг-4-настройка-на-сервере)
5. [Проверка подключения](#шаг-5-проверка-подключения)
6. [Решение проблем](#решение-проблем)

---

## Шаг 1: Создание базы данных

### 1.1. Откройте панель Timeweb
- Перейдите на https://timeweb.cloud/
- Войдите в аккаунт

### 1.2. Создайте базу данных
1. В левом меню найдите **"Базы данных"** (Databases)
2. Нажмите **"Создать базу данных"** или **"Заказать"**
3. Выберите конфигурацию:
   - **Тип:** PostgreSQL (рекомендуется PostgreSQL 18)
   - **Регион:** Выберите ближайший (например, Нидерланды)
   - **Конфигурация:** Минимум 1 CPU, 1 GB RAM, 8 GB NVMe

### 1.3. Настройка сети

**Вариант A: Публичный IP (рекомендуется для начала)**
- Нажмите **"+ Публичный IP"**
- Стоимость: ~150 ₽/мес
- **Преимущества:** Простое подключение с любого сервера
- **Недостатки:** Дополнительная стоимость

**Вариант B: Приватная сеть (если сервер и БД в одном проекте)**
- Нажмите **"+ Добавить сеть"**
- Выберите существующую приватную сеть или создайте новую
- Укажите приватный IP (например: 192.168.0.4)
- **Преимущества:** Бесплатно, безопаснее
- **Недостатки:** Работает только внутри приватной сети Timeweb

### 1.4. Дополнительные услуги
- **Бэкапы:** Рекомендуется включить (автоматические резервные копии)
- Остальные опции - по необходимости

### 1.5. Завершите создание
- Нажмите **"Заказать"**
- Дождитесь создания базы данных (обычно 1-2 минуты)

---

## Шаг 2: Получение данных подключения

### 2.1. Откройте базу данных
1. В списке баз данных найдите созданную базу
2. Нажмите на неё, чтобы открыть

### 2.2. Перейдите в раздел "Подключение"
1. Откройте вкладку **"Подключение"** (Connection)
2. Выберите **"Способ подключения"** → **"Командная строка"** (Command line)

### 2.3. Выберите параметры подключения
В выпадающих списках выберите:
- **Тип подключения:** "Домен" (Domain)
- **Пользователь:** `gen_user` (или другой пользователь)
- **База данных:** `default_db` (или другая база данных)

### 2.4. Скопируйте данные подключения
Из команды `psql` скопируйте:
- **Хост (Host):** формат `xxxxx.twc1.net` (например: `ad9d6b1abc9d6aa538e0dea5.twc1.net`)
- **Порт (Port):** обычно `5432`
- **Пользователь (User):** например, `gen_user`
- **База данных (Database):** например, `default_db`
- **Пароль (Password):** нажмите на иконку глаза 👁️, чтобы увидеть пароль

**Пример команды подключения:**
```bash
psql 'postgresql://gen_user:пароль@ad9d6b1abc9d6aa538e0dea5.twc1.net:5432/default_db?sslmode=verify-full'
```

### 2.5. Запишите IP адреса
На странице базы данных также будут указаны:
- **Публичный IP:** например, `72.56.80.65`
- **Приватный IP:** например, `192.168.0.4` (если используется приватная сеть)

---

## Шаг 3: Установка SSL сертификата

Timeweb требует SSL сертификат для безопасного подключения к PostgreSQL.

### 3.1. Скачайте сертификат

**Вариант A: Скачать напрямую на сервере (автоматически)**

```bash
ssh root@72.56.85.215

# Скачайте скрипт установки
wget https://raw.githubusercontent.com/GrekEv/facy-app/main/install_timeweb_cert.sh
chmod +x install_timeweb_cert.sh
./install_timeweb_cert.sh
```

**Вариант B: Загрузить с локального компьютера (если уже скачали)**

Если вы уже скачали `ca.crt` на свой компьютер:

```bash
# С вашего компьютера (в терминале)
scp ca.crt root@72.56.85.215:~/.cloud-certs/root.crt

# Затем на сервере
ssh root@72.56.85.215
chmod 0600 ~/.cloud-certs/root.crt
export PGSSLROOTCERT=$HOME/.cloud-certs/root.crt
```

**Вариант C: Скачать вручную на сервере**

```bash
ssh root@72.56.85.215

# Создайте директорию
mkdir -p ~/.cloud-certs

# Скачайте сертификат
curl -o ~/.cloud-certs/root.crt "https://st.timeweb.com/cloud-static/ca.crt"

# Установите права доступа
chmod 0600 ~/.cloud-certs/root.crt

# Настройте переменную окружения
export PGSSLROOTCERT=$HOME/.cloud-certs/root.crt

# Добавьте в ~/.bashrc для постоянного использования
echo 'export PGSSLROOTCERT=$HOME/.cloud-certs/root.crt' >> ~/.bashrc
```

### 3.2. Проверка установки сертификата

```bash
# Проверьте, что файл существует
ls -la ~/.cloud-certs/root.crt

# Должен показать:
# -rw------- 1 root root 1675 Nov 27 15:18 /root/.cloud-certs/root.crt
```

**Примечание:** Для Python приложений с `asyncpg` и параметром `?ssl=require` сертификат устанавливать не обязательно - библиотека сама обработает SSL. Но для командной строки (psql) и некоторых других инструментов сертификат нужен.

---

## Шаг 4: Настройка на сервере

### 4.1. Подключитесь к серверу

```bash
ssh root@72.56.85.215
cd ~/facy-app
```

### 4.2. Обновите .env файл

**Вариант A: Автоматически (скрипт)**

```bash
# Скачайте скрипт настройки
wget https://raw.githubusercontent.com/GrekEv/facy-app/main/setup_timeweb_db.sh
chmod +x setup_timeweb_db.sh
./setup_timeweb_db.sh
```

Скрипт спросит:
- Хост базы данных (например: `ad9d6b1abc9d6aa538e0dea5.twc1.net`)
- Порт (по умолчанию 5432)
- Имя базы данных (например: `default_db`)
- Пользователь (например: `gen_user`)
- Пароль

**Вариант B: Вручную**

```bash
# Откройте .env для редактирования
nano .env
```

**Удалите старую строку с Yandex Cloud:**
```
DATABASE_URL=postgresql+asyncpg://facy_user:etxX4gk272PdJYH@rc1a-6t9pb3se81b4idf5.mdb.yandexcloud.net:6432/facy_db?ssl=require
```

**Добавьте новую строку для Timeweb:**
```
DATABASE_URL=postgresql+asyncpg://gen_user:ваш_пароль@ad9d6b1abc9d6aa538e0dea5.twc1.net:5432/default_db?ssl=require
```

**Важно:**
- Формат хоста: `xxxxx.twc1.net` (не `postgres-xxx.timeweb.cloud`)
- Формат: `postgresql+asyncpg://` (не просто `postgresql://`)
- Параметр `?ssl=require` обязателен
- Если пароль содержит специальные символы (`@`, `+`, `~`, `#` и т.д.), можно использовать как есть - Python обработает их автоматически

**Пример с реальными данными:**
```
DATABASE_URL=postgresql+asyncpg://gen_user:e%7D%2B%400Go~xgGcwt@ad9d6b1abc9d6aa538e0dea5.twc1.net:5432/default_db?ssl=require
```

**Сохраните файл:**
- `Ctrl+O` (сохранить)
- `Enter` (подтвердить)
- `Ctrl+X` (выйти)

### 4.3. Перезапустите контейнеры

```bash
# Определите команду docker compose
if command -v docker-compose &> /dev/null; then
    COMPOSE="docker-compose"
else
    COMPOSE="docker compose"
fi

# Остановите контейнеры
$COMPOSE -f docker-compose.prod.yml down

# Запустите заново
$COMPOSE -f docker-compose.prod.yml up -d

# Подождите 30 секунд для запуска
sleep 30

# Проверьте статус
$COMPOSE -f docker-compose.prod.yml ps
```

---

## Шаг 5: Проверка подключения

### 5.1. Проверка health endpoint

```bash
# На сервере
curl http://localhost:8000/api/health

# Должен вернуть JSON с информацией о базе данных:
# {
#   "status": "ok",
#   "database": "connected" или "ok"
# }
```

### 5.2. Проверка через домен

```bash
# С вашего компьютера
curl https://onlyface.art/api/health
```

### 5.3. Проверка логов

```bash
# Посмотрите логи API контейнера
docker compose -f docker-compose.prod.yml logs api | grep -i database

# Или все логи
docker compose -f docker-compose.prod.yml logs api --tail=50
```

### 5.4. Проверка подключения через psql (опционально)

Если установлен PostgreSQL клиент:

```bash
# Установите PostgreSQL клиент (если не установлен)
apt update
apt install -y postgresql-client

# Подключитесь к базе данных
export PGSSLROOTCERT=$HOME/.cloud-certs/root.crt
psql 'postgresql://gen_user:пароль@ad9d6b1abc9d6aa538e0dea5.twc1.net:5432/default_db?sslmode=verify-full'

# Если подключение успешно, вы увидите приглашение psql:
# default_db=>
```

---

## Решение проблем

### Проблема: Ошибка подключения к базе данных

**Проверьте:**

1. **Правильность данных в .env:**
   ```bash
   cat .env | grep DATABASE_URL
   ```

2. **Формат хоста:** должен быть `xxxxx.twc1.net` (не `postgres-xxx.timeweb.cloud`)

3. **Формат DATABASE_URL:** должен быть `postgresql+asyncpg://` (не просто `postgresql://`)

4. **Параметр SSL:** должен быть `?ssl=require` в конце

5. **База данных запущена:** проверьте в панели Timeweb, что база данных включена

6. **Логи контейнера:**
   ```bash
   docker compose -f docker-compose.prod.yml logs api | grep -i error
   ```

### Проблема: "Connection refused"

**Возможные причины:**
- База данных не запущена в панели Timeweb
- Firewall блокирует порт 5432
- Неправильный хост или порт

**Решение:**
- Проверьте статус базы данных в панели Timeweb
- Убедитесь, что используете публичный IP или правильную приватную сеть

### Проблема: "SSL connection required"

**Решение:**
- Убедитесь, что в DATABASE_URL есть параметр `?ssl=require`
- Проверьте формат: `postgresql+asyncpg://...?ssl=require`

### Проблема: "Authentication failed"

**Решение:**
- Проверьте правильность пароля в .env
- Убедитесь, что пароль скопирован полностью (нажмите на иконку глаза в панели Timeweb)
- Если пароль содержит специальные символы, попробуйте URL-кодировать их

### Проблема: "Database does not exist"

**Решение:**
- Проверьте имя базы данных в .env
- Убедитесь, что база данных создана в панели Timeweb
- По умолчанию используется `default_db`

---

## Альтернатива: SQLite

Если не хотите настраивать PostgreSQL, можно использовать SQLite (локальная база данных):

```bash
ssh root@72.56.85.215
cd ~/facy-app

# Удалите DATABASE_URL из .env
sed -i '/^DATABASE_URL=/d' .env

# Создайте директорию для БД
mkdir -p data

# Перезапустите
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml up -d
```

SQLite создастся автоматически в `data/app.db`.

**Преимущества SQLite:**
- Не требует настройки
- Работает сразу
- Подходит для небольших проектов

**Недостатки SQLite:**
- Медленнее PostgreSQL
- Не подходит для высоких нагрузок
- Нет возможности масштабирования

---

## Важные моменты

### Публичный IP vs Приватная сеть

**Публичный IP:**
- ✅ Работает с любого сервера
- ✅ Проще настроить
- ❌ Дополнительная стоимость (~150 ₽/мес)
- ❌ Менее безопасно (нужно настроить firewall)

**Приватная сеть:**
- ✅ Бесплатно
- ✅ Безопаснее (недоступна из интернета)
- ❌ Работает только внутри Timeweb
- ❌ Нужно, чтобы сервер и БД были в одной сети

### Рекомендация

Для начала используйте **публичный IP** - это проще настроить. Позже можно переключиться на приватную сеть для экономии и безопасности.

---

## Полезные команды

```bash
# Проверка подключения к БД
curl http://localhost:8000/api/health

# Просмотр логов API
docker compose -f docker-compose.prod.yml logs api --tail=50

# Перезапуск только API контейнера
docker compose -f docker-compose.prod.yml restart api

# Проверка статуса контейнеров
docker compose -f docker-compose.prod.yml ps

# Просмотр переменных окружения в контейнере
docker compose -f docker-compose.prod.yml exec api env | grep DATABASE
```

---

## Дополнительная информация

- **Документация Timeweb:** https://timeweb.cloud/docs/
- **Документация asyncpg:** https://magicstack.github.io/asyncpg/
- **Документация PostgreSQL:** https://www.postgresql.org/docs/
