# 🚀 Развертывание на Vercel

## 📋 Обзор

Это руководство поможет развернуть ваш FastAPI бэкенд на Vercel вместе с фронтендом.

**Архитектура:**
- ✅ Backend (API) на Vercel Serverless Functions
- ✅ Frontend на Vercel (статический хостинг)
- ✅ База данных: PostgreSQL (рекомендуется) или внешний сервис

---

## ⚠️ Важные ограничения Vercel

### 1. База данных
- ❌ **SQLite НЕ работает** на Vercel (serverless функции не имеют постоянной файловой системы)
- ✅ **Используйте PostgreSQL** или другой внешний сервис БД:
  - Vercel Postgres (рекомендуется)
  - Railway PostgreSQL
  - Supabase
  - Neon
  - PlanetScale

### 2. Файловое хранилище
- ❌ Локальные файлы (`/uploads`, `/generated`) **НЕ сохраняются** между запросами
- ✅ **Используйте внешнее хранилище**:
  - Vercel Blob Storage
  - AWS S3
  - Cloudflare R2
  - Supabase Storage

### 3. Время выполнения
- ⏱️ Максимум 60 секунд на запрос (Pro план: до 300 секунд)
- ⚠️ Для длительных операций (генерация видео) используйте фоновые задачи

---

## 🚀 Быстрый старт

### Шаг 1: Подготовка проекта

1. **Убедитесь, что файлы созданы:**
   - ✅ `vercel.json` - конфигурация Vercel
   - ✅ `api/index.py` - точка входа для serverless функции
   - ✅ `requirements.txt` - зависимости Python

2. **Добавьте `mangum` в requirements.txt:**
   ```bash
   echo "mangum>=0.17.0" >> requirements.txt
   ```

### Шаг 2: Настройка базы данных

#### Вариант A: Vercel Postgres (рекомендуется)

1. В Vercel Dashboard → ваш проект → Storage → Create Database
2. Выберите **Postgres**
3. Скопируйте `POSTGRES_URL` из переменных окружения

#### Вариант B: Внешний PostgreSQL

Используйте любой PostgreSQL сервис:
- Railway PostgreSQL
- Supabase
- Neon
- PlanetScale

### Шаг 3: Настройка переменных окружения

В Vercel Dashboard → Settings → Environment Variables добавьте:

```bash
# Обязательные
BOT_TOKEN=ваш_токен_бота
WEBAPP_URL=https://your-app.vercel.app
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname

# Или для Vercel Postgres:
DATABASE_URL=$POSTGRES_URL

# API ключи (опционально)
DEEPFACE_API_KEY=your_key
FFANS_API_KEY=your_key
REPLICATE_API_KEY=your_key
OPENAI_API_KEY=your_key

# Провайдеры
IMAGE_GENERATION_PROVIDER=replicate
VIDEO_GENERATION_PROVIDER=sora
```

### Шаг 4: Деплой

#### Через Vercel CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в Vercel
vercel login

# Деплой
vercel

# Продакшен деплой
vercel --prod
```

#### Через GitHub:

1. Подключите репозиторий к Vercel
2. Vercel автоматически определит конфигурацию
3. Деплой произойдет автоматически при push в main

### Шаг 5: Настройка домена (опционально)

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен (например, `onlyface.art`)
3. Настройте DNS записи согласно инструкциям Vercel
4. Обновите `WEBAPP_URL` в переменных окружения

---

## 🔧 Адаптация кода для Vercel

### 1. Файловое хранилище

Замените локальное сохранение файлов на внешнее хранилище:

```python
# Вместо:
with open(file_path, "wb") as f:
    f.write(file_data)

# Используйте Vercel Blob или S3:
from vercel import BlobWriter
# или
import boto3  # для S3
```

### 2. База данных

Убедитесь, что используете PostgreSQL:

```python
# В .env или переменных окружения
DATABASE_URL=postgresql+asyncpg://user:password@host:port/dbname
```

### 3. Статические файлы

Статические файлы (`/static`) будут обслуживаться Vercel автоматически.

---

## 📁 Структура проекта для Vercel

```
telegram-deepface-app/
├── api/
│   ├── index.py          # Точка входа для Vercel
│   ├── main.py           # FastAPI приложение
│   └── ...
├── static/               # Статические файлы (автоматически обслуживаются)
├── templates/            # HTML шаблоны
├── vercel.json           # Конфигурация Vercel
├── requirements.txt      # Python зависимости
└── .env                  # Локальные переменные (не коммитить!)
```

---

## ✅ Проверка работы

После деплоя проверьте:

```bash
# Проверка API
curl https://your-app.vercel.app/health
# Должно вернуть: {"status":"healthy"}

# Проверка главной страницы
curl https://your-app.vercel.app/
# Должна вернуться HTML страница
```

---

## 🔍 Устранение проблем

### Проблема: "Module not found"

**Решение:**
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте, что `mangum` добавлен в requirements.txt

### Проблема: "Database connection error"

**Решение:**
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что используете PostgreSQL (не SQLite)
- Проверьте доступность БД из интернета

### Проблема: "File not found" при загрузке файлов

**Решение:**
- Используйте внешнее хранилище (Vercel Blob, S3)
- Не сохраняйте файлы локально на Vercel

### Проблема: "Function timeout"

**Решение:**
- Для длительных операций используйте фоновые задачи
- Увеличьте `maxDuration` в `vercel.json` (максимум 60 сек на бесплатном плане)

---

## 🎯 Рекомендации

### Для продакшена:

1. ✅ Используйте **Vercel Postgres** или внешний PostgreSQL
2. ✅ Используйте **Vercel Blob Storage** для файлов
3. ✅ Настройте **свой домен** (например, `onlyface.art`)
4. ✅ Используйте **Pro план** для увеличения лимитов

### Для разработки:

1. ✅ Используйте **Vercel CLI** для локального тестирования
2. ✅ Настройте **Preview deployments** для тестирования перед продакшеном

---

## 📚 Дополнительные ресурсы

- [Vercel Python Documentation](https://vercel.com/docs/functions/serverless-functions/runtimes/python)
- [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
- [Vercel Blob Storage](https://vercel.com/docs/storage/vercel-blob)
- [Mangum Documentation](https://mangum.io/)

---

## 🔄 Миграция с Yandex Cloud на Vercel

### Что нужно изменить:

1. ✅ База данных: SQLite → PostgreSQL
2. ✅ Файловое хранилище: локальное → Vercel Blob/S3
3. ✅ Конфигурация: Docker → Vercel Serverless
4. ✅ Домен: настройте в Vercel вместо Nginx

### Преимущества Vercel:

- ✅ Автоматический HTTPS
- ✅ Автоматическое масштабирование
- ✅ CDN для статических файлов
- ✅ Простое развертывание через Git
- ✅ Preview deployments для тестирования

---

**Готово! Ваш бэкенд теперь на Vercel! 🚀**

