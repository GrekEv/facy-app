# 🔐 Вход через серийную консоль

## Проблема: Неверный логин/пароль

В серийной консоли пользователь `ubuntu` создан **без пароля** и доступен только через SSH ключ.

## ✅ Решение 1: Используйте SSH (РЕКОМЕНДУЕТСЯ)

**SSH ключ уже добавлен!** Попробуйте подключиться:

```bash
ssh ubuntu@158.160.96.182
```

Если не работает, попробуйте:

```bash
ssh -i ~/.ssh/id_rsa ubuntu@158.160.96.182
```

## ✅ Решение 2: Войдите как root в серийной консоли

**В серийной консоли:**

1. В поле **"login:"** введите: `root`
2. Нажмите Enter
3. Пароль не требуется (или попробуйте пустой пароль)
4. После входа выполните:

```bash
# Переключитесь на пользователя ubuntu
su - ubuntu

# Или создайте пароль для ubuntu
passwd ubuntu
# Введите новый пароль дважды
```

## ✅ Решение 3: Добавьте пароль для ubuntu через root

**Если удалось войти как root:**

```bash
# Войдите как root (в серийной консоли)
login: root

# После входа выполните:
passwd ubuntu
# Введите новый пароль (например: OnlyFace2025!)
# Подтвердите пароль

# Теперь можно войти как ubuntu с паролем
exit
login: ubuntu
Password: OnlyFace2025!
```

## ✅ Решение 4: Используйте cloud-init для сброса пароля

**В серийной консоли как root:**

```bash
# Создайте пароль для ubuntu
echo 'ubuntu:OnlyFace2025!' | chpasswd

# Или через cloud-init
cloud-init clean
# Перезагрузите ВМ
reboot
```

## 💡 Рекомендация

**Используйте SSH** - это самый простой способ. SSH ключ уже добавлен, просто попробуйте:

```bash
ssh ubuntu@158.160.96.182
```

Если все еще не работает, войдите как `root` в серийной консоли и создайте пароль для `ubuntu`.


