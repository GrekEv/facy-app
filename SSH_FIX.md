# 🔐 Исправление SSH подключения

## Проблема: Permission denied (publickey)

Это означает, что SSH ключ не добавлен в Яндекс.Облако или используется неправильный ключ.

## ✅ Решение:

### Шаг 1: Получите ваш SSH ключ

**На вашем Mac выполните:**

```bash
# Проверьте наличие ключа
cat ~/.ssh/id_rsa.pub
# или
cat ~/.ssh/id_ed25519.pub
```

**Если ключа нет, создайте новый:**

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для всех вопросов (или укажите пароль)
```

**Скопируйте публичный ключ (весь вывод):**

```bash
cat ~/.ssh/id_ed25519.pub
# или
cat ~/.ssh/id_rsa.pub
```

### Шаг 2: Добавьте ключ в Яндекс.Облако

1. **Откройте консоль Яндекс.Облака:**
   - Перейдите на https://console.cloud.yandex.ru
   - Войдите в аккаунт

2. **Откройте вашу ВМ:**
   - Перейдите в **"Compute Cloud"** → **"Виртуальные машины"**
   - Найдите ВМ с IP `158.160.96.182`
   - Нажмите на неё

3. **Добавьте SSH ключ:**
   - Нажмите кнопку **"Редактировать"** (Edit) вверху страницы
   - Прокрутите до раздела **"Доступ"** (Access)
   - Найдите поле **"SSH-ключ"** (SSH key)
   - Нажмите **"Добавить ключ"** или **"Add key"**
   - Вставьте **весь** публичный ключ (начинается с `ssh-ed25519` или `ssh-rsa`)
   - Убедитесь, что ключ в одну строку (без переносов)
   - Нажмите **"Сохранить"** (Save)

4. **Подождите 1-2 минуты** для применения изменений

### Шаг 3: Подключитесь к серверу

**Попробуйте подключиться:**

```bash
ssh ubuntu@158.160.96.182
```

**Если не работает, попробуйте с указанием ключа:**

```bash
ssh -i ~/.ssh/id_ed25519 ubuntu@158.160.96.182
# или
ssh -i ~/.ssh/id_rsa ubuntu@158.160.96.182
```

### Шаг 4: Если все еще не работает

**Проверьте правильность ключа:**

```bash
# Покажите ваш публичный ключ
cat ~/.ssh/id_ed25519.pub
```

**Убедитесь, что:**
- Ключ скопирован полностью (от `ssh-ed25519` до конца)
- Нет лишних пробелов или переносов строк
- Ключ добавлен в правильную ВМ

**Попробуйте создать новый ключ специально для этого сервера:**

```bash
ssh-keygen -t ed25519 -C "onlyface-server" -f ~/.ssh/onlyface_key
cat ~/.ssh/onlyface_key.pub
```

**Добавьте этот новый ключ в Яндекс.Облако** (см. Шаг 2)

**Подключитесь с новым ключом:**

```bash
ssh -i ~/.ssh/onlyface_key ubuntu@158.160.96.182
```

## 🔍 Альтернатива: Использовать пароль (если настроен)

Если в ВМ настроен пароль для пользователя ubuntu:

```bash
ssh ubuntu@158.160.96.182
# Введите пароль когда попросит
```

## ✅ После успешного подключения:

Выполните команды из `DEPLOY_TO_SERVER.md` для развертывания приложения.


