# 🎨 Настройка фронтенда на Vercel

## Архитектура

- **Фронтенд**: Vercel (статический хостинг)
- **API**: Yandex Cloud (onlyface.art)
- **База данных**: Yandex Cloud PostgreSQL
- **Бот**: Yandex Cloud VM

## Автоматическое определение API URL

Фронтенд автоматически определяет, где он запущен:

1. **На onlyface.art** (Yandex Cloud):
   - Использует локальный API (относительные пути `/api/...`)
   - API работает на том же домене

2. **На Vercel** (любой другой домен):
   - Использует API на Yandex Cloud (`https://onlyface.art/api/...`)
   - Все запросы идут на ваш сервер

## Настройка в Vercel

### Переменные окружения (опционально)

Если нужно переопределить API URL, добавьте в Vercel Dashboard → Environment Variables:

```
API_BASE_URL=https://onlyface.art
```

Но это не обязательно - код автоматически определит правильный URL.

## Что работает

✅ Фронтенд на Vercel → API на Yandex Cloud  
✅ Фронтенд на onlyface.art → API на onlyface.art (локально)  
✅ Автоматическое определение без настройки

## Деплой

После пуша в GitHub Vercel автоматически задеплоит фронтенд.

Проверьте:
- Vercel URL открывается
- API запросы идут на `https://onlyface.art`
- Все функции работают

