# 🐘 Настройка PostgreSQL в Яндекс.Облаке

## 📋 Содержание

1. [Создание кластера PostgreSQL](#шаг-1-создание-кластера-postgresql)
2. [Настройка подключения](#шаг-2-настройка-подключения)
3. [Обновление приложения](#шаг-3-обновление-приложения)
4. [Миграция данных](#шаг-4-миграция-данных)
5. [Проверка работы](#шаг-5-проверка-работы)

---

## Шаг 1: Создание кластера PostgreSQL

### 1.1. Откройте консоль Яндекс.Облака

1. Перейдите на https://cloud.yandex.ru
2. Войдите в ваш аккаунт
3. Выберите нужный каталог (folder)

### 1.2. Создайте кластер PostgreSQL

1. В меню слева выберите **"Managed Databases"** → **"PostgreSQL"**
2. Нажмите **"Создать кластер"**

### 1.3. Настройте кластер

**Основные настройки:**
- **Имя кластера:** `facy-postgresql` (или любое другое)
- **Описание:** `База данных для Facy App` (опционально)
- **Окружение:** `Production` (или `Development` для теста)
- **Версия PostgreSQL:** `15` (рекомендуется) или `14`

**Вычислительные ресурсы:**
- **Класс хоста:** 
  - Для начала: `s2.micro` (2 vCPU, 8 GB RAM) - от ~500₽/месяц
  - Для продакшена: `s2.small` (4 vCPU, 16 GB RAM) - от ~1000₽/месяц
- **Размер диска:** `20 GB` (минимум) или больше
- **Тип диска:** `SSD` (рекомендуется)

**База данных:**
- **Имя БД:** `facy_db`
- **Имя пользователя:** `facy_user`
- **Пароль:** Сгенерируйте надежный пароль (сохраните его!)

**Сеть:**
- **Сеть:** Выберите сеть, где находится ваша ВМ
- **Подсеть:** Выберите подсеть
- **Публичный доступ:** ✅ **Включить** (для подключения с вашей ВМ)

**Группы безопасности:**
- ⚠️ **ВАЖНО:** Нужно настроить правила безопасности для доступа к PostgreSQL
- При создании кластера будет автоматически создана группа безопасности
- После создания кластера нужно добавить правило (см. подробную инструкцию ниже)

### 1.4. Создайте кластер

1. Нажмите **"Создать"**
2. Дождитесь создания кластера (5-10 минут)

### 1.5. Настройте группы безопасности для PostgreSQL

⚠️ **КРИТИЧЕСКИ ВАЖНО:** Без настройки групп безопасности вы не сможете подключиться к PostgreSQL!

**Где найти группы безопасности:**

1. **Способ 1: Через VPC (Virtual Private Cloud) - РЕКОМЕНДУЕТСЯ**
   - В **главном меню слева** консоли Яндекс.Облака найдите **"Virtual Private Cloud"** (или **"VPC"**)
   - Нажмите на **"Virtual Private Cloud"**
   - В подменю выберите **"Группы безопасности"** (Security Groups)
   - Найдите группу безопасности, связанную с вашим кластером PostgreSQL
   - Обычно она называется `default-sg-xxxxx` или имеет имя вашего кластера
   - Нажмите на название группы безопасности, чтобы открыть её настройки

2. **Способ 2: Через редактирование кластера**
   - Откройте созданный кластер PostgreSQL
   - Нажмите кнопку **"Редактировать"** (Edit) вверху страницы
   - В разделе **"Сеть"** или **"Network"** найдите поле **"Группы безопасности"**
   - Там будет указана группа безопасности (например: `sg-xxxxx`)
   - Нажмите на название группы безопасности - откроется страница группы

3. **Способ 3: Через раздел "Подключения"**
   - Откройте кластер PostgreSQL
   - В левом меню найдите **"Подключения"** (Connections)
   - Там может быть ссылка на группу безопасности

**Как добавить правило безопасности:**

1. **Откройте группу безопасности:**
   - Нажмите на группу безопасности, чтобы открыть её настройки

2. **Перейдите к правилам:**
   - В открывшейся странице перейдите на вкладку **"Правила"** или **"Rules"**
   - Вы увидите список существующих правил (если есть)

3. **Добавьте правило для PostgreSQL:**
   - Нажмите кнопку **"Добавить правило"** или **"Создать правило"** или **"Add Rule"**
   - Заполните форму:

   **Настройки правила:**
   - **Направление:** Входящий трафик (Inbound) ✅
   - **Протокол:** TCP
   - **Порт:** `6432` (это порт PostgreSQL в Яндекс.Облаке)
   - **Источник:** Выберите один из вариантов:
     - **CIDR** - укажите IP адрес вашей ВМ (например: `158.160.96.182/32`)
     - **CIDR** - укажите подсеть (например: `10.0.0.0/24` если ВМ в той же подсети)
     - **CIDR** - для тестирования можно указать `0.0.0.0/0` (разрешить со всех IP, но небезопасно!)
   - **Описание:** `PostgreSQL доступ с ВМ` (опционально)

4. **Сохраните правило:**
   - Нажмите **"Сохранить"** или **"Save"**
   - Правило появится в списке правил входящего трафика

5. **Проверьте правило:**
   - Убедитесь, что правило добавлено и активно
   - Правило должно быть в разделе **"Входящий трафик"** (Inbound)

**Пример настройки правила:**

```
Направление: Входящий трафик
Протокол: TCP
Порт: 6432
Источник: CIDR
Значение: 158.160.96.182/32  (IP вашей ВМ)
Описание: PostgreSQL доступ
```

**Если ВМ и PostgreSQL в одной подсети (РЕКОМЕНДУЕТСЯ):**

Если ваша ВМ и кластер PostgreSQL находятся в одной подсети, можно указать всю подсеть:

```
Источник: CIDR
Значение: 10.0.0.0/24  (пример подсети - замените на вашу)
```

✅ **Преимущества подключения через внутреннюю сеть:**
- Быстрее (низкая задержка)
- Безопаснее (не нужен публичный доступ)
- Бесплатно (нет трафика через интернет)
- Используйте тот же FQDN хост - подключение автоматически пойдет через внутреннюю сеть

**Для большей безопасности:**

Рекомендуется разрешить доступ только с IP вашей ВМ:
- Найдите публичный IP вашей ВМ в консоли
- Используйте формат: `IP_АДРЕС/32` (например: `158.160.96.182/32`)

**Где найти IP вашей ВМ:**

1. Перейдите в **"Compute Cloud"** → **"Виртуальные машины"**
2. Откройте вашу ВМ
3. Скопируйте **публичный IPv4 адрес**

---

## Шаг 2: Настройка подключения

### 2.1. Получите данные для подключения

⚠️ **ВАЖНО:** Сохраните эти данные в безопасном месте! Они понадобятся для подключения приложения к базе данных.

**Пошаговая инструкция:**

1. **Откройте созданный кластер PostgreSQL:**
   - В консоли Яндекс.Облака перейдите в **"Managed Databases"** → **"PostgreSQL"**
   - Найдите ваш кластер `facy-postgresql` в списке
   - Нажмите на название кластера, чтобы открыть его

2. **Перейдите во вкладку "Подключение":**
   - В левом меню кластера найдите пункт **"Подключения"** (Connections)
   - Или на главной странице кластера найдите вкладку **"Подключение"** вверху
   - Нажмите на **"Подключения"** или **"Подключение"**

3. **Скопируйте данные подключения:**
   
   На странице подключения вы увидите несколько разделов. 
   
   **Если подключаетесь с ВМ в той же подсети (РЕКОМЕНДУЕТСЯ):**
   - Найдите раздел **"Подключение из интернета"** или **"Public connection"**
   - Используйте тот же FQDN хост - подключение будет идти через внутреннюю сеть
   - ✅ **Преимущества:** Быстрее, безопаснее, не нужен публичный доступ
   
   **Если подключаетесь из интернета:**
   - Найдите раздел **"Подключение из интернета"** или **"Public connection"**
   - Используйте FQDN хост
   - ⚠️ Нужно настроить группы безопасности для публичного доступа
   
   **Скопируйте следующие данные:**
   
   - **Хост (Host):** 
     - Это будет строка вида: `c-xxxxx.rw.mdb.yandexcloud.net`
     - Пример: `c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net`
     - Это FQDN (полное доменное имя) вашего кластера
     - ⚠️ Скопируйте полностью, включая `.rw.mdb.yandexcloud.net`
     - 💡 **Важно:** Тот же хост используется и для внутреннего подключения с ВМ в той же подсети!
     - 💡 **Подсказка:** Идентификатор кластера (например: `c9qpp92q6spvmgf13dts`) - это часть хоста, но хост начинается с `c-` и заканчивается `.rw.mdb.yandexcloud.net`
   
   - **Порт (Port):** 
     - Для подключения: `6432` (одинаковый для публичного и внутреннего доступа)
     - Это стандартный порт PostgreSQL в Яндекс.Облаке
   
   - **Имя БД (Database name):** 
     - `facy_db` (то, что вы указали при создании кластера)
     - Если вы использовали другое имя, используйте его
   
   - **Пользователь (User):** 
     - `facy_user` (то, что вы указали при создании кластера)
     - Если вы использовали другое имя пользователя, используйте его
     - ⚠️ **Важно:** Используйте именно **имя пользователя** (`facy_user`), а не идентификатор!
     - Идентификатор (например: `a593v5u3pd2646dqhvla`) - это внутренний ID в системе Яндекс.Облака, он **НЕ нужен** для подключения
   
   - **Пароль (Password):** 
     - Это пароль, который вы создали при создании кластера
     - ⚠️ Если вы забыли пароль, его можно сбросить в разделе **"Пользователи"**
     - Пароль не отображается в интерфейсе по соображениям безопасности
   
   **Примечание об идентификаторе:**
   
   В разделе **"Пользователи"** вы можете увидеть:
   - **Идентификатор** (например: `a593v5u3pd2646dqhvla`) - это внутренний ID пользователя в системе Яндекс.Облака
   - **Имя пользователя** (например: `facy_user`) - это то, что нужно использовать для подключения
   
   ⚠️ **Для подключения используйте только имя пользователя (`facy_user`), идентификатор НЕ нужен!**

**Где найти пароль, если забыли:**

1. В кластере PostgreSQL перейдите в раздел **"Пользователи"** (Users)
2. Найдите пользователя `facy_user`
3. Нажмите на три точки (⋮) или кнопку **"Изменить"**
4. Вы можете **сбросить пароль** или **создать новый пароль**

**Пример данных подключения (на основе вашего кластера):**

```
Хост: c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net
Порт: 6432
База данных: facy_db
Пользователь: facy_user
Пароль: postgresql_password
```

**Что использовать из информации о подключении:**

✅ **Используйте:**
- **Имя пользователя:** `facy_user` ✅
- **Пароль:** `postgresql_password` (из секрета) ✅
- **База данных:** `facy_db` ✅
- **Хост:** `c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net` (найдите в разделе "Подключения") ✅
- **Порт:** `6432` ✅

❌ **НЕ используйте (это внутренние идентификаторы):**
- Идентификатор пользователя: `a593v5u3pd2646dqhvla` ❌
- Идентификатор секрета: `e6qoctog34tmam7enitd` ❌
- Идентификатор кластера: `c9qpp92q6spvmgf13dts` (используется только как часть хоста) ❌

**Строка подключения:**

После получения всех данных составьте строку подключения в следующем формате:
```
postgresql+asyncpg://facy_user:postgresql_password@c-xxxxx.rw.mdb.yandexcloud.net:6432/facy_db?ssl=require
```

**Где:**
- `facy_user` - имя пользователя
- `postgresql_password` - ваш пароль
- `c-xxxxx.rw.mdb.yandexcloud.net` - хост из раздела "Подключения"
- `6432` - порт
- `facy_db` - имя базы данных
- `ssl=require` - обязательное SSL подключение

**Пример полной строки подключения (для вашего кластера):**
```
postgresql+asyncpg://facy_user:postgresql_password@c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net:6432/facy_db?ssl=require
```

⚠️ **Важно:** 
- Если идентификатор вашего кластера `c9qpp92q6spvmgf13dts`, то хост будет: `c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net`
- Убедитесь, что хост правильный - найдите его в разделе **"Подключения"** → **"Подключение из интернета"**

⚠️ **Не забудьте:**
- Сохранить эти данные в безопасном месте
- Не делиться паролем публично
- Использовать SSL подключение (`ssl=require`)

### 2.2. Проверьте подключение (опционально)

**С вашей ВМ выполните:**

```bash
# Установка PostgreSQL клиента
sudo apt update
sudo apt install -y postgresql-client

# Подключение к базе
psql "host=c-xxxxx.rw.mdb.yandexcloud.net port=6432 dbname=facy_db user=facy_user password=postgresql_password sslmode=require"

# Если подключились успешно, выйдите:
\q
```

---

## Шаг 3: Обновление приложения

### 3.1. Установите драйвер PostgreSQL

**На сервере выполните:**

```bash
cd ~/facy-app

# Обновите requirements.txt (если еще не обновлен)
# Добавьте: asyncpg==0.29.0

# Пересоберите Docker образ
docker compose -f docker-compose.prod.yml build --no-cache
```

### 3.2. Обновите .env файл

**На сервере выполните:**

```bash
cd ~/facy-app
nano .env
```

**Измените DATABASE_URL:**

```env
# Старый (SQLite):
# DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Новый (PostgreSQL):
DATABASE_URL=postgresql+asyncpg://facy_user:postgresql_password@c-xxxxx.rw.mdb.yandexcloud.net:6432/facy_db?ssl=require
```

**Формат строки подключения:**
```
postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DATABASE?ssl=require
```

**Пример для вашего кластера (замените хост на реальный из раздела "Подключения"):**
```env
DATABASE_URL=postgresql+asyncpg://facy_user:postgresql_password@c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net:6432/facy_db?ssl=require
```

**Если идентификатор кластера `c9qpp92q6spvmgf13dts`, то хост:** `c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net`

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 3.3. Обновите docker-compose.prod.yml (если нужно)

Убедитесь, что переменная `DATABASE_URL` передается из `.env`:

```yaml
services:
  api:
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      # ... остальные переменные
```

---

## Шаг 4: Миграция данных

### 4.1. Создайте резервную копию SQLite

**На сервере выполните:**

```bash
cd ~/facy-app

# Создайте резервную копию
cp data/app.db data/app.db.backup

# Проверьте размер базы
ls -lh data/app.db
```

### 4.2. Запустите приложение с PostgreSQL

**На сервере выполните:**

```bash
cd ~/facy-app

# Остановите текущие контейнеры
docker compose -f docker-compose.prod.yml down

# Запустите с новой базой данных
docker compose -f docker-compose.prod.yml up -d

# Проверьте логи
docker compose -f docker-compose.prod.yml logs api --tail=50
```

**При первом запуске:**
- Приложение автоматически создаст все таблицы в PostgreSQL
- Данные из SQLite нужно будет мигрировать вручную (если они есть)

### 4.3. Миграция данных из SQLite в PostgreSQL (если есть данные)

**Создайте скрипт миграции:**

```bash
cd ~/facy-app
cat > migrate_to_postgresql.py << 'PYEOF'
#!/usr/bin/env python3
"""Скрипт миграции данных из SQLite в PostgreSQL"""
import asyncio
import sys
from sqlalchemy import create_engine, text
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
import sqlite3

# Подключение к SQLite
sqlite_conn = sqlite3.connect('data/app.db')
sqlite_cursor = sqlite_conn.cursor()

# Подключение к PostgreSQL (из .env)
from config import settings
postgres_engine = create_async_engine(settings.DATABASE_URL, echo=True)

async def migrate():
    """Миграция данных"""
    async with postgres_engine.begin() as conn:
        # Получаем список таблиц из SQLite
        sqlite_cursor.execute("SELECT name FROM sqlite_master WHERE type='table';")
        tables = [row[0] for row in sqlite_cursor.fetchall()]
        
        print(f"Найдено таблиц: {tables}")
        
        for table in tables:
            if table == 'sqlite_sequence':
                continue
                
            # Получаем данные из SQLite
            sqlite_cursor.execute(f"SELECT * FROM {table}")
            rows = sqlite_cursor.fetchall()
            
            if not rows:
                print(f"Таблица {table} пуста, пропускаем")
                continue
            
            # Получаем названия колонок
            sqlite_cursor.execute(f"PRAGMA table_info({table})")
            columns = [col[1] for col in sqlite_cursor.fetchall()]
            
            print(f"Миграция таблицы {table}: {len(rows)} записей")
            
            # Вставляем данные в PostgreSQL
            for row in rows:
                values = dict(zip(columns, row))
                placeholders = ', '.join([f':{col}' for col in columns])
                columns_str = ', '.join(columns)
                
                query = f"""
                    INSERT INTO {table} ({columns_str})
                    VALUES ({placeholders})
                    ON CONFLICT DO NOTHING
                """
                
                await conn.execute(text(query), values)
        
        await conn.commit()
        print("✅ Миграция завершена!")

if __name__ == "__main__":
    asyncio.run(migrate())
    sqlite_conn.close()
PYEOF

chmod +x migrate_to_postgresql.py

# Запустите миграцию
python3 migrate_to_postgresql.py
```

**Или используйте готовый скрипт:**

```bash
# Если у вас уже есть данные в SQLite
docker compose -f docker-compose.prod.yml exec api python migrate_to_postgresql.py
```

---

## Шаг 5: Проверка работы

### 5.1. Проверьте подключение

**На сервере выполните:**

```bash
cd ~/facy-app

# Проверьте логи API
docker compose -f docker-compose.prod.yml logs api | grep -i "database\|postgres\|error"

# Проверьте статус контейнеров
docker compose -f docker-compose.prod.yml ps

# Проверьте health endpoint
curl http://localhost:8000/health
```

### 5.2. Проверьте данные в PostgreSQL

**Подключитесь к базе:**

```bash
psql "host=c-xxxxx.rw.mdb.yandexcloud.net port=6432 dbname=facy_db user=facy_user password=postgresql_password sslmode=require"

# Проверьте таблицы
\dt

# Проверьте количество записей
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM generations;

# Выйдите
\q
```

### 5.3. Проверьте работу приложения

1. Откройте веб-интерфейс: `http://158.160.96.182:8000`
2. Проверьте работу бота в Telegram
3. Создайте тестового пользователя
4. Проверьте, что данные сохраняются в PostgreSQL

---

## 🔧 Устранение проблем

### Проблема: Не удается подключиться к PostgreSQL

**Решение:**
1. Проверьте, что **публичный доступ включен** в настройках кластера
2. Проверьте **группы безопасности** - порт 6432 должен быть открыт
3. Проверьте правильность строки подключения в `.env`
4. Проверьте SSL режим: `ssl=require` или `sslmode=require`

### Проблема: Ошибка "asyncpg not found"

**Решение:**
```bash
# Убедитесь, что asyncpg добавлен в requirements.txt
grep asyncpg requirements.txt

# Пересоберите образ
docker compose -f docker-compose.prod.yml build --no-cache
```

### Проблема: Ошибка миграции

**Решение:**
1. Убедитесь, что таблицы созданы в PostgreSQL
2. Проверьте логи: `docker compose logs api`
3. Если нужно, удалите таблицы и создайте заново:
   ```bash
   # В PostgreSQL
   DROP TABLE IF EXISTS users, generations, transactions CASCADE;
   # Перезапустите приложение - таблицы создадутся автоматически
   ```

---

## 💰 Стоимость

**Яндекс.Облако Managed PostgreSQL:**

- **Минимальная конфигурация (s2.micro):**
  - 2 vCPU, 8 GB RAM, 20 GB SSD
  - От ~500₽/месяц

- **Рекомендуемая конфигурация (s2.small):**
  - 4 vCPU, 16 GB RAM, 50 GB SSD
  - От ~1000₽/месяц

- **Бесплатный период:** Первые 60 дней можно использовать бесплатно (если есть бесплатные средства)

---

## 📊 Мониторинг

### Просмотр метрик в Яндекс.Облаке

1. Откройте кластер PostgreSQL
2. Перейдите во вкладку **"Мониторинг"**
3. Просматривайте:
   - Использование CPU
   - Использование памяти
   - Использование диска
   - Количество подключений
   - Запросов в секунду

### Настройка алертов

1. В разделе **"Мониторинг"** → **"Алерты"**
2. Создайте алерты на:
   - Высокое использование CPU (>80%)
   - Нехватку памяти
   - Нехватку места на диске
   - Большое количество подключений

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте надежный пароль** (минимум 16 символов)
2. **Включите SSL** (`ssl=require` в строке подключения)
3. **Ограничьте доступ** через группы безопасности (только с вашей ВМ)
4. **Регулярно делайте резервные копии** (автоматически в Яндекс.Облаке)
5. **Не храните пароль в коде** - используйте переменные окружения

---

## ✅ Готово!

После настройки PostgreSQL:
- ✅ База данных будет в облаке (надежнее)
- ✅ Автоматические резервные копии
- ✅ Лучшая производительность
- ✅ Масштабируемость

**Важно:** После миграции на PostgreSQL можно удалить файл `data/app.db` (но лучше оставить резервную копию на некоторое время).

