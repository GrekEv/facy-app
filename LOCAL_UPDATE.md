# 🚀 Обновление локальной версии

## ✅ Что было обновлено:

1. **Новые API эндпоинты:**
   - `POST /api/register` - регистрация с реферальным кодом
   - `POST /api/login` - вход пользователя
   - `POST /api/logout` - выход пользователя
   - `GET /api/stats` - статистика системы

2. **Всплывающее окно первого входа:**
   - Модальное окно при первом посещении
   - Поле для ввода реферального кода
   - Автоматическое предложение создать первое видео/изображение

3. **Реферальная система:**
   - Генерация уникального кода для каждого пользователя
   - Обработка реферальных кодов при регистрации
   - Начисление бонусов рефереру

4. **Обновленный фронтенд:**
   - Динамические кнопки входа/выхода
   - Проверка авторизации
   - Загрузка статистики

## 🏃 Запуск локального сервера:

### Вариант 1: Использовать скрипт перезапуска
```bash
./restart_local.sh
```

### Вариант 2: Использовать существующий скрипт
```bash
./start_local.sh
```

### Вариант 3: Запустить напрямую
```bash
export BOT_TOKEN=test_token_123
export WEBAPP_URL=http://localhost:8000
export ENVIRONMENT=development
export HOST=127.0.0.1
export PORT=8000

python3 run_api.py
```

## 🌐 Проверка работы:

После запуска сервера откройте в браузере:
- **Главная страница:** http://localhost:8000
- **Health check:** http://localhost:8000/health
- **Статистика:** http://localhost:8000/api/stats
- **API документация:** http://localhost:8000/docs

## 🧪 Тестирование функций:

1. **Первый вход:**
   - Откройте http://localhost:8000
   - Должно появиться модальное окно с предложением ввести реферальный код
   - Зарегистрируйтесь или пропустите

2. **Регистрация:**
   - Введите реферальный код (опционально)
   - Нажмите "Зарегистрироваться"
   - После регистрации откроется модальное окно создания контента

3. **Вход/Выход:**
   - Кнопка в хедере меняется на "Выход" после входа
   - При выходе состояние очищается

4. **Статистика:**
   - Статистика загружается автоматически при загрузке страницы
   - Проверьте консоль браузера для просмотра данных

## 📝 Примечания:

- Все функции подключены к БД через SQLAlchemy
- Реферальные коды генерируются автоматически для всех пользователей
- Статистика берется из базы данных в реальном времени
- Состояние входа сохраняется в localStorage

## 🔧 Если что-то не работает:

1. Проверьте, что порт 8000 свободен:
   ```bash
   lsof -ti:8000
   ```

2. Остановите старый процесс:
   ```bash
   pkill -f "uvicorn.*api.main"
   pkill -f "python.*run_api"
   ```

3. Проверьте логи сервера в консоли

4. Убедитесь, что все зависимости установлены:
   ```bash
   pip install -r requirements.txt
   ```

