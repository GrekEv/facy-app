# 🚀 Быстрый старт

## Минимальная настройка (5 минут)

### 1. Получите токен бота
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### 2. Настройте проект
```bash
cd telegram-deepface-app

# Создайте .env файл
cp .env.example .env

# Откройте .env и вставьте токен:
# BOT_TOKEN=ваш_токен_здесь
```

### 3. Запустите приложение

**На macOS/Linux:**
```bash
./start.sh
```

**На Windows:**
```bash
start.bat
```

**Или вручную:**
```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите API (в одном терминале)
python run_api.py

# Запустите бота (в другом терминале)
python main.py
```

### 4. Настройте Web App в боте
1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выберите вашего бота
3. `Bot Settings` → `Menu Button` → `Edit menu button URL`
4. Укажите: `http://ваш-домен.com` (или `http://localhost:8000` для тестов)

### 5. Тестирование
1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите "🚀 Открыть приложение"

## Готово! 🎉

Ваше приложение работает!

## Что дальше?

### Для локального тестирования
- Используйте [ngrok](https://ngrok.com) для создания публичного URL:
  ```bash
  ngrok http 8000
  ```
- Используйте полученный URL в настройках Web App

### Для продакшена
1. Разверните на VPS
2. Настройте nginx + SSL
3. Получите API ключи от [deepfacevideo.com](https://deepfacevideo.com) и [ffans.ai](https://ffans.ai)
4. Обновите `.env` с реальными ключами

### С помощью Docker
```bash
docker-compose up -d
```

## Проблемы?

**Бот не отвечает:**
- Проверьте правильность токена в `.env`
- Убедитесь, что `python main.py` запущен без ошибок

**Web App не открывается:**
- Убедитесь, что `python run_api.py` запущен
- Проверьте, что URL в BotFather указан правильно
- Для локального теста используйте ngrok

**Ошибки при установке:**
- Убедитесь, что у вас Python 3.9 или выше
- На macOS может потребоваться: `brew install python@3.11`
- На Ubuntu: `sudo apt install python3.11 python3.11-venv`

Полная документация: [README.md](README.md)

