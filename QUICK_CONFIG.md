# ⚡ Быстрая настройка WEBAPP_URL

## 🎯 Ваш случай: С доменом

Если у вас есть домен (например, `onlyface.art`):

```bash
# В .env файле
WEBAPP_URL=https://onlyface.art
```

**Готово!** Приложение будет использовать ваш домен.

---

## 🔄 Альтернативные варианты

### Без домена (Railway/Render)

```bash
# Оставьте пустым - определится автоматически
WEBAPP_URL=

# ИЛИ укажите явно
WEBAPP_URL=https://your-app.up.railway.app
```

### Локальное тестирование (ngrok)

```bash
WEBAPP_URL=https://xxx.ngrok-free.app
```

---

## ✅ Как это работает

1. **Если указан `WEBAPP_URL`** → используется указанный URL
2. **Если пустой на Railway** → автоматически определяется из `RAILWAY_PUBLIC_DOMAIN`
3. **Если пустой на Render** → автоматически определяется из `RENDER_EXTERNAL_URL`
4. **Если ничего не найдено** → используется `http://localhost:8000`

---

## 📝 Важно

- ✅ Приложение работает **и с доменом, и без домена**
- ✅ Можно указать явно или оставить пустым для автоматического определения
- ✅ Главное - указать правильный URL в BotFather (Menu Button)

---

## 🔍 Проверка

После настройки проверьте:

```bash
# Проверка API
curl https://your-url/health
# Должно вернуть: {"status":"healthy"}
```

В Telegram:
1. Откройте бота
2. `/start`
3. Нажмите "🚀 Открыть приложение"
4. Должно открыться Web App!

