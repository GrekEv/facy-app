# 🔧 Исправление конфигурации Vercel

## Проблема

Ошибка: `The functions property cannot be used in conjunction with the builds property`

## Решение

Обновлен `vercel.json` для использования современного формата Vercel без `builds`.

### Что изменено:

1. ✅ Удалено свойство `builds`
2. ✅ Использован `rewrites` вместо `routes` для маршрутизации
3. ✅ Оставлено только `functions` для настройки серверлес-функций
4. ✅ Добавлен `buildCommand` для установки зависимостей

### Структура проекта для Vercel:

```
telegram-deepface-app/
├── api/
│   └── index.py          # Serverless функция (точка входа)
├── static/               # Статические файлы (автоматически обслуживаются)
├── templates/            # HTML шаблоны
├── vercel.json           # Конфигурация Vercel
└── requirements.txt      # Python зависимости
```

## Деплой

После исправления конфигурации:

1. Закоммитьте изменения:
   ```bash
   git add vercel.json
   git commit -m "Fix Vercel configuration"
   git push
   ```

2. Vercel автоматически пересоберет проект

3. Или запустите деплой вручную:
   ```bash
   vercel --prod
   ```

## Проверка

После деплоя проверьте:
- ✅ `/health` - должен вернуть `{"status":"healthy"}`
- ✅ `/` - должна открыться главная страница
- ✅ `/api/user/{telegram_id}` - должен работать API

