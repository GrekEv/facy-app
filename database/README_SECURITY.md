# 🔐 Правила безопасности базы данных

## 📍 Где прописывать правила безопасности

Правила безопасности для PostgreSQL прописываются в файле:

```
database/security_policies.sql
```

Этот файл автоматически применяется при инициализации базы данных через функцию `init_db()` в `database/database.py`.

## 🚀 Как это работает

1. **При запуске приложения** вызывается `init_db()`
2. **Создаются все таблицы** через SQLAlchemy
3. **Автоматически применяются правила безопасности** из `security_policies.sql` (только для PostgreSQL)

## 📝 Структура правил безопасности

### 1. Row Level Security (RLS)

Включение RLS для всех таблиц:
```sql
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
```

### 2. Политики доступа

Для каждой таблицы создаются политики:
- **SELECT** - кто может читать данные
- **INSERT** - кто может создавать данные
- **UPDATE** - кто может обновлять данные
- **DELETE** - кто может удалять данные

### 3. Примеры политик

**Пользователи видят только свои данные:**
```sql
CREATE POLICY users_select_own ON users
    FOR SELECT
    USING (user_id = current_user_id());
```

**Пользователи могут создавать только свои генерации:**
```sql
CREATE POLICY generations_insert_own ON generations
    FOR INSERT
    WITH CHECK (user_id = current_user_id());
```

## 🔧 Ручное применение правил

Если нужно применить правила вручную:

### Через psql:

```bash
psql "host=YOUR_HOST port=6432 dbname=facy_db user=facy_user password=YOUR_PASSWORD sslmode=require" -f database/security_policies.sql
```

### Через Python:

```python
from database.database import apply_security_policies
import asyncio

asyncio.run(apply_security_policies())
```

## ⚙️ Настройка правил

### Текущее состояние

Сейчас все политики используют `USING (true)` - это означает, что правила безопасности **временно отключены** для упрощения разработки.

### Для продакшена

Нужно заменить `USING (true)` на реальные проверки:

```sql
-- Вместо:
USING (true)

-- Использовать:
USING (user_id = current_setting('app.current_user_id')::integer)
```

### Создание функции current_user_id()

Для полноценной работы RLS нужно создать функцию:

```sql
CREATE OR REPLACE FUNCTION current_user_id()
RETURNS INTEGER AS $$
BEGIN
    RETURN current_setting('app.current_user_id', true)::integer;
END;
$$ LANGUAGE plpgsql;
```

И устанавливать ID пользователя в сессии:

```python
# В database/database.py при создании сессии
async with AsyncSessionLocal() as session:
    await session.execute(text("SET app.current_user_id = :user_id"), {"user_id": user_id})
    # ... работа с сессией
```

## 📋 Таблицы с правилами безопасности

1. **users** - пользователи видят только свои данные
2. **generations** - пользователи видят только свои генерации (или публичные)
3. **transactions** - пользователи видят только свои транзакции
4. **payment_methods** - пользователи видят только свои методы оплаты
5. **withdrawals** - пользователи видят только свои выводы
6. **audit_logs** - пользователи видят только свои логи
7. **reports** - пользователи видят только свои жалобы
8. **rate_limits** - пользователи видят только свои ограничения
9. **promo_codes** - все могут видеть активные промокоды

## 🔒 Дополнительные меры безопасности

### Индексы

В файле `security_policies.sql` также создаются индексы для производительности:
- `idx_users_telegram_id` - быстрый поиск по telegram_id
- `idx_generations_user_status` - поиск генераций по пользователю и статусу
- `idx_transactions_user_status` - поиск транзакций по пользователю и статусу
- `idx_promo_codes_active` - поиск активных промокодов

### Триггеры

Можно добавить триггеры для дополнительной защиты:
- Предотвращение прямого изменения баланса
- Автоматическое логирование изменений
- Проверка целостности данных

## ⚠️ Важные замечания

1. **RLS работает только в PostgreSQL** - для SQLite правила безопасности не применяются
2. **Политики применяются автоматически** при инициализации БД
3. **Ошибки применения правил не прерывают** инициализацию БД
4. **В продакшене нужно настроить** реальные проверки вместо `USING (true)`

## 🐛 Устранение проблем

### Правила не применяются

1. Проверьте, что используется PostgreSQL:
   ```python
   print(settings.DATABASE_URL.startswith("postgresql"))
   ```

2. Проверьте логи при запуске:
   ```bash
   docker compose logs api | grep "безопасности"
   ```

3. Примените правила вручную через psql

### Ошибка "policy already exists"

Это нормально - правила уже применены. Можно игнорировать или добавить `IF NOT EXISTS` (PostgreSQL 9.5+).

### Правила не работают

Убедитесь, что:
- RLS включен для таблицы: `ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;`
- Политики созданы: `\d+ table_name` в psql
- Функция `current_user_id()` существует (если используется)

## 📚 Дополнительные ресурсы

- [PostgreSQL Row Level Security](https://www.postgresql.org/docs/current/ddl-rowsecurity.html)
- [SQLAlchemy и RLS](https://docs.sqlalchemy.org/en/20/dialects/postgresql.html)

