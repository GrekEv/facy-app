# 🚀 Быстрая инструкция: Развертывание на Яндекс.Облаке

## 📋 Необходимые API ключи

Для работы приложения вам понадобятся:

1. **BOT_TOKEN** (обязательно) - токен Telegram бота от [@BotFather](https://t.me/BotFather)
2. **REPLICATE_API_KEY** (рекомендуется) - для генерации изображений и видео
   - Регистрация: [Replicate.com](https://replicate.com)
   - Получение ключа: Account → API Tokens → Create token
3. **OPENAI_API_KEY** (опционально) - для генерации видео через Sora
   - Регистрация: [OpenAI Platform](https://platform.openai.com)
   - Получение ключа: API Keys → Create new secret key
4. **HIGGSFIELD_API_KEY** (опционально) - альтернатива для генерации видео
   - Регистрация: [Higgsfield.ai](https://higgsfield.ai)
   - Получение ключа: API → Create new API key

**Минимальная конфигурация для старта:** BOT_TOKEN + REPLICATE_API_KEY

---

## Шаг 1: Создание виртуальной машины (5 минут)

1. **Откройте консоль Яндекс.Облака:**
   - Перейдите на https://console.cloud.yandex.ru
   - Войдите в свой аккаунт

2. **Создайте виртуальную машину:**
   - Нажмите синюю кнопку **"+ Создать ресурс"** в правом верхнем углу
   - Выберите **"Виртуальная машина"**

3. **Выберите образ загрузочного диска:**
   - Убедитесь, что выбрана вкладка **"Marketplace"** (по умолчанию)
   - В поиске или в списке найдите **"Ubuntu"**
   - Выберите **"Ubuntu"** (версия 24.04 или новее)
   - Образ будет автоматически добавлен как загрузочный диск

4. **Настройте диск:**
   - В разделе **"Диски и файловые хранилища"** вы увидите созданный диск
   - **Тип диска:** Оставьте **"SSD"** (рекомендуется)
   - **Размер диска:** Установите минимум **20 ГБ** (рекомендуется 50 ГБ для продакшена)
     - Используйте ползунок или введите значение вручную
     - Минимум: 10 ГБ, максимум: 8192 ГБ

5. **Выберите зону доступности:**
   - В разделе **"Расположение"** → **"Зона доступности"**
   - Выберите **`ru-central1-a`** (Москва)

6. **Выберите вычислительные ресурсы:**
   - В разделе **"Вычислительные ресурсы"** выберите вкладку **"Standard"** (по умолчанию)
   - **Для продакшена (рекомендуется):** Выберите **"Intel 4 vCPU"** с **"8 ГБ RAM"**
   - **Для тестирования (минимальная конфигурация):** Можно выбрать **"Intel 2 vCPU"** с **"2 ГБ RAM"** или **"4 ГБ RAM"**
     - ⚠️ Минимальная конфигурация может быть медленнее, но подходит для тестирования
     - 💡 Вы всегда можете изменить конфигурацию позже или создать новую ВМ
   - Если нужной конфигурации нет в списке, выберите **"Своя конфигурация"** и укажите:
     - **vCPU:** 4 (или 2 для тестирования)
     - **RAM:** 8 ГБ (или 4 ГБ для тестирования)
     - **Платформа:** Intel Ice Lake
     - **Гарантированная доля vCPU:** 100%

7. **Настройте сеть:**
   - В разделе **"Сетевые настройки"** → **"Сетевой интерфейс № 0"**
   - **Подсеть:** Оставьте по умолчанию (`default / default-ru-central1-a`) или создайте новую
   - **Публичный IP-адрес:** ✅ Выберите **"Автоматически"** (важно!)
   - **Защита от DDoS-атак:** Опционально (можно оставить выключенной)
   - **Группы безопасности:** Будет автоматически назначена группа безопасности (например: `default-sg-...`)
     - ⚠️ **Важно:** После создания ВМ нужно будет настроить правила в этой группе безопасности (см. Шаг 2)

8. **Настройте доступ:**
   - В разделе **"Доступ"** → **"SSH-ключ"**
   - Если видите предупреждение **"Нет доступных ssh-ключей"**, нажмите кнопку **"Добавить ключ"**
   
   **Вариант A: Проверка существующего SSH ключа**
   
   Сначала проверьте, есть ли у вас уже SSH ключ:
   
   ```bash
   # На Mac/Linux откройте Терминал
   # На Windows откройте PowerShell или Git Bash
   
   ls ~/.ssh/id_rsa.pub
   ```
   
   - Если файл существует, выполните:
     ```bash
     cat ~/.ssh/id_rsa.pub
     ```
     - Скопируйте весь вывод (начинается с `ssh-rsa` или `ssh-ed25519`)
     - Пример вывода: `ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQ... your_email@example.com`
     - Вставьте скопированный ключ в поле "SSH-ключ" в консоли Яндекс.Облака
   
   **Вариант B: Создание нового SSH ключа (подробная инструкция)**
   
   Если ключа нет, создайте его на вашем компьютере:
   
   **Шаг 1: Откройте терминал**
   - **Mac:** Откройте "Терминал" (Terminal) через Spotlight (Cmd+Space) или Finder → Программы → Утилиты
   - **Linux:** Откройте терминал (Ctrl+Alt+T или через меню приложений)
   - **Windows:** Откройте PowerShell или Git Bash (если установлен Git)
   
   **Шаг 2: Создайте SSH ключ**
   
   Выполните команду (замените `your_email@example.com` на ваш реальный email):
   
   ```bash
   ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
   ```
   
   **Что произойдет:**
   
   1. **Вопрос 1: "Enter file in which to save the key"**
      - Нажмите **Enter** (используется путь по умолчанию: `~/.ssh/id_rsa`)
      - Или укажите свой путь, если хотите
   
   2. **Вопрос 2: "Enter passphrase" (пароль для ключа)**
      - **Вариант 1:** Нажмите **Enter** дважды (ключ без пароля - проще для начала)
      - **Вариант 2:** Введите пароль и подтвердите его (более безопасно)
      - 💡 Рекомендация: для начала можно оставить без пароля, потом добавить
   
   3. После этого вы увидите сообщение:
      ```
      Your public key has been saved in /Users/ваше_имя/.ssh/id_rsa.pub
      ```
   
   **Шаг 3: Скопируйте публичный ключ**
   
   Выполните команду для отображения публичного ключа:
   
   ```bash
   cat ~/.ssh/id_rsa.pub
   ```
   
   Вы увидите что-то вроде:
   ```
   ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQC7... очень_длинная_строка ... your_email@example.com
   ```
   
   **Как скопировать ключ:**
   
   - **Mac/Linux:** Выделите мышью всю строку и скопируйте (Cmd+C или Ctrl+C)
   - **Windows PowerShell:** Выделите строку и нажмите Enter (она скопируется в буфер обмена)
   - **Windows Git Bash:** Выделите строку и щелкните правой кнопкой мыши → Copy
   - **Альтернативный способ (Mac/Linux):** Можно автоматически скопировать в буфер обмена:
     ```bash
     # Mac
     cat ~/.ssh/id_rsa.pub | pbcopy
     
     # Linux (требует установки xclip)
     cat ~/.ssh/id_rsa.pub | xclip -selection clipboard
     ```
   
   **Важно:** Скопируйте ВСЮ строку целиком (от `ssh-rsa` до конца с email), включая пробелы
   
   **Шаг 4: Вставьте ключ в Яндекс.Облако**
   
   - Вернитесь в консоль Яндекс.Облака
   - В поле **"SSH-ключ"** вставьте скопированный ключ
   - Ключ должен быть в одну строку
   
   **Логин администратора ВМ:**
   
   - В поле **"Логин*"** (под полем SSH-ключ) введите: **`ubuntu`**
   - ⚠️ **Важно:** Для образов Ubuntu логин всегда `ubuntu`
   - Для других образов логин может отличаться:
     - Ubuntu → `ubuntu`
     - CentOS → `centos`
     - Debian → `debian`
     - Но так как мы используем Ubuntu, всегда используйте `ubuntu`
   
   **Проверка:**
   - Убедитесь, что:
     - ✅ SSH-ключ вставлен (одна длинная строка, начинается с `ssh-rsa`)
     - ✅ Логин указан: `ubuntu`
     - ✅ Нет ошибок или предупреждений красным цветом
   
   **Если возникли проблемы:**
   
   - **Ключ не вставляется:** Убедитесь, что скопирована вся строка целиком, без переносов строк
   - **Ошибка формата ключа:** Проверьте, что ключ начинается с `ssh-rsa` или `ssh-ed25519`
   - **Не знаете, какой логин:** Для Ubuntu всегда используйте `ubuntu` (не `root`, не `admin`)

9. **Общая информация:**
   - **Имя ВМ:** Измените автоматически сгенерированное имя на `facy-app` (или оставьте по умолчанию)
   - **Описание:** Опционально, можно добавить описание

10. **Проверьте стоимость:**
    - В правой панели справа вы увидите примерную стоимость
    - **Для минимальной конфигурации (2 vCPU / 2 ГБ RAM / 20 ГБ SSD):** около 2500₽/месяц
    - **Для рекомендуемой конфигурации (4 vCPU / 8 ГБ RAM / 50 ГБ SSD):** около 4000-5000₽/месяц
    - 💰 **Бесплатный период:** Яндекс.Облако предоставляет **4000₽ на 60 дней** для новых пользователей!
      - Этого хватит для тестирования и запуска приложения
      - После бесплатного периода начнется платная подписка
    - Убедитесь, что вас устраивает стоимость

11. **Создайте ВМ:**
    - Нажмите кнопку **"Создать ВМ"** внизу страницы
    - Дождитесь создания ВМ (1-2 минуты)
    - После создания вы будете перенаправлены на страницу со списком ВМ

12. **Скопируйте публичный IP адрес:**
    - Найдите созданную ВМ `facy-app` в списке
    - Скопируйте **публичный IPv4 адрес** (например: `51.250.12.34`)
    - Сохраните этот адрес — он понадобится для подключения

## Шаг 2: Настройка групп безопасности (2 минуты)

⚠️ **Важно:** Без настройки групп безопасности вы не сможете подключиться к серверу и приложение не будет доступно из интернета!

1. **Найдите группу безопасности:**
   - При создании ВМ была автоматически назначена группа безопасности (например: `default-sg-enphh0lu4156jbt957h1`)
   - Вы можете найти её двумя способами:
     - **Способ 1:** В консоли перейдите в **"Virtual Private Cloud"** → **"Группы безопасности"**
     - **Способ 2:** Откройте вашу ВМ → перейдите на вкладку **"Сеть"** → там будет указана группа безопасности

2. **Откройте группу безопасности:**
   - Найдите группу безопасности (обычно начинается с `default-sg-` или имеет имя `default`)
   - Нажмите на неё, чтобы открыть

3. **Перейдите к правилам:**
   - В открывшейся странице перейдите на вкладку **"Правила"**
   - Вы увидите список существующих правил (если есть)

4. **Добавьте правила для входящего трафика:**
   - Нажмите кнопку **"Добавить правило"** или **"Создать правило"**
   - Добавьте следующие правила одно за другим:

   **Правило 1: SSH доступ (порт 22)**
   - **Направление:** Входящий трафик
   - **Протокол:** TCP
   - **Порт:** 22
   - **Источник:** CIDR (0.0.0.0/0) - для доступа с любого IP
     - Или укажите ваш IP для большей безопасности
   - **Описание:** SSH доступ
   - Нажмите **"Сохранить"**

   **Правило 2: HTTP доступ (порт 80)**
   - **Направление:** Входящий трафик
   - **Протокол:** TCP
   - **Порт:** 80
   - **Источник:** CIDR (0.0.0.0/0)
   - **Описание:** HTTP доступ
   - Нажмите **"Сохранить"**

   **Правило 3: HTTPS доступ (порт 443)**
   - **Направление:** Входящий трафик
   - **Протокол:** TCP
   - **Порт:** 443
   - **Источник:** CIDR (0.0.0.0/0)
   - **Описание:** HTTPS доступ
   - Нажмите **"Сохранить"**

   **Правило 4: Приложение (порт 8000)**
   - **Направление:** Входящий трафик
   - **Протокол:** TCP
   - **Порт:** 8000
   - **Источник:** CIDR (0.0.0.0/0)
   - **Описание:** Facy App порт
   - Нажмите **"Сохранить"**

5. **Проверьте правила:**
   - Убедитесь, что все 4 правила добавлены и активны
   - Правила должны быть в разделе **"Входящий трафик"**

6. **Примечание о безопасности:**
   - Для большей безопасности можно ограничить SSH доступ только вашим IP адресом
   - Но для начала можно оставить 0.0.0.0/0 для всех портов

## Шаг 3: Подключение к серверу (1 минута)

На вашем компьютере выполните:

```bash
ssh ubuntu@<ваш-ip-адрес>
```

Например:
```bash
ssh ubuntu@51.250.12.34
```

**Если подключение не удается:**
- Убедитесь, что ВМ запущена (статус "Работает" в консоли)
- Проверьте, что группы безопасности настроены правильно
- Попробуйте подключиться с указанием ключа:
  ```bash
  ssh -i ~/.ssh/id_rsa ubuntu@<ваш-ip-адрес>
  ```

## Шаг 4: Автоматический деплой (5 минут)

После подключения к серверу выполните одну команду:

```bash
curl -fsSL https://raw.githubusercontent.com/GrekEv/facy-app/main/deploy-cis.sh | bash
```

Скрипт автоматически:
- ✅ Установит Docker и Docker Compose
- ✅ Клонирует репозиторий
- ✅ Создаст необходимые директории
- ✅ Подготовит конфигурацию

**После выполнения скрипта:**
1. Выйдите из SSH: `exit`
2. Подключитесь снова: `ssh ubuntu@<ваш-ip-адрес>`
3. Перейдите в директорию: `cd facy-app`

## Шаг 5: Настройка переменных окружения (3 минуты)

Создайте файл `.env`:

```bash
nano .env
```

Добавьте следующие переменные (замените на ваши значения):

```env
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ
BOT_TOKEN=ваш_токен_от_BotFather
WEBAPP_URL=http://158.160.96.182:8000
ENVIRONMENT=production
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
HOST=0.0.0.0
PORT=8000

# ============================================
# API КЛЮЧИ ДЛЯ ГЕНЕРАЦИИ ИЗОБРАЖЕНИЙ
# ============================================

# Replicate (рекомендуется для изображений)
REPLICATE_API_KEY=ваш_replicate_api_ключ
REPLICATE_API_URL=https://api.replicate.com/v1

# ============================================
# API КЛЮЧИ ДЛЯ ГЕНЕРАЦИИ ВИДЕО
# ============================================

# OpenAI Sora (для генерации видео)
OPENAI_API_KEY=ваш_openai_api_ключ
SORA_MODEL=sora-1.0-pro  # или sora-2.0

# Higgsfield.ai (для генерации видео)
HIGGSFIELD_API_KEY=ваш_higgsfield_api_ключ
HIGGSFIELD_API_URL=https://api.higgsfield.ai

# Replicate (также поддерживает генерацию видео)
# Используйте тот же REPLICATE_API_KEY что и для изображений

# ============================================
# ВЫБОР ПРОВАЙДЕРОВ
# ============================================

# Для генерации изображений
IMAGE_GENERATION_PROVIDER=replicate  # replicate

# Для генерации видео (выберите один)
VIDEO_GENERATION_PROVIDER=replicate  # replicate, sora, higgsfield
```

---

## 📝 Как получить API ключи:

### 1. BOT_TOKEN (Telegram) - ОБЯЗАТЕЛЬНО

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `Facy AI`)
   - Введите username бота (например: `facy_ai_bot`)
4. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Добавьте токен в `.env` файл

### 2. REPLICATE_API_KEY (для изображений и видео)

**Replicate** - платформа для запуска AI моделей с API. Поддерживает генерацию изображений (FLUX, Stable Diffusion) и видео.

**Как получить ключ:**

1. Зарегистрируйтесь на [Replicate.com](https://replicate.com)
2. Нажмите **"Sign in"** (можно через GitHub или Google)
3. После входа перейдите в **"Account"** → **"API Tokens"**
4. Нажмите **"Create token"**
5. Скопируйте токен (начинается с `r8_...`)
6. Добавьте в `.env`:
   ```env
   REPLICATE_API_KEY=r8_ваш_токен_здесь
   ```

**Стоимость:** Pay-as-you-go (оплата за использование)
- Изображения: от $0.002 за изображение
- Видео: зависит от модели (обычно $0.05-0.20 за видео)

**Документация:** [Replicate API Docs](https://replicate.com/docs)

### 3. OPENAI_API_KEY (для Sora - генерация видео)

**OpenAI Sora** - продвинутая модель для генерации видео высокого качества.

**Как получить ключ:**

1. Зарегистрируйтесь на [OpenAI.com](https://platform.openai.com)
2. Перейдите в **"API Keys"** → **"Create new secret key"**
3. Скопируйте ключ (начинается с `sk-...`)
4. Добавьте в `.env`:
   ```env
   OPENAI_API_KEY=sk-ваш_ключ_здесь
   SORA_MODEL=sora-1.0-pro  # или sora-2.0
   ```

**Важно:** 
- Sora может быть доступен не всем пользователям (waitlist)
- Проверьте доступность в вашем аккаунте OpenAI
- Если Sora недоступен, используйте Replicate или Higgsfield

**Стоимость:** Зависит от тарифа OpenAI (обычно от $0.05 за секунду видео)

**Документация:** [OpenAI Sora](https://openai.com/sora)

### 4. HIGGSFIELD_API_KEY (для генерации видео)

**Higgsfield.ai** - платформа для генерации видео с помощью AI.

**Как получить ключ:**

1. Зарегистрируйтесь на [Higgsfield.ai](https://higgsfield.ai)
2. Перейдите в раздел **"API"** или **"Developers"**
3. Создайте новый API ключ
4. Скопируйте ключ
5. Добавьте в `.env`:
   ```env
   HIGGSFIELD_API_KEY=ваш_ключ_здесь
   HIGGSFIELD_API_URL=https://api.higgsfield.ai
   ```

**Стоимость:** Зависит от тарифа провайдера

**Документация:** [Higgsfield.ai](https://higgsfield.ai)

---

## 💡 Рекомендации по выбору провайдеров:

### Для генерации изображений:
- **Replicate** ⭐ (рекомендуется) - множество моделей, хорошее качество, доступная цена

### Для генерации видео:
- **Replicate** ⭐ (рекомендуется для старта) - доступно, множество моделей
- **OpenAI Sora** - лучшее качество, но может быть недоступен
- **Higgsfield.ai** - альтернатива с хорошим качеством

### Минимальная конфигурация для старта:
```env
BOT_TOKEN=ваш_токен
REPLICATE_API_KEY=ваш_ключ
IMAGE_GENERATION_PROVIDER=replicate
VIDEO_GENERATION_PROVIDER=replicate
```

### Полная конфигурация (если доступны все сервисы):
```env
BOT_TOKEN=ваш_токен
REPLICATE_API_KEY=ваш_ключ
OPENAI_API_KEY=ваш_ключ
HIGGSFIELD_API_KEY=ваш_ключ
IMAGE_GENERATION_PROVIDER=replicate
VIDEO_GENERATION_PROVIDER=sora  # или higgsfield
```

**Сохраните файл:**
- Нажмите `Ctrl+O`, затем `Enter`
- Нажмите `Ctrl+X` для выхода

## Шаг 6: Запуск приложения (1 минута)

```bash
cd facy-app
docker-compose -f docker-compose.prod.yml up -d
```

Проверьте статус:
```bash
docker-compose -f docker-compose.prod.yml ps
```

Просмотр логов:
```bash
docker-compose -f docker-compose.prod.yml logs -f
```

## Шаг 7: Проверка работы

1. **Проверьте API:**
   Откройте в браузере: `http://<ваш-ip>:8000/health`
   Должно вернуться: `{"status": "healthy"}`

2. **Проверьте Web App:**
   Откройте в браузере: `http://<ваш-ip>:8000`
   Должна открыться главная страница приложения

3. **Настройте Telegram бота:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - Отправьте `/mybots`
   - Выберите вашего бота
   - Выберите **"Bot Settings"** → **"Menu Button"**
   - Включите **"Menu Button"**
   - В поле **"URL"** введите: `http://<ваш-ip>:8000`
   - Нажмите **"Save"**

4. **Проверьте бота:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Нажмите кнопку "🚀 Открыть приложение"
   - Должна открыться Web App

## ✅ Готово!

Ваше приложение развернуто и готово к работе!

## 📝 Полезные команды

**Просмотр логов:**
```bash
docker-compose -f docker-compose.prod.yml logs -f
```

**Остановка приложения:**
```bash
docker-compose -f docker-compose.prod.yml down
```

**Перезапуск приложения:**
```bash
docker-compose -f docker-compose.prod.yml restart
```

**Обновление приложения:**
```bash
cd facy-app
git pull
docker-compose -f docker-compose.prod.yml up -d --build
```

## 🐛 Решение проблем

**Проблема: Порт не открыт**
- Проверьте группы безопасности в консоли Яндекс.Облака
- Убедитесь, что порт 8000 открыт для входящих соединений

**Проблема: Бот не отвечает**
- Проверьте токен в `.env`: `cat .env | grep BOT_TOKEN`
- Убедитесь, что токен правильный и бот активен в BotFather

**Проблема: Web App не открывается**
- Проверьте `WEBAPP_URL` в `.env` - должен быть доступен из интернета
- Убедитесь, что URL настроен в BotFather (Menu Button)

## 📚 Дополнительная информация

### Документация по деплою:
- **Полная инструкция:** [DEPLOY_FULL.md](DEPLOY_FULL.md)
- **Руководство по API сервисам:** [API_SERVICES_GUIDE.md](API_SERVICES_GUIDE.md)
- **Документация Яндекс.Облака:** https://cloud.yandex.ru/docs

### Документация по API сервисам:
- **[Replicate API](https://replicate.com/docs)** - Документация Replicate для генерации изображений и видео
- **[OpenAI Sora](https://openai.com/sora)** - Документация OpenAI Sora для генерации видео
- **[Higgsfield.ai](https://higgsfield.ai)** - Документация Higgsfield.ai для генерации видео
- **[Telegram Bot API](https://core.telegram.org/bots/api)** - Документация Telegram Bot API

---

**Нужна помощь?** Проверьте логи: `docker-compose -f docker-compose.prod.yml logs -f`

