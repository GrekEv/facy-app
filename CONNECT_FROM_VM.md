# 🔗 Подключение к PostgreSQL с ВМ через внутреннюю сеть

## ✅ Преимущества подключения через внутреннюю сеть:

1. **Быстрее** - низкая задержка, высокая скорость
2. **Безопаснее** - не нужен публичный доступ
3. **Бесплатно** - нет трафика через интернет
4. **Проще** - используйте тот же FQDN хост

## 📋 Что нужно:

1. ВМ и PostgreSQL кластер должны быть в **одной подсети**
2. Настроить группы безопасности для доступа из подсети
3. Использовать тот же FQDN хост (подключение автоматически пойдет через внутреннюю сеть)

## 🔧 Настройка:

### Шаг 1: Проверьте подсеть

**В консоли Яндекс.Облака:**

1. Откройте вашу ВМ → вкладка **"Сеть"**
2. Запомните **подсеть** (например: `default-ru-central1-a`)
3. Откройте кластер PostgreSQL → вкладка **"Обзор"**
4. Проверьте, что кластер в той же подсети

### Шаг 2: Настройте группы безопасности

**Для подключения через внутреннюю сеть:**

1. Откройте **"Virtual Private Cloud"** → **"Группы безопасности"**
2. Найдите группу безопасности вашего кластера PostgreSQL
3. Добавьте правило:

```
Направление: Входящий трафик
Протокол: TCP
Порт: 6432
Источник: CIDR
Значение: 10.0.0.0/24  (ваша подсеть - замените на реальную!)
Описание: PostgreSQL доступ из подсети
```

**Где найти вашу подсеть:**

- В консоли: **"Virtual Private Cloud"** → **"Подсети"**
- Или в настройках ВМ: **"Compute Cloud"** → **"Виртуальные машины"** → ваша ВМ → **"Сеть"**

### Шаг 3: Получите данные подключения

**В консоли Яндекс.Облака:**

1. Откройте кластер PostgreSQL
2. Перейдите в **"Подключения"** (Connections)
3. Скопируйте **FQDN хост** (например: `c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net`)
4. Используйте тот же хост для подключения с ВМ

### Шаг 4: Подключитесь с ВМ

**На вашей ВМ выполните:**

```bash
# Установка PostgreSQL клиента (если еще не установлен)
sudo apt update
sudo apt install -y postgresql-client

# Подключение к базе (используйте тот же FQDN хост!)
psql "host=c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net port=6432 dbname=facy_db user=facy_user password=postgresql_password sslmode=require"
```

**Или обновите .env на ВМ:**

```bash
cd ~/facy-app
nano .env
```

```env
DATABASE_URL=postgresql+asyncpg://facy_user:postgresql_password@c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net:6432/facy_db?ssl=require
```

## ⚠️ Важно:

- Используйте **тот же FQDN хост** - подключение автоматически пойдет через внутреннюю сеть
- Не нужно включать "Публичный доступ" для внутреннего подключения
- Группы безопасности должны разрешать доступ из вашей подсети
- SSL все равно рекомендуется (`ssl=require`)

## 🚀 Проверка подключения:

```bash
# С ВМ проверьте подключение
psql "host=c-xxxxx.rw.mdb.yandexcloud.net port=6432 dbname=facy_db user=facy_user password=postgresql_password sslmode=require" -c "SELECT version();"

# Или через приложение
curl http://localhost:8000/health
```

## 📝 Пример полной настройки на ВМ:

```bash
# 1. Подключитесь к ВМ
ssh ubuntu@158.160.96.182

# 2. Перейдите в директорию проекта
cd ~/facy-app

# 3. Обновите .env файл
nano .env

# 4. Добавьте/обновите DATABASE_URL:
DATABASE_URL=postgresql+asyncpg://facy_user:postgresql_password@c-9qpp92q6spvmgf13dts.rw.mdb.yandexcloud.net:6432/facy_db?ssl=require

# 5. Установите asyncpg (если еще не установлен)
pip3 install asyncpg==0.29.0

# 6. Перезапустите приложение
docker compose -f docker-compose.prod.yml restart
# или
python3 run_api.py
```

## ✅ Готово!

Теперь ваше приложение подключено к PostgreSQL через внутреннюю сеть - быстро, безопасно и бесплатно!


