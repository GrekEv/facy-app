# ⚡ БЫСТРЫЙ СТАРТ - Деплой на сервисы СНГ

## 🎯 Выберите сервис:

### 🟢 Yandex Cloud (рекомендуется)
- ✅ Бесплатный период: 4000₽ на 60 дней
- ✅ Полностью доступен в СНГ
- ✅ Принимает оплату из РФ и РБ

### 🟡 Timeweb Cloud
- ✅ От 200₽/месяц
- ✅ Российский хостинг
- ✅ Простая настройка

### 🟡 Selectel
- ✅ От 300₽/месяц
- ✅ Российский облачный провайдер
- ✅ Хорошая производительность

### 🟡 Beget
- ✅ От 150₽/месяц
- ✅ Российский хостинг
- ✅ Простое управление

---

## 🚀 Деплой за 3 шага:

### Шаг 1: Создайте сервер (5 минут)

#### Yandex Cloud:
1. Откройте: https://cloud.yandex.ru
2. Зарегистрируйтесь (бесплатный период 4000₽)
3. Compute Cloud → Виртуальные машины → Создать ВМ
4. Настройки:
   - ОС: Ubuntu 22.04 LTS
   - vCPU: 2, RAM: 4 GB, Диск: 20 GB SSD
   - Публичный IP: Включить
5. Скопируйте IP адрес

#### Timeweb / Selectel / Beget:
1. Зарегистрируйтесь на сайте провайдера
2. Создайте VPS сервер
3. Выберите Ubuntu 22.04
4. Скопируйте IP адрес

---

### Шаг 2: Подключитесь и запустите скрипт (5 минут)

```bash
# Подключение к серверу
ssh ubuntu@ваш-ip-адрес

# Скачивание и запуск скрипта деплоя
curl -fsSL https://raw.githubusercontent.com/GrekEv/facy-app/main/deploy-cis.sh | bash
```

Или вручную:

```bash
# Клонирование репозитория
git clone https://github.com/GrekEv/facy-app.git
cd facy-app

# Запуск скрипта
chmod +x deploy-cis.sh
./deploy-cis.sh
```

---

### Шаг 3: Настройте переменные (2 минуты)

```bash
# Редактирование .env
nano .env
```

Обновите:
```env
BOT_TOKEN=8254778202:AAH-1RebJBOKpr5fKorcIcFHqKAihbCBQ_o
WEBAPP_URL=https://ваш-ip:8000
ENVIRONMENT=production
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

```bash
# Перезапуск
docker-compose -f docker-compose.prod.yml restart
```

---

## ✅ Готово!

Откройте в браузере: `http://ваш-ip:8000`

---

## 🔧 Дополнительная настройка:

### Настройка домена (опционально)

1. Настройте DNS записи у регистратора:
   ```
   A запись: ваш-домен.ru → ваш-ip
   ```

2. Установите Nginx:
   ```bash
   sudo apt install nginx -y
   ```

3. Создайте конфигурацию (см. DEPLOY_CIS.md)

4. Установите SSL:
   ```bash
   sudo apt install certbot python3-certbot-nginx -y
   sudo certbot --nginx -d ваш-домен.ru
   ```

---

## 📊 Управление:

```bash
# Просмотр логов
docker-compose -f docker-compose.prod.yml logs -f

# Остановка
docker-compose -f docker-compose.prod.yml down

# Запуск
docker-compose -f docker-compose.prod.yml up -d

# Перезапуск
docker-compose -f docker-compose.prod.yml restart
```

---

## 🐛 Решение проблем:

### Порт не открыт
```bash
# Проверка firewall
sudo ufw status

# Открытие порта
sudo ufw allow 8000/tcp
```

### Docker не запускается
```bash
# Проверка логов
docker-compose -f docker-compose.prod.yml logs

# Проверка статуса
docker-compose -f docker-compose.prod.yml ps
```

---

**Подробная инструкция:** [DEPLOY_CIS.md](DEPLOY_CIS.md)

**Время деплоя: ~15 минут** ⏱️

