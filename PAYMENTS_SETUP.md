# 💳 Настройка платежных систем

Инструкция по подключению всех платежных методов для Facy.

## 🎯 Поддерживаемые методы оплаты

1. **Telegram Payments** - встроенные платежи Telegram
2. **Visa / Mir** - через YooKassa
3. **Google Pay / Samsung Pay** - через Stripe или YooKassa
4. **Криптовалюта** - BTC, ETH, USDT

---

## 1. Telegram Payments

### Настройка

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots` → выберите бота
3. Выберите **"Payments"** → **"Configure"**
4. Следуйте инструкциям BotFather для настройки платежей
5. Укажите валюту: **RUB** (рубли)
6. Получите токен провайдера платежей

### Переменные окружения

```env
# Telegram Payments использует токен от платежного провайдера
# Укажите токен в handlers/payments.py в методе process_telegram_payment
# Или используйте переменную STRIPE_SECRET_KEY для тестирования
```

### Тестирование

Telegram Payments работает только в продакшене. Для тестирования используйте тестовые данные.

---

## 2. YooKassa (Visa / Mir)

### Регистрация

1. Зарегистрируйтесь на https://yookassa.ru
2. Пройдите верификацию
3. Получите Shop ID и Secret Key

### Переменные окружения

```env
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
```

### Установка библиотеки

```bash
pip install yookassa
```

### Настройка в коде

Обновите `handlers/payments.py` - метод `process_card_payment`:

```python
import yookassa
from yookassa import Payment, Configuration

Configuration.account_id = settings.YOOKASSA_SHOP_ID
Configuration.secret_key = settings.YOOKASSA_SECRET_KEY

# Создание платежа
payment = Payment.create({
    "amount": {
        "value": str(transaction.price),
        "currency": "RUB"
    },
    "confirmation": {
        "type": "redirect",
        "return_url": f"{settings.WEBAPP_URL}/payment/success"
    },
    "capture": True,
    "description": f"Покупка {transaction.amount} поинтов"
})
```

---

## 3. Stripe (Google Pay / Samsung Pay)

### Регистрация

1. Зарегистрируйтесь на https://stripe.com
2. Получите API ключи (Publishable Key и Secret Key)
3. Настройте Google Pay и Samsung Pay в панели Stripe

### Переменные окружения

```env
STRIPE_SECRET_KEY=sk_test_...  # Тестовый ключ
STRIPE_PUBLISHABLE_KEY=pk_test_...  # Публичный ключ
```

### Установка библиотеки

```bash
pip install stripe
```

### Настройка в коде

Обновите `handlers/payments.py` - метод `process_wallet_payment`:

```python
import stripe

stripe.api_key = settings.STRIPE_SECRET_KEY

# Создание Payment Intent
intent = stripe.PaymentIntent.create(
    amount=int(transaction.price * 100),  # В копейках
    currency='rub',
    payment_method_types=['card', 'google_pay', 'samsung_pay']
)
```

---

## 4. Криптовалюта (BTC, ETH, USDT)

### Настройка кошельков

1. Создайте кошельки для каждой криптовалюты:
   - Bitcoin (BTC)
   - Ethereum (ETH)
   - Tether (USDT) - на сети Ethereum или Tron

2. Получите адреса кошельков

### Переменные окружения

```env
CRYPTO_WALLET_ADDRESS_BTC=ваш_btc_адрес
CRYPTO_WALLET_ADDRESS_ETH=ваш_eth_адрес
CRYPTO_WALLET_ADDRESS_USDT=ваш_usdt_адрес

# Опционально: API ключ для проверки транзакций
CRYPTO_API_KEY=ваш_api_ключ  # BlockCypher, Blockchain.info и т.д.
```

### Проверка транзакций

Для автоматической проверки криптоплатежей используйте API:

- **BlockCypher** (https://www.blockcypher.com) - для BTC и ETH
- **Blockchain.info API** - для BTC
- **Etherscan API** - для ETH

Пример проверки BTC транзакции:

```python
import requests

def check_btc_transaction(address, amount):
    url = f"https://api.blockcypher.com/v1/btc/main/addrs/{address}/balance"
    response = requests.get(url)
    data = response.json()
    # Проверка баланса и транзакций
```

---

## 5. Настройка промокодов

### Создание промокода через админ-панель

Промокоды создаются через админ-панель (будет реализована) или напрямую в БД:

```python
from database.models import PromoCode
from datetime import datetime, timedelta

promo = PromoCode(
    code="WELCOME10",
    description="Скидка 10% для новых пользователей",
    discount_type="percent",
    discount_value=10.0,
    max_uses=100,
    valid_until=datetime.utcnow() + timedelta(days=30),
    is_active=True
)
```

### Типы скидок

- **percent** - процентная скидка (например, 10%)
- **fixed** - фиксированная сумма (например, 100₽)

---

## 6. Настройка вывода средств

### Минимальный вывод

По умолчанию: **100₽**

Настройка в `config.py`:
```python
MIN_WITHDRAWAL: float = 100.0
WITHDRAWAL_FEE_PERCENT: float = 5.0  # Комиссия 5%
```

### Методы вывода

1. **На карту** - через YooKassa или другой провайдер
2. **Криптовалюта** - на указанный адрес
3. **Электронные кошельки** - через соответствующие API

---

## 7. Реферальная система

### Настройка

В `config.py`:
```python
REFERRAL_COMMISSION_PERCENT: float = 50.0  # 50% от платежа реферала
```

### Как работает

1. Пользователь получает уникальный реферальный код при регистрации
2. Другие пользователи регистрируются по его ссылке
3. При оплате рефералом, рефереру начисляется 50% от суммы платежа
4. Средства накапливаются в `referral_balance`
5. Можно вывести через систему вывода

---

## 8. Тестирование платежей

### Telegram Payments

- Работает только в продакшене
- Используйте тестовые данные от провайдера

### YooKassa

- Тестовый режим: используйте тестовые карты
- Тестовая карта: `5555 5555 5555 4444`
- Срок действия: любая будущая дата
- CVV: любой 3-значный код

### Stripe

- Тестовый режим: используйте ключи с префиксом `sk_test_`
- Тестовая карта: `4242 4242 4242 4242`
- Срок действия: любая будущая дата
- CVV: любой 3-значный код

### Криптовалюта

- Используйте тестовые сети (testnet) для разработки
- Для продакшена используйте реальные адреса

---

## 9. Безопасность

### Рекомендации

1. **Никогда не храните секретные ключи в коде**
2. Используйте переменные окружения
3. Включите HTTPS для всех платежных операций
4. Логируйте все платежные транзакции
5. Регулярно проверяйте логи на подозрительную активность

### Webhook безопасность

Для webhook от платежных систем:

1. Проверяйте подпись запроса
2. Используйте HTTPS
3. Валидируйте данные перед обработкой
4. Логируйте все webhook запросы

---

## 10. Мониторинг

### Логирование

Все платежи логируются в таблицу `transactions` и `audit_logs`.

### Проверка статуса

```python
# Через API
GET /api/payments/status/{transaction_id}

# Через сервис
status = await PaymentService.get_payment_status(session, transaction_id)
```

---

## 📝 Чеклист настройки

- [ ] Настроен Telegram Payments в BotFather
- [ ] Зарегистрирован аккаунт YooKassa
- [ ] Зарегистрирован аккаунт Stripe
- [ ] Созданы криптокошельки (BTC, ETH, USDT)
- [ ] Добавлены все переменные окружения
- [ ] Установлены необходимые библиотеки
- [ ] Протестированы все методы оплаты
- [ ] Настроены webhook для платежных систем
- [ ] Настроена система промокодов
- [ ] Настроена реферальная система

---

**Готово!** Все платежные системы настроены и готовы к работе! 💳

