# 🚀 Деплой Facy на сервисы СНГ

Инструкция по развертыванию на сервисах, доступных в СНГ с оплатой из России и Беларуси.

## 🎯 Рекомендуемые варианты

### Вариант 1: Yandex Cloud (рекомендуется) ⭐
- ✅ Полностью доступен в СНГ
- ✅ Принимает оплату из РФ и РБ
- ✅ Бесплатный период: 4000₽ на 60 дней
- ✅ Поддержка Docker и виртуальных машин
- ✅ Объектное хранилище для статики

### Вариант 2: Timeweb Cloud
- ✅ Российский хостинг
- ✅ Принимает оплату из РФ и РБ
- ✅ Простая настройка
- ✅ VPS и облачные серверы

### Вариант 3: Selectel
- ✅ Российский облачный провайдер
- ✅ Принимает оплату из РФ и РБ
- ✅ Хорошая производительность
- ✅ Объектное хранилище

### Вариант 4: Beget
- ✅ Российский хостинг
- ✅ Принимает оплату из РФ и РБ
- ✅ Простое управление
- ✅ VPS серверы

---

## 🟢 Вариант 1: Yandex Cloud

### Преимущества
- Бесплатный период 4000₽ на 60 дней
- Отличная документация на русском
- Поддержка Docker контейнеров
- Объектное хранилище (S3-совместимое)
- CDN для статики
- Удобное управление через веб-интерфейс

### Шаг 1: Регистрация

1. Откройте https://cloud.yandex.ru
2. Зарегистрируйтесь (можно через Яндекс ID)
3. Подтвердите телефон
4. Получите бесплатный период 4000₽

### Шаг 2: Создание виртуальной машины

1. В консоли Yandex Cloud выберите "Compute Cloud" → "Виртуальные машины"
2. Нажмите "Создать ВМ"
3. Настройки:
   - **Имя:** `facy-app`
   - **Зона доступности:** `ru-central1-a` (Москва)
   - **Платформа:** Intel Ice Lake
   - **vCPU:** 2
   - **RAM:** 4 ГБ
   - **Диск:** 20 ГБ SSD
   - **Образ:** Ubuntu 22.04 LTS
   - **Публичный IP:** Включить
   - **Пользователь:** `ubuntu`
   - **SSH ключ:** Добавьте ваш публичный SSH ключ

4. Нажмите "Создать"

### Шаг 3: Подключение к серверу

```bash
ssh ubuntu@<ваш-ip-адрес>
```

### Шаг 4: Установка Docker

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление пользователя в группу docker
sudo usermod -aG docker ubuntu

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Перезагрузка (или выйдите и войдите снова)
exit
```

### Шаг 5: Клонирование репозитория

```bash
# Установка Git
sudo apt install git -y

# Клонирование репозитория
git clone https://github.com/GrekEv/facy-app.git
cd facy-app
```

### Шаг 6: Настройка переменных окружения

```bash
# Создание .env файла
nano .env
```

Добавьте:

```env
BOT_TOKEN=8254778202:AAH-1RebJBOKpr5fKorcIcFHqKAihbCBQ_o
WEBAPP_URL=https://ваш-домен.ru
ENVIRONMENT=production
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
HOST=0.0.0.0
PORT=8000
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 7: Запуск через Docker Compose

```bash
# Запуск приложения
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

### Шаг 8: Настройка домена (опционально)

1. В Yandex Cloud: "Network" → "Load Balancer"
2. Создайте балансировщик нагрузки
3. Настройте DNS записи у вашего регистратора домена

### Шаг 9: Настройка статики (опционально)

Для статики можно использовать:
- Yandex Object Storage + CDN
- Или просто отдавать с того же сервера (FastAPI уже настроен)

---

## 🟡 Вариант 2: Timeweb Cloud

### Регистрация

1. Откройте https://timeweb.cloud
2. Зарегистрируйтесь
3. Выберите тариф (есть бесплатный тестовый период)

### Создание VPS

1. В панели управления: "Облачные серверы" → "Создать сервер"
2. Выберите:
   - **ОС:** Ubuntu 22.04
   - **Конфигурация:** 2 CPU, 4 GB RAM, 20 GB SSD
   - **Регион:** Москва

3. После создания подключитесь по SSH и следуйте шагам 3-7 из варианта с Yandex Cloud

---

## 🟡 Вариант 3: Selectel

### Регистрация

1. Откройте https://selectel.ru
2. Зарегистрируйтесь
3. Пополните баланс (минимум 100₽)

### Создание сервера

1. В панели: "Облачные серверы" → "Создать"
2. Выберите конфигурацию
3. Подключитесь по SSH и следуйте шагам 3-7 из варианта с Yandex Cloud

---

## 🟡 Вариант 4: Beget

### Регистрация

1. Откройте https://beget.com
2. Зарегистрируйтесь
3. Выберите тариф VPS

### Создание VPS

1. В панели: "VPS" → "Заказать"
2. Выберите конфигурацию
3. Подключитесь по SSH и следуйте шагам 3-7 из варианта с Yandex Cloud

---

## 🔧 Обновление docker-compose.yml для продакшена

Создайте `docker-compose.prod.yml`:

```yaml
version: '3.8'

services:
  api:
    build: .
    container_name: facy-api
    command: python run_api.py
    ports:
      - "8000:8000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - WEBAPP_URL=${WEBAPP_URL}
      - ENVIRONMENT=production
      - DATABASE_URL=${DATABASE_URL}
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./generated:/app/generated
    restart: unless-stopped
    networks:
      - facy-network

  bot:
    build: .
    container_name: facy-bot
    command: python main.py
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - WEBAPP_URL=${WEBAPP_URL}
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./data:/app/data
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - facy-network

networks:
  facy-network:
    driver: bridge
```

Запуск:
```bash
docker-compose -f docker-compose.prod.yml up -d
```

---

## 🔒 Настройка firewall

```bash
# Установка UFW
sudo apt install ufw -y

# Разрешить SSH
sudo ufw allow 22/tcp

# Разрешить HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Разрешить порт приложения (если не используете 80/443)
sudo ufw allow 8000/tcp

# Включить firewall
sudo ufw enable
```

---

## 🌐 Настройка Nginx (рекомендуется)

Для продакшена лучше использовать Nginx как reverse proxy:

```bash
# Установка Nginx
sudo apt install nginx -y

# Создание конфигурации
sudo nano /etc/nginx/sites-available/facy
```

Добавьте:

```nginx
server {
    listen 80;
    server_name ваш-домен.ru;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Статические файлы
    location /static/ {
        alias /путь/к/проекту/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

Активируйте:
```bash
sudo ln -s /etc/nginx/sites-available/facy /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## 🔐 SSL сертификат (Let's Encrypt)

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получение сертификата
sudo certbot --nginx -d ваш-домен.ru

# Автоматическое обновление
sudo certbot renew --dry-run
```

---

## 📊 Мониторинг и логи

```bash
# Просмотр логов Docker
docker-compose logs -f

# Просмотр логов API
docker-compose logs -f api

# Просмотр логов бота
docker-compose logs -f bot

# Статус контейнеров
docker-compose ps
```

---

## 💰 Стоимость

### Yandex Cloud
- Бесплатный период: 4000₽ на 60 дней
- После: ~500-1000₽/месяц за базовую конфигурацию

### Timeweb Cloud
- От 200₽/месяц за базовый VPS

### Selectel
- От 300₽/месяц за базовый сервер

### Beget
- От 150₽/месяц за базовый VPS

---

## ✅ Проверка работы

1. **API:** `http://ваш-ip:8000/health` → должно быть `{"status": "healthy"}`
2. **Web App:** `http://ваш-ip:8000` → должна открыться главная страница
3. **Бот:** Откройте в Telegram → должно работать

---

## 🐛 Решение проблем

### Порт не открыт
- Проверьте firewall: `sudo ufw status`
- Проверьте настройки безопасности в панели облачного провайдера

### Docker не запускается
- Проверьте логи: `docker-compose logs`
- Убедитесь, что порт 8000 свободен: `sudo netstat -tulpn | grep 8000`

### Бот не отвечает
- Проверьте `BOT_TOKEN` в `.env`
- Проверьте логи бота: `docker-compose logs bot`

---

## 📚 Дополнительные ресурсы

- [Yandex Cloud Docs](https://cloud.yandex.ru/docs)
- [Timeweb Docs](https://timeweb.com/ru/docs)
- [Selectel Docs](https://selectel.ru/docs)
- [Beget Docs](https://beget.com/ru/kb)

---

**Готово! Ваше приложение развернуто на сервисах СНГ!** 🎉

