# 🚂 Настройка Railway - ГОТОВЫЕ ДАННЫЕ

## Быстрая настройка

1. **Откройте Railway:**
   - https://railway.app
   - Войдите через GitHub

2. **Создайте проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Найдите и выберите `GrekEv/facy-app`
   - Railway автоматически начнет деплой

3. **Добавьте переменные окружения:**

   В Railway Dashboard → ваш проект → Variables → New Variable

   Добавьте эти переменные:

   ```
   BOT_TOKEN = ваш_токен_от_BotFather
   ```
   *(См. TOKENS.md для вашего токена)*

   ```
   WEBAPP_URL = https://ваш-vercel-url.vercel.app
   ```
   *(Обновите после деплоя на Vercel)*

   ```
   ENVIRONMENT = production
   ```

4. **Получите Railway URL:**
   - В настройках сервиса: Settings → Generate Domain
   - Или используйте автоматически созданный домен
   - Скопируйте URL (например: `https://facy-app.up.railway.app`)

5. **Настройте процессы:**
   - Railway автоматически определит `Procfile`
   - Создаст два процесса:
     - `web` - FastAPI сервер (автоматически)
     - `worker` - Telegram бот (может потребоваться создать вручную)
   
   Если `worker` не создался:
   - Settings → Service Source → Add Service
   - Empty Service
   - Start Command: `python main.py`
   - Название: `worker`

## ✅ Готово!

После настройки Railway переходите к Vercel.

---

**Railway URL:** Скопируйте его, он понадобится для Vercel!

